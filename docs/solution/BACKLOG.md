# Weaver — Backlog

What's left to build. For what's already built, see [`Capabilities.md`](./Capabilities.md). For completeness-claim rules, see [`Claim_Register.md`](./Claim_Register.md). For permanent non-goals (with reasons), see [`OOS_Registry.md`](./OOS_Registry.md).

**Update rule:** when an item ships, remove its row here and reflect it in `Capabilities.md` / `Claim_Register.md` as appropriate.

## P1 — Correctness and coverage gaps

| Item | Description |
|---|---|
| **`unresolved-outbound-target` residual class — designed 2026-09-02, not yet built** | Closes a real, previously-flagged gap: `unresolved-http-target`/`unresolved-env-target` `CROSS_DOMAIN_UNRESOLVED` ignored-items (a real outbound-HTTP call or ConfigMap value with no distinguishable target owner) are correctly never fabricated into a relationship, but were also never resurfaced for review — found reviewing a real scan (a reference Java microservices banking sample) where the 3 most architecturally central edges in that repo's own published diagram were silently missing for exactly this reason. Full design in `Architecture_as_Code_Solution_Design_v2.md` §7.1 (revised eligibility rule) and `Architect_Residual_Review_Session.md` §3.2 (the concrete Tier B class, producer wiring, and batch-drafting justification). **Not yet implemented** — needs: `hitl-review-trigger.ts` wiring for the new trigger, `triage.py` `_TRIGGER_MAP` + `cards.py` template (reuses `single-candidate-below-threshold`'s shape), a non-interactive batch-drafting entry point in `tools/review-session/` (extends `draft_tier_b.py`/`llm_common.py`, never a new parallel LLM system), and a canonical-CALM-plus-drafts preview/merge tool (named, not yet designed in code-level detail). |
|---|---|
| **CodeGraph unit extraction degrades under repeated same-process invocation** | Found 2026-08-13 investigating a reference-platform regression-test count mismatch (94 vs 60). A fresh, isolated `node dist/orchestration/run-slice.js` process against the reference Java/JAX-RS banking platform's core module alone gives 94 relationships deterministically across repeated cold runs — the raw Graphify structural graph is byte-identical each time (8553 nodes/21277 edges). Invoking the same scan repeatedly via `execFileSync` from within one long-lived Node process (this pipeline's own regression-test harness shape) instead gives a lower but internally-consistent count (60-64 observed) — same raw Graphify graph, but fewer nodes resolve to a CodeGraph-typed unit. CodeGraph's own per-invocation extraction silently returns fewer units under repeated same-process load, no error or warning surfaced. Not root-caused (closed-source SDK; candidate causes include an internal timeout/budget or memory/fd contention across repeated spawns) — `pipeline/test/regression.test.js`'s reference-platform-core R0/R2b test was hardened to assert a floor instead of an exact count as an immediate mitigation, but the underlying silent degradation is real and undiagnosed. **Cuts against this pipeline's core "deterministic" claim under this one specific condition** (repeated same-process invocation, not the normal single-invocation `run-slice` product usage) — worth its own investigation before this pipeline is ever run as a long-lived service rather than a per-invocation CLI. |
| **JDBC read-service-vs-table-owner ambiguity — remains a documented limitation, not yet a scoped fix** | Already a named, disclosed limitation (`persistence-import-vs-ownership-ambiguous` in `scope-limitations.yml`); Phase A investigated further (`soln/bug3-jdbc-ownership-phase-a-memo.md`) and found no reliable, corroborated signal: `RowMapper<T>` presence is FALSIFIED as a distinguishing signal (present on the flagship false-positive class itself, `ChargeReadPlatformServiceImpl`, and on 55% of JdbcTemplate-importing files overall — doesn't separate real owners from read-only query services); a bare `@Service`/`@Component` stereotype does NOT already exclude a class either (confirmed via `JournalEntryRunningBalanceUpdateServiceImpl`, `@Service`-annotated and still typed `database`); a naming convention (`*ReadPlatformServiceImpl`) is single-repo-sourced, not corroborated. **Constraint for any future attempt**: `ChargeReadPlatformServiceImpl` has no other mechanism giving it unit existence at all — `Claim_Register.md`'s gold-scored `R2-multi-root-access-terminal` row depends on it resolving to a unit; a fix must only ever change `kind` from `database` to `service`, never suppress unit creation, or it regresses that claim. **Candidate approach identified but not yet tested (2026-08-13): `docs/solution/Engine_Capability_Research_Java_SpringBoot_JAXRS.md` §4.3 — every falsified signal above (`RowMapper`, stereotype presence, naming) is presence-based; the real distinguishing fact is behavioral (does this class actually execute a query through the client it imports, or only wire it through). That's what real data-flow/taint analysis answers and presence-based AST matching structurally cannot — CodeQL's Java data-flow library is the first tool this project has access to that could plausibly resolve this. Untested hypothesis, not a result. **Second, independent real-repo instance found 2026-08-13 while building T-LR-2 against the reference governance platform**: `LegalEntityRelationshipKindService` (a `service`-named class, real import-only persistence evidence, weight 20) resolves as the terminal store of a real direct-delegate chain (`BulkUploadLegalEntityRelationshipEndpoint -> [bridge] -> LegalEntityRelationshipKindService`) — the same false-positive class as `ChargeReadPlatformServiceImpl`, now confirmed in a second, structurally different codebase (the reference banking platform's JAX-RS/JDBC vs the reference governance platform's Spark-routing/jOOQ), corroborating that this isn't a reference-platform-specific naming quirk.** |
| **Possible bare-identifier collision inside multi-hop bridge detection — NOT YET REPRODUCED** | Flagged by review of the stereotype-name-collision fix (`graphify-reconciler.ts`'s `isBareNameCollision`), not independently confirmed with real evidence. `multi-hop-bridge-detector.ts` walks the same raw Graphify `imports`/`references` edges via its own separate loop, using the identical bare-label-only node resolution the collision bug is rooted in — but its bridge-candidacy gate (`if (nodeToUnit.has(bridgeNodeId)) continue`) requires the target to have ZERO existing units, which the evidenced collision shape (`Component`/`Response`, both real `@Entity` classes with existing units) structurally cannot satisfy — so the fixed mechanism's exact evidenced shape cannot reach this path. A theoretically-reachable variant (a bare-name collision landing on a genuinely zero-unit interface that is itself `implements`-ed elsewhere by a real persistence/messaging unit) has not been reproduced against a real repo. Needs its own real-evidence pass before scoping a fix, per this project's own discipline — do not extend the collision check here speculatively. **UPDATE 2026-09-02 — a DIFFERENT, real sub-case now reproduced and fixed**: a live scan of the reference Java microservices banking sample (its ledger service — `ledgerwriter`/`balancereader`/`transactionhistory`, 3 independently-deployed services each defining their own same-named `Transaction`/`TransactionRepository` classes) found `isBareNameCollision`'s ORIGINAL no-import branch always treated "no explicit import" as "can't disprove, leave it" — correct for its original target case, but wrong for the very common shape of several genuinely independent packages/services each defining an unrelated, identically-named class with no import needed (same-package self-reference). Fixed at all 3 real call sites that share this mechanism: `graphify-reconciler.ts`'s direct edge emission, and `multi-hop-bridge-detector.ts`'s r2c AND R2b second-hop resolution AND its own first-hop bridge-candidate identification (a 4th, distinct instance found via debug tracing after the first 3 fixes still left one fabricated edge). A second real false-positive was then found and fixed in the SAME session: the new same-package-mismatch check initially over-fired on a legitimate DI/interface-implementation edge (the reference Java/JAX-RS banking platform's security module, `TwoFactorConfigurationApiResource -> SpringSecurityPlatformSecurityContext`, resolved via `implements PlatformSecurityContext`, not a direct same-name reference) — fixed by gating the same-package-mismatch branch on the destination's bare name being LITERALLY present as a token in the source file (`javaFileReferencesBareName`, `java-import-resolver.ts`), so an edge that arrived via a different mechanism (interface dispatch) is never treated as a same-name-collision case. Full regression suite (191/191) and a live re-scan of both real repos confirm zero fabricated cross-service edges and zero lost legitimate edges. **The row's OTHER sub-case — `implementersByTarget`, the first-hop `implements`-edge WALK described below in the "Bare-implementer-list collision" row — remains open and unreproduced, and is unaffected by this fix.** |
| **Exception classes mistaken for multi-hop bridge candidates** | Found verifying T-LR-3's real regression against a reference Java/JAX-RS banking platform's charge module: `ChargeRepositoryWrapper` (a real, bare-`@Service` class T-LR-3's new catalogue row correctly makes visible as a bridge SOURCE for the first time) references two real, in-repo exception classes it throws (`ChargeIsNotActiveException`, `ChargeNotFoundException`) — both produce zero TypedUnits (correctly — an exception isn't an architectural unit), pass `isRealBridgeCandidate` (they're real, in-repo files, not external/annotation noise), and so are examined as bridge candidates and honestly refused (0 implementers) rather than fabricated. Not a fabrication bug — no relationship is emitted — but real, avoidable noise in `ignoredItems`. **Evidence pass 2026-08-19 (not built):** Graphify emits `inherits` (not `extends`) for those exception classes, targeting the out-of-root parent (`abstractplatformdomainruleexception`) — the chain cannot be walked to `java.lang.Exception` on a charge-only scan. Presence of *any* `inherits` edge is **falsified** as a discriminator: the same relation exists on real bridge-adjacent types (`ChargeRepository extends JpaRepository`, 20 interface-extends in the reference banking platform's charge/core modules, 15 in the reference governance platform, and 2 real DAOs there that extend a base). A name-suffix or `/exception/` path heuristic remains forbidden (`OOS-sample-repo-detectors`). Still needs a discriminator that is not presence-of-inherits and not a name. |
| **Bare-implementer-list collision, T-LR-3's stereotype-disambiguation branch** | `multi-hop-bridge-detector.ts`'s new `r2-stereotype` branch is the first mechanism in this file to actively PICK a winner from the raw `implementers` list rather than only ever refusing under ambiguity — re-assessed against the EXISTING \"Possible bare-identifier collision inside multi-hop bridge detection\" row below (same file, same underlying `isBareNameCollision` gap) and found NOT covered by that row's own reasoning: that reasoning is about the bridge TARGET (structurally unreachable, since a bridge candidate requires zero existing units), not the IMPLEMENTER list this new branch walks. Two real, differently-packaged classes sharing one bare name could theoretically cause Graphify's bare-label node resolution to misattribute an `implements` edge, inflating a bridge's real implementer count. Not reproduced against real data (2026-08-16 real-repo verification found no such case) — a real evidence pass is the correct next step if this is ever observed, not a speculative fix now. See `scope-limitations.yml`'s `bare-implementer-list-collision-not-reproduced`. |
| **Isolated-node completeness flag (`S6`) — shipped 2026-08-22** | `coverage-report.ts`'s `computeCompleteness` now counts every unit with zero relationships touching it in either direction (`completeness.isolatedNodeCount`) and pushes an `S6-isolated-nodes` `silenceFlags` entry when non-zero. **Excludes the synthetic `system`-node `composed-of` edge by construction, not by explicit filtering**: `computeCompleteness` runs on `typed-facts.json`'s own `TypedRelationship[]`, strictly BEFORE `system-node-builder.ts` ever builds that synthetic edge in the CALM-generation pass — the edge doesn't exist yet at the point S6 is computed, so no exclusion code was needed. Design decision (soft vs. hard gate) resolved by the project owner: **soft flag by default**, same posture as S1/S2/S5, with `--strict-isolated-nodes` as the opt-in hard gate (precedent: `--strict-detect`) — added to `README.md`'s CLI reference table. 4 new regression tests (unit-level S6 computation including a `to`-only-touched negative case, a real end-to-end `--strict-isolated-nodes` pass/fail run against `spring-mvc-sample`, and one more added on review: the gate is also wired into `runFromFacts` — a review pass caught that `isolatedNodeCount` was already honestly recomputed in `--from-facts` mode (same principle as the existing S1/S2 comment there) but the gate itself wasn't wired to it, AND that the first fix attempt referenced `strictIsolatedNodes` before its own `const` declaration in `main()`, a real TDZ bug that would have thrown on every `--from-facts` invocation — caught by actually running the reconstructed-facts path end to end, not just by reading the diff). |
| **Mechanical bug-fix generalization check — shipped 2026-08-22** | `Catalogue_Intake.md` already named a CI check (grep a diff for new catalogue rows lacking test/claim-cell/scope-limitations updates) as "a reasonable escalation if drift is ever observed in practice." That trigger was hit for real (T-LR-1's first pass hardcoded the annotation name `'Configuration'` as a literal; only caught by a human reading the diff). **Built**: `pipeline/scripts/check-generalization.js`, wired into `.github/workflows/pipeline-test.yml` as a real CI step (`git diff origin/main...HEAD`, `fetch-depth: 0` so the base ref is real). Flags (a) a sample-repo-shaped literal (`fineract`/`waltz`/`shopizer`/etc., sourced from real `spikes/` clones) used in an actual comparison/lookup (`===`, `.includes(`, `case`, etc.) in `pipeline/src/` outside test/fixture paths — comment/doc-string citations of the same repo names are deliberately exempt, since evidence-citation prose is required elsewhere in this project's own discipline, not forbidden; (b) a change to `src/analysis/`, `src/scanner/`, `src/modules/calm-generator/`, or a `src/rules/*.yml` catalogue with no matching `test/regression.test.js` or `Claim_Register.md` change in the same diff. Verified against three real scenarios in a disposable scratch git clone before trusting it (bad literal + no test → fails; bad literal + test added → still fails on the literal alone; clean diff → passes). Also removed the now-dead `pip install graphifyy` CI step in the same file (a leftover from the Graphify→CodeGraph migration earlier this session — `graphify-provider.ts` no longer exists). |
| **Unmapped-signals report double-counts decorators already resolved via native route typing** | Found 2026-08-21 doing a hands-on beginner-usability review. **Fixed 2026-08-21, split into two real, distinct root causes once investigated further, not one:** (1) **Catalogue gap** — NestJS's `@Controller` already had a real catalogue row (`nestjs-controller-decorator`); Spring MVC's class-level equivalents (`@RestController`/`@Controller`) had none at all. Fixed with `signal-catalogue.yml`'s new `spring-mvc-controller-decorator` row, mirroring the NestJS row exactly — catalogue-row-only, no mechanism change. Deliberately does NOT include `RequestMapping`: it's genuinely dual-purpose (class-level prefix vs. a real method-level route when used without `@GetMapping`/`@PostMapping`), and `findRule()` has no `fromNodeKind` input to disambiguate the two uses safely — left out rather than guessed at. (2) **Mechanism gap** — `signal-mapper.ts`'s `nativeRoutes` and `decoratorFacts` loops ran fully independently with zero cross-referencing, so a method-level route decorator (`@GetMapping`/`@PostMapping`) already consumed into a real `NativeRouteFact` still got its own independent, rule-less catalogue lookup and landed in `ignoredItems` as unmapped. Fixed by building a `filePath:startLine` set from `nativeRoutes` before the `decoratorFacts` loop and skipping `ignoreUnknownSignal()` for any `fromNodeKind === 'method'` decorator fact matching it — the same "mark it consumed" idea `route-composer-registry.ts`'s own `consumed` set already established for JAX-RS composition, generalized to native-route-typed frameworks (Spring MVC, Flask, NestJS-native), which never went through that composer at all. Verified against a real `NativeRouteFact`/`DecoratorFact` dump (not assumed): `startLine`/`.line` are identical for the same method (both CodeGraph-reported), so the correlation is exact. New `test/fixtures/spring-mvc-sample` fixture + 3 regression tests (catalogue-row match, zero-unmapped end-to-end, and a negative case proving a genuinely-unmapped method decorator on a DIFFERENT line is never over-suppressed). `npm test`: 130/0/29 (was 128/0/29 before this fix, +2 new tests; +1 more from the coe-lab-fixture-review commit before it). |

| **CodeQL + Gradle: pre-existing daemon silently empties the database** | Found 2026-08-21 running a real three-engine benchmark (a reference Java/JAX-RS banking platform + Spring Boot Admin, all three engines, no fixtures). A Gradle daemon left running from an earlier local build causes `codeql database create` to report `BUILD SUCCESSFUL` with real `javac` output (and `--rerun-tasks` genuinely forcing recompilation) yet still finalize an empty database (`CodeQL detected code written in Java/Kotlin but could not process any of it`) — the daemon executes the real compile *outside* the process tree CodeQL's tracer instruments, since the daemon itself predates that invocation. `codeql-di-provider.ts` already degrades this gracefully (a WARNING, zero bindings, no crash) — the residual gap is that a genuinely-broken build and a daemon-reuse false-empty look identical to the caller today. README documented `--no-daemon` as required for any Gradle `--codeql-build-command` (2026-08-21). **Now also fixed structurally, not just documented**: `--auto-codeql` (`scanner/codeql-auto-detect.ts`, 2026-08-21) always emits `--no-daemon --rerun-tasks` for a detected Gradle build (and `clean` for Maven) — a caller using the new flag can no longer hit this by omission, only a caller still hand-writing `--codeql-build-command` can. **WARNING-text distinction shipped 2026-08-22**: `codeql-database-cache.ts`'s `tryBuildDatabase` (the actual `database create` call site as of the shared-cache refactor, not `codeql-di-provider.ts` — the row's original file reference was stale by the time this was built) inspects `execFileSync`'s captured `stderr`/`stdout`/message for CodeQL's exact `"could not process any of it"` phrase; a match gets a distinct WARNING naming the real cause (pre-existing daemon) and the fix (`--no-daemon`), a genuinely broken build still gets the original generic message — verified both directions with 2 new regression tests (mocked `execFileSync`, real captured phrase text). Maven is not affected (no persistent daemon by default) — verified via the same benchmark. |
| **`--auto-codeql` — CodeQL DI-resolution auto-derived from a detected build file, still opt-in** | Shipped 2026-08-21. `scanner/codeql-auto-detect.ts`'s `detectCodeqlBuildConfig` finds `build.gradle`/`build.gradle.kts` (with a checked-in `gradlew` wrapper) or `pom.xml` (preferring a checked-in `mvnw`) at the common ancestor of the run's package roots and derives `--codeql-source-root`/`--codeql-build-command` — a caller no longer has to hand-write a build command to get CodeQL evidence. **Deliberately still gated behind an explicit `--auto-codeql` flag, not unconditional default-on** — considered and rejected: this pipeline's own `Claim_Register.md` (`T-LR-5-codeql-di` row) already documents that the free CodeQL CLI license permits automated/CI use only against an Open Source Codebase or under a paid GHAS license, and this repo has no `LICENSE` file; making CodeQL reachable without a conscious opt-in would silently put every other user of this shared codebase at license risk the moment they scan a private Java repo, regardless of any one org's own paid-license status. Refuses to guess a Gradle build with no `gradlew` wrapper (a system-wide `gradle` could be any version) rather than picking one — logs a message and continues without CodeQL evidence, same graceful-degradation convention as `graphifyy`/`cdxgen` absence elsewhere. Explicit `--codeql-source-root`/`--codeql-build-command` always take precedence when both are passed with `--auto-codeql`, verified by a real CLI-wiring regression test, not just the two flags' documented intent. **Real, disclosed residual**: the generic default command (plain `compileJava`/`clean compile`, no per-module exclusions) is not guaranteed to succeed against every real repo's build quirks — verified live against the reference Java/JAX-RS banking platform, whose client-SDK OpenAPI-codegen task independently OOMs under a plain `compileJava` (a pre-existing, unrelated build issue, not caused by this flag); `--auto-codeql` degraded gracefully exactly as designed (a WARNING, the rest of the run completing normally, `calm validate` still clean) rather than crashing, but a repo with build quirks like this one still needs a hand-tuned `--codeql-build-command` to get real CodeQL bindings, not just a graceful no-op. 6 new regression tests (`pipeline/test/regression.test.js`): Gradle-with-wrapper, Gradle-without-wrapper (refuses), Maven-with/without-`mvnw`, no-build-file-found, and CLI-precedence-over-explicit-flags. `npm test`: 135/0/29 (was 130/0/29). |
| **`WEAVER_CODEQL_LICENSE_CONFIRMED` env var + `--no-auto-codeql`** | Shipped 2026-08-21. A per-ENVIRONMENT confirmation (set once in a shell profile or CI job config), not a per-repo default — must equal exactly `"1"`, checked in `run-slice.ts`'s `main()`, triggers the identical `detectCodeqlBuildConfig` path `--auto-codeql` uses. **Does not weaken the opt-in guarantee**: a fresh clone or any CI job that hasn't explicitly set this variable behaves exactly as before (CodeQL off); this repo's own CI does not set it. `--no-auto-codeql` is the escape hatch — suppresses both the `--auto-codeql` flag and the env var for a single invocation, without requiring the variable to be unset (useful for a fast smoke-test run in an environment where it's set globally). README documents the real license text/warning next to the env var, linking GitHub's own CodeQL CLI license terms (verified real via fetch before linking, not guessed). `codeql-di-pass.ts`'s doc comment and `Claim_Register.md`'s `T-LR-5-codeql-di` row updated in the same change for consistency — "opt-in only" was previously stated in three places, all three now describe the same (flag OR env var, always explicit) reality. 3 new regression tests: env var triggers the same path as `--auto-codeql` with no flag, a non-`"1"` value (e.g. `"true"`) is correctly rejected, `--no-auto-codeql` suppresses both the flag and the env var. `npm test`: 138/0/29 (was 135/0/29). |

| **CodeQL DI-resolution generalizes safely, but yield is architecture-shape-gated, not framework-gated** | Found 2026-08-21 running `E2-java-framework-generalization-experiment.md` — the first real second/third-repo verification of `T-LR-5-codeql-di` since the original reference-platform-only proof (the reference governance platform's own attempt was real-blocked on jOOQ Pro licensing, disclosed separately). Ran the unmodified `di_resolution.ql` against `spring-projects/spring-petclinic` and `finos/spring-bot` (both real, grep-verified real DI volume before use): mechanically clean on both (no false positives, correct ambiguity/empty-name refusal), but **zero usable relationships on either** — traced to real cause, not assumed: neither repo has much of the "ambiguous interface resolved via `@Bean`-factory/stereotype" shape the reference banking platform has in abundance (30,778 decorator facts → 6,741 units vs. `spring-bot`'s 1,071 → 22), and `spring-bot`'s one otherwise-valid binding was correctly refused because its injecting class (`RoomMembershipChangeHandler`) carries no unit at all in this pipeline's own extraction. **Conclusion, not yet acted on**: `fact-trust-matrix.ts`'s confidence tier for `codeql-di-*` should not be read as validated across "Java/Spring" broadly — only as unrefuted by two additional, low-yield data points. The next useful experiment is a repo *known* to be interface-abstraction-heavy, not another arbitrary Spring app. |
| **Silent zero-bindings CodeQL run — fixed 2026-08-22** | `codeqlDiPass`'s `if (bindings.length === 0) return;` had no accompanying log line, so a genuine "ran clean, found nothing" result looked identical to a silent extraction failure. **Fixed in both CodeQL mechanisms** (review caught the second instance, not just the one named originally): `codeql-di-provider.ts` and `codeql-command-dispatch-provider.ts` both now log `"query ran cleanly, 0 real ... bindings found (not a failure — see WARNING above if the database/query itself failed)"` right after a successful build+query+decode with zero rows — every earlier failure path (missing query file, binary/build failure, query run/decode failure) already logs its own distinct WARNING before returning, so this is the one previously-silent case. `npm test`: 179/0/29. |
| **`--auto-codeql` prefers Gradle over Maven even when Maven would have worked — fixed 2026-08-22** | Found running `E2-java-framework-generalization-experiment.md` against `spring-projects/spring-petclinic` (both build.gradle+gradlew and pom.xml+mvnw checked in; Gradle's toolchain resolution failed there, `Cannot find a Java installation ... matching languageVersion=17`, while Maven compiled cleanly against the identical source). **Fixed**: `codeql-auto-detect.ts`'s `detectCodeqlBuildConfig` now attaches a Maven `fallbackBuildCommand` to its result whenever Gradle is primary AND a `pom.xml` also exists at the same source root; `getOrBuildCodeqlDatabase` (`codeql-database-cache.ts`) tries it only after the primary build genuinely fails, never speculatively, and caches whichever command actually succeeded under the original (sourceRoot, primaryBuildCommand) key so a second CodeQL-based pass reuses the result without re-attempting either build. Built at the shared-cache layer (not per-provider), so both `codeql-di-provider.ts` and `codeql-command-dispatch-provider.ts` get the fallback, not just DI resolution as originally scoped. Verified against a real scratch git clone simulating the exact `spring-petclinic` toolchain-mismatch shape (primary command throws, fallback succeeds, dbPath returned) — not just asserted. 2 new regression tests. `npm test`: 179/0/29. |
| **JSR-330/Sisu DI generalization — run 2026-08-22, mechanism generalizes to a 4th DI shape, zero live yield (structural, not a corner case)** | `Tier3-sisu-jsr330-di-resolution-experiment.md`. `sisu_di.ql` (`pipeline/src/rules/codeql-queries/sisu-di-resolution/`) resolved 16 real bindings against `apache/maven-doxia` across THREE structurally distinct Sisu conventions — single-implementer implicit binding, `Map<String,Interface>` role-hint multi-binding (a genuinely new shape: one field resolving to N keyed bindings), and `Collection<Interface>` multibind. Real bug found+fixed authoring it: direct-supertype-only (`getASupertype()`) silently missed every map-role-hint binding since real implementers (`XdocParser` etc.) implement their interface only transitively through several abstract-class levels — fixed with the transitive `getAnAncestor()`. **Real, disclosed ceiling found BEFORE writing the query**: `maven-doxia` produces ZERO Weaver units anywhere (pure library, no HTTP/persistence/messaging/control evidence) — `codeql-di-pass.ts` requires the injecting class to already be a Weaver unit, so this repo can never produce a single live relationship regardless of query quality, confirmed via a direct `run-slice` run first. Project owner chose to proceed mechanism-only (same framing as `E2`'s zero-yield Spring runs). Main/test contamination checked clean (Guice-experiment class of gap) — build is `compile`-only, verified zero `src/test/` files in the real database. **Not scoped as a live pass**: no real-yield evidence exists to place a trust tier on; the right next step is a DIFFERENT Sisu/JSR-330 repo that also has genuine HTTP/persistence surface, not a better query against this one. |
| **E2a (TypeScript/NestJS) — real, measured gap found and closed by a first exploratory query** | `E2a-typescript-nestjs-codeql-evaluation.md` (2026-08-22). Ran a real, buildless CodeQL JavaScript/TypeScript extraction (`codeql database create --language=javascript`, no `--command` needed — completed in seconds) against `ghostfolio/ghostfolio` (real wealth-management platform, chosen for fintech-domain relevance to Weaver's first target customer). Found a real, measured gap: NestJS's string-token custom-provider DI (`{ provide: 'Token', inject: [...], useFactory: ... }`, the NestJS analogue of Spring's `@Bean`-factory pattern) is completely invisible to CodeGraph/Graphify's own import-tracing — confirmed zero relationships touch the affected classes in the baseline. A first, generic (no hardcoded token/class names) exploratory query resolved **15 real bindings across 3 injecting classes and 2 tokens**, all 15 new versus baseline, generalizing within the repo to a second token the query wasn't written with in mind. **Real, disclosed gap, not fixed here**: one token (`DataProviderInterfaces`) is genuinely registered twice with slightly different provider lists (confirmed via `diff`) — the exploratory query silently unions both rather than refusing the ambiguity, the same class of gap `T-LR-5` had to build explicit refusal logic for on the Java side. Not yet a live pipeline pass — a real `T-LR-5`-shaped follow-up (ambiguity refusal, second-repo verification, `--auto-codeql` extension to detect `package.json`+`@nestjs/core`, a real trust-tier placement) is the natural next step, not started. **Re-checked 2026-08-22 for the main/test contamination class found in the Guice experiment below — clean, no contamination found** (see that row). |
| **Guice explicit-module DI resolution — real gap found, and a more severe contamination problem surfaced** | `Tier2-guice-di-resolution-experiment.md` (2026-08-22). Found via FINOS landscape research (background agent): `finos/legend-sdlc` (real, active Goldman Sachs-originated SDLC server), 261 real `@Inject` sites wired via **Guice** (`com.google.inject`) — a third, structurally distinct DI-resolution mechanism class from both Spring and NestJS's string-token pattern. A real gap confirmed (zero baseline relationships for the affected classes) and a first exploratory query (`guice_di.ql`, generic — joins on real Java interface type identity, no hardcoded names) resolved 218 raw bindings. **But 208 of those 218 are noise**: the query joined across the WHOLE CodeQL database (main + test source, both compiled by a normal `mvn install`), so real production classes (`src/main/java`) got matched against `InMemoryModule`'s test-only bindings (`src/test/java`) purely because both bind the same interface type — a real, more severe class of gap than E1b's/E2a's own ambiguity findings (those were ambiguity WITHIN one real deployment's wiring; this is cross-contamination BETWEEN two mutually-exclusive deployment configurations). Only **10 of 218 rows are genuine, hand-verified production bindings**. **Not fixed here** — a correct version needs source-root or active-Module scoping before joining, not merely an ambiguity-refusal predicate (a different fix shape). **Re-checked 2026-08-22: `E1b`/`E2`/`E2a` are all clean.** Verified directly (not re-derived from theory) against a real "list indexed `src/test/` files" query run on the reference Java/JAX-RS banking platform's, `spring-bot`'s (rebuilt with `E2`'s exact command), and `spring-petclinic`'s (same) real CodeQL databases — zero test-source files in any of them, despite `spring-bot`/`spring-petclinic` genuinely containing test-scoped `@Configuration`/`@Bean` classes. **Root cause is build-command-specific, not query-logic-specific**: Maven's `compile` goal and Gradle's `compileJava` task both stop before `test-compile` ever runs, so test source is structurally never seen by CodeQL's tracer — `legend-sdlc` hit the bug only because `mvn install` (chosen for an unrelated reactor test-jar issue specific to that repo) reaches `test-compile`. Any future `--codeql-build-command` reaching a test-compile phase (`install`, `test`, `verify`) should get main/test scoping; a `compile`-only command structurally cannot hit this. |
| **E2b (Python/FastAPI+Flask) — run, negative result: no gap found, CodeGraph already primary** | `E2b-python-fastapi-codeql-evaluation.md` (2026-08-22). Ran CodeQL's official Python HTTP models against two real repos — `openbb-core` (FastAPI) and a reference Java microservices banking sample's `userservice` (Flask) — same probes as CodeGraph/Graphify. Unlike `E1b`(Java)/`E2a`(TypeScript), **found no gap CodeQL closes**: CodeGraph's native `route` typing already beats CodeQL's official Python model on both precision (real HTTP verb vs. CodeQL's generic `"route"` string on bare `@app.route`) and coverage (FastAPI `APIRouter(prefix=...)` composed paths — CodeQL's `getUrlPattern()` returns the leaf path only, a miss at L1 symbol level, `full=0 file=0`). Aggregate: `openbb-core` CodeGraph 4/4 vs. CodeQL 2/4; the reference sample's `userservice` CodeGraph 5/6 vs. CodeQL 3/6. **Recommendation: do not scope a Python CodeQL HTTP-routing pass** — real per-run cost (44–113s) for weaker precision than the existing primary signal. Closed as answered, not open. |
| **JPA entity→table class resolution — shipped 2026-08-22, corroboration-only** | `jpa_entity_table.ql` (`pipeline/src/rules/codeql-queries/jpa-entity-table/`) resolves an `@Entity` class's real table name from its own `@Table(name="...")` annotation — generic, no hardcoded names, never infers JPA's implicit default table-name convention. Real result against the reference Java/JAX-RS banking platform's own repo: 39 real bindings (`Charge → m_charge`, `Client → m_client`, etc.), 0 `@Entity` classes lacking an explicit `@Table` in that codebase. `codeql-jpa-table-pass.ts` (opt-in, same `--codeql-source-root`/`--codeql-build-command`/`--auto-codeql` gate as `T-LR-5`) enriches an ALREADY-detected persistence unit's evidence with the real table name — never introduces a unit, never changes `kind`, never builds a relationship. Verified end-to-end: a real `run-slice` scan of the reference platform's charge module with CodeQL enabled raised `Charge`'s confidence 50→60 with the real `m_charge` evidence attached, `calm validate` 0 errors/0 warnings. `CONTRACT_VERSION` 16.0.0→17.0.0 (`Evidence.source` gained `'codeql-jpa-table'`, all three modules reviewed/bumped). 4 new regression tests. See `Claim_Register.md`'s `jpa-entity-table-codeql` row for the full evidence trail. **Does not resolve the P1 JDBC read-service-vs-table-owner row above** — that needs a read/write BEHAVIORAL distinction (real data-flow/taint analysis) this class→table name resolution alone doesn't provide; this mechanism only corroborates units already correctly classified via JPA annotations. **Real, disclosed gap**: never attempts JPA's implicit default table-name convention (no explicit `@Table`) — a repo relying on that convention gets zero corroboration, not yet measured on any real repo. **Second-repo verification not yet done** — one real repo (the reference banking platform) only. |
| **Shopizer `ANY`-method routes — checked 2026-08-22, does not reproduce in Weaver** | External benchmark flagged 13 Shopizer routes using bare `@RequestMapping` (method-unspecified) as a possible admission gap. **Checked directly against this pipeline's own output, not assumed**: a real fixture with a bare `@RequestMapping("/legacy/ping")` route, run through `run-slice` end to end, produces `signal: 'ANY /legacy/ping', source: 'native-route', category: 'http-entry-point', weight: 40` in `typed-facts.json` — CodeGraph itself names the route `ANY /path`, and `signal-mapper.ts` has no verb-pattern filter anywhere in its admission path (`route.name` is carried through as an opaque string). The unit resolves `kind: service` and is correctly flagged by `threat-signals` for missing security control. **The external harness's own `^(GET\|POST\|...)` admission regex was the source of the "13 rejected" count, not anything in Weaver's pipeline.** No fix needed — closing as checked, not open. |
| **Unit-classification QL query — CodeQL independently classifying units, not just resolving wiring** | All shipped/evaluated CodeQL work (`T-LR-5`, `E1`/`E1b`/`E2`/`E2a`/Tier-2, `E2b` row above) resolves DI/dispatch WIRING between units this pipeline's own extraction already found — none of it asks CodeQL to independently decide whether something IS a unit (Weaver's own `kind` classification: service/database/queue/etc.). Speculative — no measured gap or false-classification instance has been found or cited that this would fix (contrast the P1 JDBC-ownership row above, which has two real, corroborated instances driving its own CodeQL follow-up). Per this project's evidence-first intake discipline, not scheduled until a real classification miss is found and cited. |

## P2 — Detection breadth

| Item | Description |
|---|---|
| **Kinesis stream detection** | Evidenced as a real pattern in job-posting research; no catalogue coverage yet. |
| **Deeper OAuth2 evidence** | Currently only detected via OpenAPI security schemes; library-import/config-level detection not yet built. |
| **Container/compose corroboration** | The dependency-manifest corroboration lane (via an external SBOM tool) covers build-file dependencies; Docker/compose/Kubernetes container facts from the same tool are not yet wired in. |
| **Maven `<modules>`/Gradle `include` module graph — not yet extracted** | Flagged by external research (`~/Innovation/research/engine_test`, `arch-walk`'s `src/extract/maven.ts`/`gradle.ts`) as a cheap, deterministic catalogue pass: parse a root `pom.xml`'s `<modules>` or `settings.gradle`'s `include`/`project(':x')` declarations into a sibling `depends_on` module graph. Weaver has no equivalent today — `deployable-manifest-provider.ts` only reads `pom.xml`/`build.gradle` as dependency-manifest filenames for SBOM corroboration, not as a module-declaration source. Useful as a cheap L1 structural fact ahead of/instead of a full CodeGraph index, and for multi-module repos where CodeGraph's own per-root indexing already requires package-root enumeration. Not yet scoped against a real Weaver-side repo showing the gap in practice (the `arch-walk` finding is from a sibling tool's own design, not a measured miss in Weaver's own output) — a real reference-banking-platform/reference-governance-platform-shaped multi-module scan should confirm the gap before building. |
| **Liquibase/changelog table extraction — persistence catalogue gap beyond JPA** | Flagged by the same external research (`arch-walk`'s `extractLiquibase` in `frameworks.ts`) — real table names declared in Liquibase XML/YAML/SQL changelogs are invisible to Weaver's persistence detection today, which is JPA-annotation- and ORM-import-shaped only (`persistence-detection-catalogue.yml`). A repo using Liquibase for schema migration without corresponding `@Entity`/`@Table` classes (or with the table name diverging from the entity name) would under-detect real stores. Same caveat as the module-graph row above: sourced from a sibling tool's design, not yet confirmed against a real Weaver-scanned repo with Liquibase changelogs — needs that confirmation before scoping the catalogue/mechanism shape. |
| **ADR ingestion** | Linking existing Architecture Decision Record documents into the generated CALM output's `adrs[]` array — not yet built. |
| **Persistence ownership-shape coverage beyond Dynamo/Prisma** | `TypeORM`, `Mongoose`, `Sequelize`, `pg`, `mysql2`, `ioredis`, `com.mongodb` remain plain-import-only in `persistence-detection-catalogue.yml` (no `ownerBaseClass`/`ownerFieldType`) — a class that merely imports one of these for typing/parameter purposes can still be over-classified as a database owner, the same risk Q13 (Prisma) and T-MR-4 (Dynamo) closed for those two libraries only. Deliberately not generalized further: neither of those two fixes was built ahead of evidence — both started from a real, cited import-vs-ownership false positive (Prisma's `AccessService`, Dynamo's `TierService`), and no such case has been found or reported for any of the libraries above. Guessing a shape for them now risks the opposite failure — a wrong heuristic silently suppressing real database units nobody has observed a problem with — which is worse than today's disclosed, honest over-classification risk (`scope-limitations.yml`'s `persistence-import-vs-ownership-ambiguous`). **Revisit trigger:** a real repo or fixture produces a documented import-vs-ownership false positive for one of these libraries, the same way the Prisma and Dynamo counterexamples were found — see `Claim_Register.md`'s `U-persist-import` row. |
| **Louvain community/domain-boundary clustering — explicitly open, evidence-gated, not adopted** | Investigated during the Graphify→CodeGraph cross-package-backbone migration (`docs/solution/E6-cross-package-backbone-evaluation.md`, `.claude/plans/snug-juggling-badger.md`'s decision trail): Graphify's own `cluster-only` output is real data, not speculation — god-node/degree-centrality correctly surfaced real domain-central classes on two unrelated repos (the reference banking platform: `Charge`/`ChargesApiResource`; Shopizer: `MerchantStore`/`Product`/`Customer`/`Order`) — but that specific signal is **not Graphify-exclusive** (any engine's edge counts give the same statistic; CodeGraph's `getIncomingEdges`/`getOutgoingEdges` already do, uncomputed, not built here). Louvain community clustering IS the one thing CodeGraph structurally can't do, but the one real cohesion measurement available (the reference platform's charge module, 22 communities) scored 0.08–0.25 — low, and several communities mixed unrelated things (annotations grouped with real domain classes). **Revisit trigger**: a real domain-boundary-grouping requirement appears, and/or a real cohesion re-measurement (ideally tuned, ideally a second repo) clears a stated confidence bar — not before. Not a rejected idea; deliberately not built on today's noisy signal. |

## P3 — Platform

| Item | Description |
|---|---|
| **Structured-file-derived units have no resolvable evidence snippet for the LLM layer — found 2026-08-23, first real end-to-end run** | Running the full scan → pack `--with-dossier` → architect decision → apply loop for real (not per-script isolation, the first time this exact sequence was ever run end to end) against `spring-config-sample` surfaced a real, different-shaped gap from the two already fixed this cycle: a Spring-config-derived unit's `evidenceRefs` (e.g. `application-prod.yml:spring.datasource.url`) uses a dotted-key-path shape, not `file:line` — `pack.py`'s snippet builder (`_build_evidence_packs`/`_read_snippet`, keyed on the `file:line`-shaped `_REF_RE`) never produces a packs.json entry for it, so any LLM pass (dossier or Tier B) asked about that unit gets an empty `evidence_snippets` and correctly (not a bug in the guardrail) refuses to hypothesize rather than fabricate. Real, observed consequence: `R-002` (a genuine `S1-zero-service-touching-relationships` residual, `application-prod.yml::spring-datasource`) got a dossier explicitly stating no evidence content was provided. Not fixed here — this is a `pack.py` snippet-building gap (a fourth ref shape to support), a different mechanism than the `known_evidence_refs` union fix already shipped for the whole-unit-empty-`evidenceRefs` class. Needs its own scoped fix: either a structured-file-aware snippet reader (read the actual YAML key's line, same idea `_read_snippet` already does for source files) or an explicit "no snippet available for this evidence shape" signal the LLM layer can react to honestly instead of silently getting nothing. |
| **Residual-review LLM layer redesign — T-1 and T-2 closed with real live-model evidence 2026-08-23, T-3–T-5 not started** | `Architect_Residual_Review_Session.md` §3 redesigned to separate tier (write-permission ceiling) from a new, orthogonal Evidence Dossier concept, plus a fully code-verified producer registry. Follow-on build in `docs/solution/AGENT_TASKS_Residual_Assist_Redesign.md`, deliberately sequenced so T-1 (live-model verification) ran before any shared-module refactor. **T-1**: the first-ever live run of `draft_tier_b.py`/`advisory.py` in this project, using the `claude` CLI itself as the backend (already authenticated, no separate `ANTHROPIC_API_KEY` needed — this project's fintech target-customer profile doesn't leave API keys in environment variables). Found and fixed 3 real bugs (markdown-fenced JSON responses, prose-before-fence responses, a 120s timeout too short for a real 131.75s Tier B call) and captured real cost/latency ($0.08–$0.32, 40–132s per residual) that closed T-2's open question: the dossier pass is **opt-in**, not automatic. **T-2** (`docs/solution/AGENT_TASKS_Residual_Dossier_Module.md`): `llm_common.py` now holds the shared evidence-assembly + network-boundary logic (including the stdin-not-argv fix for evidence-bearing prompts); `dossier.py` + `pack.py --with-dossier` is the opt-in pass; `draft_tier_b.py` records `draftOutcome: "bar-not-met"` per §3's named state transition. One real bug found during T-2's own required live confirmation — `residual.evidenceRefs` is empty for whole-unit trigger classes (e.g. `S2-http-without-security-control`), real evidence lives on the unit's own `evidenceRefs` instead — fixed in `dossier.py` via `llm_common.known_evidence_refs`, and the identical latent gap in `advisory.py`'s own guardrail (found+disclosed same day) closed in the same follow-up pass, 2 new regression tests, 217/0 suite. T-3 (`fetch-span --anchor`), T-4 (mechanical taxonomy-sync check), T-5 (step consolidation, now also foldable with the `--with-dossier` flag into its documented minimum path) remain not started. |
| **Decision Record/Override draft provenance — evidence-gated, not yet a real gap** | Named `B-chatmode-host-enforcement-gap` in `Architect_Pilot_Feedback_Notes.md`/`AGENT_TASKS_Architect_Pilot_Fixes.md` but never actually added as a row here — corrected 2026-08-23 (`Architect_Residual_Review_Session.md` changelog): that name framed this as a safety hole to close ("structurally closing the gap for non-VS-Code hosts"), which mis-stated where the real guarantee lives — `apply.py`'s own explicit confirmation gate already holds regardless of which chat client, host, or IDE auto-approve setting drafted a file under `drafts/`, so there is no gap in the applying step. What's left, genuinely open and not yet needed by any real incident: `apply.py`/`override-applier.ts` could independently record *which tool/host produced* a given draft (today `decision.reviewer`'s `architect:`/`llm-advisory:` prefix convention is the only provenance signal) — useful for audit trail, not for safety, since the confirmation gate doesn't depend on it. Not scoped until a real need for that audit granularity is evidenced. |
| **Portable, CALM-round-trippable IR** | The current human-readable architecture summary is one-way (facts → markdown). A portable version that round-trips real CALM node/relationship fragments would let it double as a distribution format — proposed, needs review before implementation. |
| **Third-party plugin discovery / embed API** | The module boundary (contract version, module registry) exists; external plugin discovery and a library embed API do not. |
| **Two-tier (global + domain) mapping configuration** | Not yet built. |
| **Additional engines on measured gaps only** | Static-analysis engines beyond the current pair should only be added where a real, measured coverage gap justifies it — not speculatively. **CodeQL now has four such measured gaps** (command-bus dispatch, `E1-codeql-engine-evaluation.md`; Java DI resolution, `E1b-codeql-di-resolution-experiment.md`; NestJS string-token DI resolution, `E2a-typescript-nestjs-codeql-evaluation.md`; Guice explicit-module DI resolution, `Tier2-guice-di-resolution-experiment.md`) — this principle row's own bar is met for all four; see the relevant BACKLOG rows for what shipping each actually requires, and note the Guice row's real main/test-contamination caveat before treating any of these as production-ready off the exploratory-query result alone. **`CodeQL-engine-synthesis-2026-08-22.md` rolls all five real experiments (`E1`/`E1b`/`E2`/`E2a`/Tier-2) up into one evidence-backed answer** to "should CodeQL be more central" — the headline finding: yield is gated by architecture shape and DI idiom, not by language/framework identity, and the reliability objection (five real build-command failure modes this session) is Java-specific, not CodeQL-general, since Python/TypeScript extraction is genuinely buildless. |
| **CodeQL command-bus dispatch — shipped and live end-to-end, 2026-08-22 (#18)** | Closes `OOS_Registry.md`'s `OOS-command-bus` revisit trigger for real: the actual goal was always making `ChargesApiResource -> CreateChargeDefinitionCommandHandler` (the hand-verified relationship that first surfaced that permanent non-goal) a real CALM relationship. Two real, measured gaps evidenced originally: `E1-codeql-engine-evaluation.md` (7 real command-bus dispatch edges) and `E1b-codeql-di-resolution-experiment.md` (2106 real DI-resolution bindings), both reference-banking-platform-only at measurement time; T-LR-5 (2026-08-19) shipped DI resolution only. **Three real, distinct bugs found and fixed getting command-dispatch from "evaluated" to actually live**, none of them polish: (1) the original query never left gitignored `soln/` (the identical gap `di_resolution.ql` once had) — reconstructed from `E1`'s own precise mechanism description. (2) Running both CodeQL mechanisms together in one `run-slice` invocation silently zeroed the second one's output — root-caused to Gradle's build cache (`org.gradle.caching=true`, the reference platform's own `gradle.properties`): `--rerun-tasks` disables the up-to-date check but not the cache, so a second independent `codeql database create` could restore cached compiler outputs without re-invoking `javac`. Fixed structurally: `codeql-database-cache.ts` builds at most one real database per (sourceRoot, buildCommand) per invocation, shared by both providers — also a real performance win (one ~7min build, not two). (3) **The real, root-cause bug**: the first reconstruction selected the wrong node as "dispatcher" — the fluent builder method's own declaring class (`CommandWrapperBuilder`, a plain utility with zero framework signal, never independently recognized as a unit) rather than the real REST resource that calls it. Every one of 408 mechanically-real bindings was silently refused by the live pass's own `dispatcherUnit`-must-exist gate — a materially different failure than "not wired in yet," since it looked shipped (real query, real evidence, 0 errors) while producing 0 live relationships. Found by actually checking the target node's own presence in `typed-facts.json`, not by re-reading the query. Fixed by adding the real missing hop — a `RealDispatchCall` join tracing from the builder method to its actual call site. **Confirmed end-to-end against a real, live reference-platform build**: `[codeql-command-dispatch] 454 real dispatch binding(s) resolved by CodeQL; 446 new relationship(s), 0 new unit(s) introduced` — the flagship chain reproduces exactly (`ChargesApiResource -> CreateChargeDefinitionCommandHandler`, `status: inferred`, `grade: architecture`, not `requires-review`, since both real classes were already independently recognized units), `calm validate` clean. Shipped as `codeql-command-dispatch-provider.ts`/`codeql-command-dispatch-pass.ts`, sharing `--codeql-source-root`/`--codeql-build-command` with the DI pair. Trust tier: same as DI resolution (7/4), comparably real evidence. `engine-capability-matrix.yml`'s `relationshipMechanisms` entry marked `proven`. 7 new regression tests. `npm test`: 148/0/29 (was 141/0/29). See `Claim_Register.md`'s new command-dispatch claim row for the full evidence trail. |
| **Per-(engine, fact-type) trust tiers** | **T-LR-6 shipped, 2026-08-20** — `pipeline/src/analysis/fact-trust-matrix.ts` is now the evidence-earned, per-(engine, mechanism, scope) trust ranking for every fact type currently in production (Graphify's `r2-phase1`/`r2-stereotype`/`r2b`/`r2c`/`admitted-unresolved` multi-hop family, plus CodeQL's `codeql-di-stereotype`/`codeql-di-bean-factory`/unit-introduction), each row citing the real measurement that earned it; `assertCodeqlNeverPrimary()` (regression-tested against both the real matrix and a deliberately-rigged one) enforces "CodeQL is never automatically primary" structurally. **Partially closed, 2026-08-22 (T-onboarding-16)**: `engine-capability-matrix.yml`'s own (language, framework) routing config is still a separate axis from this trust matrix's per-(engine,mechanism,scope) confidence numbers — deliberately NOT merged, since real per-framework confidence numbers don't exist and inventing them would be unevidenced (see `CodeQL-engine-synthesis-2026-08-22.md`). What shipped instead: a real, cheap DRIFT check, not a numeric connection — `engine-capability-matrix.yml` gained a new, separately-shaped `relationshipMechanisms:` section (relationship-resolution capabilities cut across units, a different shape from the existing per-unit `routes:` — forcing DI-resolution into `routes:`'s primaryEngine/augmentEngine fields would have misrepresented what's measured), and `codeql-di-pass.ts` now calls `warnIfMechanismUnverified()` after every real run — logs a WARNING (never blocks) if this mechanism ever produces real bindings for a (language, mechanism) combo `engine-capability-matrix.yml` doesn't record as `proven`. Today's three recorded entries: `java`/`codeql-di-resolution` = proven (T-LR-5/E1b), `typescript`/`codeql-nestjs-token-di` and `java`/`codeql-guice-di` = `exploratory-not-shipped` (E2a, Tier2). 3 new regression tests. `npm test`: 141/0/29 (was 138/0/29). **Still open**: a full numeric per-(engine,mechanism,scope,framework) confidence system — deliberately not attempted, would need real per-framework measurement this project doesn't have yet. |
| **Fact identity, incremental merge, and review history** | Every run rebuilds facts; Decision Records persist human corrections across runs but individual facts carry no stable identity or status history. **Fact identity shipped (T-CL-1, 2026-08-20)** — `TypedUnit.id` audited as already content-derived per-producer; `TypedRelationship.id` added (`kind\|from\|to\|mechanism-or-source`), replacing a real run-scoped-counter bug (`relationship-builder.ts`'s old `rel-${i}` CALM id). Two second-instance fixes of the same mechanism class landed in the same pass: `k8s-trust-detector.ts` (2+ shared secrets between one pair no longer collide onto one id) and `port-interface-builder.ts` (`iface-port-<port>`, was a positional index). See `Claim_Register.md`'s `T-CL-1-fact-identity` row. **Incremental merge (T-CL-2, 2026-08-20) and review history (T-CL-3, 2026-08-20) shipped** — `analysis/incremental-merge.ts` merges a fresh run against the prior `typed-facts.json` in the same `--out` directory, keyed on T-CL-1's stable ids; a `'reviewed'` fact with unchanged evidence carries its status forward, one with changed evidence is flagged `requires-review`, never silently overwritten either way. `analysis/fact-history.ts` appends every analysis-driven status transition to `<outDir>/fact-history.json` (human-driven `'reviewed'` transitions already have their own audit trail on the Decision Record itself). See `Claim_Register.md`'s `T-CL-2-incremental-merge` row. **Determinism test (T-CL-6, 2026-08-20) shipped** — two identical `run-slice` runs into the same `--out` directory now assert byte-identical facts/status (generatedAt excluded, evidence order-independent) as a permanent regression test. **Contract bump (T-CL-4, 2026-08-20) shipped** — `CONTRACT_VERSION` `13.0.0` → `14.0.0`: `TypedUnit.status`/`TypedRelationship.status`/`TypedRelationship.id` promoted optional → required (a real new-required-field tier-(c) change per `Contract_Evolution_Policy.md` §2(c), since `assignStatusPass`/`factIdentityPass` are unconditional always-last passes). All three modules' `supportedMajorVersion` reviewed and bumped to `"14"` in the same commit, none needed a code change. See `Claim_Register.md`'s `T-CL-4-contract-bump` row. **Contract change — see `Contract_Evolution_Policy.md`; bumping the contract silently skips any module whose declared major version wasn't bumped with it.** |
| **Cross-repo relationship resolution (beyond co-scanned roots)** | Multi-root covers repos scanned together. Repos *not* co-scanned have no join path. **Shipped, 2026-08-20 (T-MR-1/T-MR-2)** — `scanner/repo-manifest-provider.ts` reads a small, human-authored, checked-in per-repo manifest declaring the repo's own node ids and published contracts (`weaver-manifest.yml`); `analysis/cross_package/cross-repo-join-detector.ts` (`--repo-manifests <dir>`, opt-in) resolves in the exact strict reliability order proposed here — shared API-spec identity → published artifact coordinates → service-catalogue/DNS, strictly ranked (stops at the first tier that resolves per pair). Tier 4 ("human review") is the existing `relationship_add` override path, not an automatic step — no relationship or ignored-item is produced for a non-match. Every resulting relationship is capped at `status: requires-review`/`grade: structural` regardless of which tier resolved it, and never infers an edge from naming alone (tier 3 is exact-string match only). `TypedRelationship.source` gained `'repo-manifest'` — `CONTRACT_VERSION` `15.0.0` → `16.0.0`. See `Claim_Register.md`'s `T-MR-1-repo-manifest`/`T-MR-2-cross-repo-join` rows. **Real, disclosed residual**: verified end-to-end against three checked-in fixtures (one per tier), but no real cross-organization two-repo pair was available in this environment to verify against — `scope-limitations.yml`'s `cross-repo-join-root-granularity-only`. |
| **Scope-limitations ↔ claim-register consistency** | Ongoing discipline: every new detection gap named in generated-output metadata should have a matching completeness-claim entry, checked each time scope changes. |
| **`.graphify-cache` now lives under `--out`; `.codegraph` staying beside the source is a permanent third-party-SDK constraint, not deferred work** | Found 2026-08-21 running `run-slice` against a real (non-fixture) target directory end to end. **`.graphify-cache` fixed 2026-08-21**: `runGraphifyPass` (`graphify-provider.ts`) now takes an explicit `cacheBaseDir` argument, threaded from the new `AnalysisContext.outDir` field (`pass-registry.ts`/`run-slice.ts`) — the cache lives at `<outDir>/.graphify-cache`, verified end to end against a real target directory outside `pipeline/test` (confirmed via directory listing: absent beside the source, present under `--out`). This also **structurally eliminated a real staleness bug class**, not just relocated a directory: the old beside-source cache could retain a stale node-id assignment across separate local dev runs against the same fixture path (the exact reason ~50 regression tests forced an `fs.rmSync` before every run) — a `--out`-scoped cache is fresh by construction, since test `--out` dirs are always freshly `mkdtempSync`'d; all 50 now-unnecessary cleanup calls removed. Real, disclosed trade-off: the incremental speedup (a second run against unchanged source going from a full AST pass to ~0.1s) now applies only across reruns into the SAME `--out` directory, not any rerun against the same source regardless of `--out` — verified this still works via a real same-`--out` rerun, and it's already the convention T-CL-2's own incremental merge expects. **`.codegraph` investigated and found genuinely impossible to move**: it's the third-party `@colbymchenry/codegraph` SDK's own internal behavior (`CodeGraph.init`/`open(packageRoot, ...)`) — confirmed by reading the SDK's own `InitOptions`/`OpenOptions` types, neither has a location override; the SDK's `CODEGRAPH_DIR` env var only renames the directory (for Windows/WSL coexistence, per its own doc comment), never relocates it outside `projectRoot`. This is a **permanent limitation**, not deferred work — no further action expected unless the SDK itself adds a location option. |
| **README beginner-onboarding gaps found via a hands-on Spring Boot review — fixed 2026-08-21** | A user asked for a fresh, non-audit README review from the perspective of a first-time user with a real Spring Boot repo. Two real gaps found by actually building a realistic minimal Spring Boot project and running the README's own instructions against it end to end (not by re-reading prose), **both fixed**: (1) **Java/Spring "try it now" example added** — a second real walkthrough (`test/fixtures/spring-config-sample`, chosen deliberately over the new `coe-lab/` fixture per `CoE Lab isolation` — platform/doc work stays on `pipeline/test`) alongside the existing NestJS one, real captured output: one small fixture produces 6 real CALM nodes (the JAX-RS resource + 5 nodes read straight from two `application*.yml` Spring profiles — datasource per environment, Kafka, Redis, RabbitMQ), `calm validate` clean. (2) **"What's in my `--out` directory?" table added** — names every file `run-slice` writes with a "look at it first?" column, and explicitly reframes `unmapped-signals-report.json`'s count as real-and-expected on most real repos rather than a failure signal, tying back to the T-onboarding-1a/1b fix above so the guide describes the now-fixed behavior, not the old confusing one. A new fixture covering the original triggering shape was added to `coe-lab/fixtures/monorepo/packages/java-spring-orders/` for future cross-verification — source only, **no gold authored for it in this pass**: the same agent turn that added it had already run `run-slice` against it and reported its real output to the user, which disqualifies that agent from being a clean gold author per `coe-lab/ISOLATION.md` (gold must be hand-authored from source only, never informed by what the generator already produced). Gold for this package should be authored by a fresh session/human reading only `src/`. |

## Phase 2 — needs research, inputs, and validation

Session A leftover lenses and Session B's remaining confidence question. **Not current-phase execution.** Do not start a build session against a hypothetical feed or a combination formula E5 already rejected. T-LR-5 (CodeQL DI) and T-LR-4 (bean-factory, via that engine) shipped in `6173b36`. T-LR-6 (trust matrix) shipped 2026-08-20 (`fact-trust-matrix.ts`) — no CodeQL work remains in this file's own scope; do not start a second DI session.

| Item | Why Phase 2 | Revisit trigger |
|---|---|---|
| **Vulnerability lens** (`T-LM-1`) | Session A checked the gate: `cdxgen` is name/version only; no CVE/advisory feed (`grype`/`trivy` absent, no scan artefact in-repo). A fabricated schema is forbidden. | A real SCA/vulnerability feed on a real repo, then gold + scorer (`T-LM-0` pattern) before any gate |
| **Data-flow / lineage lens** (`T-LM-3`) | No data-classification facts exist on `TypedFacts`. Field-level lineage is new extraction, not a catalogue row on current flows. | A real classification or lineage signal designed and evidenced, then a module + gold |
| **Green-engineering lens** (`T-LM-4`) | No cloud-billing / IaC cost source. k8s `resources.requests` would be a capacity proxy, which this task forbids as a stand-in for cost. | A real non-code cost feed; do not approximate from LOC or request/limit declarations |
| **Probabilistic confidence** (`T-FS-5`) | E5 negative (22/617 band changes, all less correct). Additive weights stay. Already listed under Decided-not-to-build; repeated here so Phase 2 research (a *different* combination rule) has a home. | A real independent-evidence shape where a new formula is more correct — not a re-run of E5's correlated-`extends` case |

## Decided not to build

Parked after a real experiment or evidence pass — not forgotten, not scheduled. Reopen only via the named trigger.

| Item | Why parked | Revisit trigger |
|---|---|---|
| **Probabilistic, independence-aware confidence combination** (`T-FS-5`) | E5 (`docs/solution/E5-confidence-replay-experiment.md`, 2026-08-14) replayed probabilistic-OR vs. the live summed-weight formula over 617 real units. 22 band changes, all reviewed as **less** correct (one `extends JpaRepository<X,Long>, JpaSpecificationExecutor<X>` treated as two independent signals). Additive weights stay. Contradiction detection (`T-FS-3`) shipped separately and does not reopen this row. **Phase 2** if a different combination rule is researched — see the Phase 2 table. | A real independent-evidence shape (not two facets of one declaration) changes a band in the more-correct direction |

## Explicitly out of scope, near-term

- **Helm/Kustomize template resolution** — only flat, pre-rendered Kubernetes manifests are read today; template/overlay resolution is a materially larger problem.
- **Additional languages / frontend frameworks** (Go, .NET/C#, React/Angular, etc.) — no evidenced near-term need.

See [`OOS_Registry.md`](./OOS_Registry.md) for permanent (not just near-term) non-goals and their reasoning.
