# AGENT TASK — Build `unresolved-outbound-target` (§3.2): the 3 missing high-value edges become reviewable

**T-1 status: SHIPPED 2026-09-02.** See `BACKLOG.md`'s row for the real end-to-end verification and the one real, disclosed follow-on gap it surfaced (`triage.py`'s `_evidence_refs_for` stub — a separate BACKLOG row, out of this task's scope). T-2 (preview/merge tool, below) remains not started.

**Lane:** T-1 independent; T-2 optional/separate, can follow later
**Governance/DoD:** `docs/solution/Architect_Residual_Review_Session.md` §3.2 (this file executes that design, already written and reviewed — do not re-derive the design, follow it), §3.1's producer registry (T-1 must land in the SAME diff as the registry change, per that section's own CI-enforced rule — already done, see below), `docs/solution/Architecture_as_Code_Solution_Design_v2.md` §7.1 (the eligibility rule this class satisfies)
**Priority:** #1 in the 2026-09-02 LLM-assist consolidated planning list — highest user-visible value: this is what actually gets the 3 architecturally-central missing edges (found reviewing a real scan against its own published reference diagram) in front of an architect instead of silently sitting in `ignored-items-report.json`.
**Why this file exists:** the design (§3.2) is fully written and reviewed; §3.1's producer table registry row already exists (`| unresolved-outbound-target (new, 2026-09-02) | B | unresolved-outbound-target |`). **Nothing in code exists yet** — confirmed directly: no reference to `unresolved-outbound-target` anywhere in `pipeline/src/` or `tools/review-session/` before this task. This file is the concrete build list, T-1 being the part that delivers the real value, T-2 being a separate, lower-priority, larger piece explicitly named "not yet built" in the design.

## T-1 — wire the trigger (the part that actually surfaces the 3 edges)

**Real, good news found while scoping this task: batch/non-interactive drafting (§3.2's "New" bullet) already exists as a side effect of how `draft_tier_b.py`'s `main()` works today** — it already collects every open Tier B residual (`tier_b = [r for r in residuals if r.get("tier") == "B" and r.get("status") == "open"]`) and runs `process_tier_b_batch` over the whole set in one non-interactive pass, `draft_tier_b.py:184-211`. §3.2's own "New" bullet about batch drafting is **already mechanically true for any Tier B class**, including this one, once it exists — no new permission or code path is needed there. Don't build a second batch mechanism; the new trigger just needs to become a real Tier B residual and it flows through the existing pipeline automatically.

**What's actually missing — 3 small, precisely-located changes, all following an existing pattern in the same files:**

1. **`pipeline/src/analysis/ir/hitl-review-trigger.ts`** — add a new always-on block (same pattern as the existing `multi-hop-single-candidate-below-threshold` block at lines 219-230, NOT gated on a `silenceFlags` check, since — like that block — this is about one specific, already-evidenced edge, not a run-wide completeness gap): scan `facts.ignoredItems` for `reason === 'CROSS_DOMAIN_UNRESOLVED'` and `detail` starting with either `unresolved-http-target:` (from `outbound-http-detector.ts`) or `unresolved-env-target:` (from `env-soft-graph-detector.ts`) — both prefixes are real, already-emitted string constants; grep each detector file for its own prefix literal before hardcoding it a second time here, per this project's "one source of truth" convention (see how `UNRESOLVED_MULTI_HOP_PREFIX`/`TIER_B_SINGLE_CANDIDATE_PREFIX` are imported, not re-typed, at the top of this file). Add `'unresolved-outbound-target'` to the `ReviewQueueItem['trigger']` union. Extract the source unit id from `item.ref` if the ignored item carries it (check both detector files for whether `ref` is already set to the referencing unit's id, same convention `contradiction-detector.ts` uses) — prefer that over string-parsing `detail`, only fall back to parsing if `ref` isn't populated.
2. **`tools/review-session/triage.py`** — add `"unresolved-outbound-target": ("B", "unresolved-outbound-target")` to `_TRIGGER_MAP` (`triage.py:52-59`), matching the exact row `Architect_Residual_Review_Session.md` §3.1 already documents. Add the 1-2 line comment explaining the class, matching the style of every other `_TRIGGER_MAP` entry's preceding comment block.
3. **Confirm (likely no code change) `draft_tier_b.py`'s `SYSTEM_PROMPT` and `build_user_prompt`, and `cards.py`'s card rendering, are generic enough to handle this new trigger correctly as-is** — both are evidence/tier-driven, not trigger-keyed (`cards.py` has zero trigger-specific branches today, confirmed by grep). §3.2 says this class "reuses `single-candidate-below-threshold`'s card shape" — verify that's literally true by reading a real generated card for this trigger in the live-run step below, not by inspecting the code alone. If the card or prompt reads oddly for this specific evidence shape (a ConfigMap value or an HTTP call site, rather than a bridge-interface candidate list), a small trigger-aware addendum (same pattern as `AGENT_TASKS_Dossier_Uncatalogued_Auth.md`'s `S2_AUTH_JUDGMENT_ADDENDUM`, already shipped) is acceptable — but confirm the need with a real live run first, don't add one speculatively.

**Non-negotiable constraints:**
- **Reuse the detail-prefix constants, don't re-type them.** `outbound-http-detector.ts` and `env-soft-graph-detector.ts` each already export (or should export, if not already) their own prefix string — import, don't duplicate, matching this file's own existing convention for `UNRESOLVED_MULTI_HOP_PREFIX`/`TIER_B_SINGLE_CANDIDATE_PREFIX`/`CONTRADICTION_PREFIX`.
- **`unresolved-multi-hop`'s zero/2+-candidate cases and `unresolved-k8s-deployed-in` stay excluded, unchanged** — do not widen the new block's prefix match beyond exactly `unresolved-http-target:`/`unresolved-env-target:`. This is the whole point of §3.2's evidence-specificity eligibility rule (design doc §7.1) — widening it here would silently undo that design decision.
- **`pipeline/scripts/check-residual-taxonomy-sync.js` must pass** — it fails a diff touching `silenceFlags`/`IgnoredItem.reason`/`_TRIGGER_MAP` without a matching `Architect_Residual_Review_Session.md` change in the same diff. §3.1's row already exists from the design pass, so this diff should already satisfy it — but run the check to confirm, don't assume.
- **No change to `override-applier.ts`, `apply.py`, or the Decision Record/Override schema** — every part of the write path is unchanged; this task only makes a new residual reachable, it doesn't touch how a draft becomes a fact.

**Verification:**
- `cd pipeline && npm test` — full suite green, plus a new regression test in `pipeline/test/regression.test.js` asserting `buildReviewQueue` emits an `unresolved-outbound-target` item for a synthetic `unresolved-http-target:`/`unresolved-env-target:`-prefixed ignored item, and does NOT emit one for `unresolved-multi-hop`/`unresolved-k8s-deployed-in`-prefixed items (the negative case closing the eligibility-rule bar).
- `cd tools/review-session && python3 -m unittest discover -s . -p "test_*.py"` — full suite green, plus a new `test_triage.py` case confirming `unresolved-outbound-target` maps to `("B", "unresolved-outbound-target")`.
- `node pipeline/scripts/check-residual-taxonomy-sync.js` (or however it's wired in CI — check `.github/workflows/residual-taxonomy-sync.yml` for the exact invocation) passes.
- **Real end-to-end live verification, this project's own standing bar for this whole layer** — re-run the same real scan used to find this gap originally (or an equivalent fixture with a real `unresolved-http-target`/`unresolved-env-target` ignored item), confirm `review-queue.json` now lists an `unresolved-outbound-target` item, `pack.py` turns it into a real Tier B residual, `draft_tier_b.py` drafts it as part of its existing batch pass, and the drafted `relationship_add` — once an architect applies it via `apply.py` — actually closes one of the 3 real missing edges this whole design exists to fix. This is the actual proof the design works, not just that the code compiles.
- `docs/solution/BACKLOG.md` — the existing `unresolved-outbound-target` row (added `c84da7b`) should be updated from "designed" to a dated "shipped" status once T-1 is verified end-to-end, same convention as the dossier-addendum row above it.

## T-2 — preview/merge tool (separate, lower priority, do not block T-1 on this)

**Named explicitly in §3.2 as "new, and not yet built" — a real, standalone gap, not a blocker for T-1's value.** A read-only tool that renders canonical `architecture.calm.json` plus the current `drafts/` directory's pending `relationship_add` overrides as one merged CALM document, with drafted-but-unconfirmed relationships tagged distinctly (e.g. `x-aac-draft: true`) — never touching the canonical file or `drafts/` itself. Needed so an architect can see the complete, semantically-marked picture before confirming anything, rather than a bare text list of pending drafts.

This is a genuinely separate, larger design question (a new CALM-merge renderer, a new tagging convention, a decision about where this tool lives in `tools/review-session/`) — per `CLAUDE.md`'s "two solutioning passes, hard cap," give this its own short design pass before writing code, rather than treating it as an extension of T-1's mechanical wiring. Do not start T-2 until T-1 is shipped and verified — T-1 alone already delivers the batch's highest-priority value (the 3 missing edges become reviewable and draftable); T-2 only improves how the resulting drafts are previewed.
