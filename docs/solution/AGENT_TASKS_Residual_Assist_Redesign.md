# AGENT TASKS — Residual-review LLM layer redesign (evidence dossier + taxonomy sync)

**Lane:** Independent · **After** nothing — all inputs already exist and are real, tested code
**Governance/DoD:** `docs/solution/Architect_Residual_Review_Session.md` §3 (redesigned 2026-08-23 — read this first, it's the design authority for every task below)
**Why this file exists:** §3's redesign separated "write-permission tier" from a new "Evidence Dossier" concept and added a fully code-verified producer registry, but that was a doc-only pass — no runtime code changed. This file breaks the follow-on build into session-sized tasks, per this repo's own session-economy convention (one task, one fresh session, self-contained enough that a session doesn't need this conversation's history).

**Sequencing note (2026-08-23, resequenced on review before any task started):** the original draft of this file put the live-key verification last, after building a shared evidence-assembly module and wiring a dossier pass into every `pack.py` run by default. That's backwards — it designs a mechanism, and a cost-sensitive default, before the one real data point (an actual model run) that should inform both exists. **T-1 now runs first, against today's unmodified code, specifically so its findings can inform T-2's design instead of the other way around.**

**Read before starting any task below**: `tools/review-session/README.md` (tool inventory + S1–S12 rules), `Architect_Residual_Review_Session.md` §3 (the redesigned taxonomy — Tier vs. Dossier, the producer registry), and the specific source files named in each task's own row.

| Task | Status | Depends on | Acceptance |
|---|---|---|---|
| **T-1 Live-key smoke test — run first, no refactor** | **Closed 2026-08-23 — real evidence below, see also Changelog** | — | Run against a real fixture scan (`pipeline/test/fixtures/nestjs-sample`, real `S2-http-without-security-control` residual) for `advisory.py`, plus one synthetic-but-schema-real Tier B residual (no real Tier B trigger exists in any checked-in fixture — `spikes/fineract` is the only known real source and was deliberately not scanned this pass for time/cost reasons; same disclosed gap `test_draft_tier_b.py`'s own `TestNoKeyCliPath` already names) for `draft_tier_b.py`. **Backend used: the `claude` CLI itself (`claude -p --output-format json`), not a raw `ANTHROPIC_API_KEY`** — this environment had Claude Code already authenticated, so `_call_llm` was extended (both files) to prefer the CLI when present and fall back to a raw key otherwise; this is itself a real, useful T-1 finding (a pilot operator doesn't need to manage a separate secret if they already have Claude Code). **Three real bugs found and fixed, none hypothetical:** (1) the model wraps its JSON answer in a bare ` ```json ` fence even when told not to — `parse_and_validate_response`/its `advisory.py` twin now strip a leading/trailing fence. (2) A second live call prefixed a full paragraph of "Evidence Analysis" prose BEFORE the fence — the first fix didn't catch this; `_strip_markdown_json_fence` now searches for a fenced block anywhere in the text first (regex, both files), falling back to the original leading-strip. (3) The original hardcoded 120s subprocess timeout was too short for a real Tier B drafting call — one real call took 131.75s (128,464ms per the CLI's own report) and would have been killed mid-response; raised to 240s (`CLAUDE_CLI_TIMEOUT_SECONDS`) in both files, a real measured margin, not a guess. **Real cost/latency captured** (redacted of nothing sensitive — no secrets in these numbers): `advisory.py` (Tier A, evidence explanation only) ≈ 40–49s wall, ≈ $0.08/call, ~9–10K tokens of fixed per-call overhead from the CLI's own context (tool defs, not this tool's prompt) on top of the actual content. `draft_tier_b.py` (Tier B, drafting) ≈ 47–132s wall, up to $0.32/call for one drafting attempt — materially higher than the dossier call. **Real reliability finding**: the exact same synthetic Tier B residual, run twice, produced two different real outcomes (a full drafted `relationship_add` the first time, `cannot_decide` the second, citing missing schema context) — genuine run-to-run variance, not a bug, but real information for how much a single call's outcome should be trusted. **Quality**: `advisory.py`'s dossier output was accurate, fully evidence-grounded, cited only real file:line refs already in the pack, and correctly reasoned about NestJS's global-guard pattern (auth can exist outside the file it flagged) — a genuinely good signal for the design's core premise. **Verdict feeding T-2**: real cost ($0.08–$0.32) and real latency (40–132s, non-trivial variance) per residual — this closes T-2's open default-on-vs-opt-in question in favor of **opt-in**, not automatic, matching the `--auto-codeql` precedent this row anticipated. |
| **T-2 Shared evidence-assembly + Evidence Dossier — default decided (opt-in), by T-1's real evidence** | Not started — unblocked, T-1's numbers are in | T-1 (closed 2026-08-23) | Extract `advisory.py`'s evidence-gathering/prompt-assembly logic (the code that builds a residual's relevant-units + evidence-snippets context, and `parse_and_validate_response`'s grounding checks) into a new shared module, `tools/review-session/llm_common.py`. `advisory.py` and `draft_tier_b.py` both call it instead of each maintaining their own copy — reuse T-1's own `_call_llm`/`_strip_markdown_json_fence`/`_llm_backend_available` (claude-CLI-preferred, API-key-fallback, 240s timeout, fence-with-prose-tolerant) as the shared network boundary, don't re-derive it. Wire an evidence-dossier pass into `pack.py`. **Decided by T-1's real numbers, not by default**: the dossier pass is **opt-in**, behind an explicit flag (e.g. `pack.py --with-dossier`) — T-1 measured $0.08–$0.32 and 40–132s per residual via the claude-CLI backend, non-trivial for a pack with more than a handful of open residuals, matching the `--auto-codeql` precedent this row anticipated. Also worth carrying into the design: T-1 found real run-to-run variance on an identical input (draft vs. `cannot_decide`) — the dossier/draft pass should not be assumed idempotent across reruns. No key present → same graceful no-op convention every other LLM-optional tool here already uses. Dossier output (`{explanation, hypotheses, evidenceRefsUsed}`) attaches as an additive `dossier` field on each residual in `residuals.json` (update `residuals-schema.json` to match, same pattern `consequence.py`'s additive field already established). Tier B's own draft attempt reuses the dossier's already-assembled evidence context rather than re-gathering it. New tests: dossier attaches for Tier A/B/C residuals alike (synthetic responses, no live key — same test convention `test_advisory.py`/`test_draft_tier_b.py` already use, now also matching T-1's PATH-stripping trick so these stay deterministic regardless of what's installed on the test machine); no-key AND no-`--with-dossier` paths still produce a normal, dossier-less pack; Tier B's bar-not-met path is recorded as `draftOutcome: "bar-not-met"` per §3's named state-transition rule, not a silent reclassification. |
| **T-3 `fetch-span --anchor`/`--context-lines`** | Not started | — (independent; useful to have before T-2 finishes, not a hard blocker) | Extend `tools/review-session/pack.py`'s `fetch_span_main` with an anchor-based mode alongside the existing exact `--start-line`/`--end-line` mode: `pack.py fetch-span --session-dir <pack> --residual-id <id> --anchor <file:line> --context-lines <n>`. Reuse the existing internal window-computation helper `pack.py` already uses for its own first-pass snippet extraction (grep for where initial `evidence/packs.json` snippets are built) — do not write a second windowing implementation. Same redaction, same path-restriction-to-`packageRoots`, same `FETCH_SPAN_SESSION_MAX_CALLS`/`FETCH_SPAN_SESSION_MAX_LINES`/`FETCH_SPAN_HARD_CAP_LINES` caps as the existing mode — this is an alternate way to *specify* a span, not a new mechanism with its own limits. New tests in `test_pack.py` covering the anchor mode's own boundary cases (anchor past EOF, anchor outside package roots, cap interaction with the existing exact-range mode in the same session). Update the chat-mode file's "Extra-read" section and `README.md`'s `fetch-span` usage example to show both modes. |
| **T-4 Mechanical residual-taxonomy sync check** | Not started | — (fully independent — no dependency on any LLM work) | New `pipeline/scripts/check-residual-taxonomy-sync.js`, structurally modeled on the existing `pipeline/scripts/check-generalization.js` (same git-diff-against-base-ref shape, same "flag it, exit 1, don't try to judge intent" posture). Flags a diff that touches `coverage-report.ts`'s `silenceFlags.push` call sites, `typed-facts.ts`'s `IgnoredItem.reason` union, or `triage.py`'s `_TRIGGER_MAP` with no corresponding change to `Architect_Residual_Review_Session.md` in the same diff. Wire into `.github/workflows/pipeline-test.yml` alongside the existing `check-generalization.js` step. Verify against 3 real scenarios in a disposable scratch clone (same verification discipline `check-generalization.js`'s own BACKLOG.md row used): a producer change with no doc change → fails; the same change with the doc updated → passes; an unrelated change → passes. |
| **T-5 Fold `validate_drafts.py` into `apply.py`'s existing auto-revalidation** | Not started | T-2 (the documented minimum path below names the dossier/draft step, so this should reflect T-2's final default-on/opt-in decision) | `README.md`'s residual-review workflow already documents that `apply.py` re-validates every draft automatically before applying — `validate_drafts.py` as a separate required step is redundant with that, already optional in practice (the docs already say "run it yourself first if you want to check before apply.py does it again"). Make this explicit rather than leaving it as an implied optional step: update `tools/review-session/README.md`'s "How to run" quick-start and the top-level `README.md`'s "Residual review workflow" section so the documented minimum path is pack → (dossier/draft, per T-2's decided default) → answer cards → apply, with `validate_drafts.py` demoted to "optional, for checking before you're ready to commit to applying." No code change required if `apply.py`'s auto-revalidation is already equivalent to `validate_drafts.py`'s own checks (confirm this directly by diffing what each actually checks before touching any docs) — if they've drifted apart, that's a real bug to fix here, not assume away. |

**Refuse:** building a new MCP server or any new source-reading mechanism beyond T-3's `fetch-span` extension — considered and rejected during the design pass (`Architect_Residual_Review_Session.md` §3 Changelog, 2026-08-23): a genuinely new extraction/read mechanism needs its own evidenced justification per this project's Simplicity First principle, and `fetch-span`'s existing bounded/redacted/capped model already does the job. Also refuse: deciding T-2's default-on-vs-opt-in question without T-1's real numbers in hand — that's exactly the "specified vs. proven" gap this whole redesign exists to stop repeating.

**Contract:** none of these tasks touch `typed-facts.json`/`TypedFacts` — this entire lane operates strictly downstream of `run-slice`'s own deterministic output, per `OOS_Registry.md`'s `OOS-llm-core-path` (revisit trigger: never). No `CONTRACT_VERSION` bump is expected for any task above; if one seems necessary, stop and re-read `Contract_Evolution_Policy.md` before proceeding — that would mean the task has drifted outside this lane's intended scope.
