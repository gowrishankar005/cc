# Claim Register — what we may say is “built”

**Status:** active (Wave 1-A0)  
**Authority:** Prefer this file over marketing language in STATUS/CLAUDE when they disagree on **relationship or control completeness**.  
**Master sequence:** AGENT TASKS Master Sequence Claim Honesty and AREC  
**Last updated:** 2026-08-08

---

## How to use this register

| Status | Meaning | Allowed green |
|---|---|---|
| **proven** | Verified on real evidence + regression or documented run | L1 unit recall; L2 only if claim says so |
| **partial** | Some strategies work; not the full product claim | L0–L1; L2 only for proven sub-shapes |
| **specified-unbuilt** | Design/requirements named it; code incomplete or absent | Must not claim “works”; OOS or fail L2 |
| **unevidenced** | No solid sample yet | No product claim |
| **OOS** | Explicitly out of product scope for now | Silence OK only if scope-limitation id present |

**Rule:** Do not mark STATUS “built” for a cell here without evidence pointer + validation layer that is allowed to green.

---

## Dual-unit Graphify decision (product, not implementation accident)

| Decision | **Dual-unit structural edges are an interim / structural product (R0), not the full definition of “architecture relationship.”** |
|---|---|
| Meaning | Graphify reconciler only emits edges when **both** endpoints are TypedUnits. That is correct for **code-level structural** edges. It is **insufficient** alone for enterprise **architecture stories** (e.g. layered the reference Java/JAX-RS banking platform API → platform service → entity). |
| Claim language | May say: “structural/code-graph relationships between discovered units.” Must **not** say: “service→persistence architecture is recovered for Java enterprise monorepos” without R1/R2 strategies (AREC). |
| Date | 2026-08-07 (Wave 1-A0 lock for claim honesty; AREC details in Wave 2) |

---

## Mechanism-class matrix

| ID | Mechanism class | Status | Evidence | Validation allowed green | Notes |
|---|---|---|---|---|---|
| **U-http** | HTTP entry via **framework routes** (native route typing / composed JAX-RS / Nest / Spring-style annotations) | **proven** for that shape only | the reference Python app Flask, Nest fixture, the reference Java/JAX-RS banking platform charge/core, lab fixtures | L1 paths for framework-annotated / native-typed routes | **Does not** include AWS Lambda + API Gateway (paths often live only in CFN/SAM). Do not cite this cell as “HTTP complete for a large financial-services organization cloud.” See **U-http-serverless**. |
| **U-http-serverless** | HTTP entry via **serverless / API Gateway** (Lambda `RequestHandler` / handler class + path from CFN/SAM/Serverless and/or OpenAPI join) | **partial → proven for the Java+explicit-CFN shape, 2026-08-08 (Y2+Y3+Y4)** | Real, full join now works end to end: `implements RequestHandler` → real `service` unit (Y3); handler owning `DynamoDbClient` directly stays `service`, not `database` (Y2, D-dynamo-priority); **CFN/SAM path join (Y4, `cfn-manifest-provider.ts`)** resolves the real cross-file `Resource`→`Method`→`Function.Handler` join and attaches real paths. **Verified on the real wild repo this whole finding was built around**: `spikes/the reference AWS SaaS sample/repo/services/tier-service` re-scanned with its own real CFN templates — `TierService` now a real `service` unit with **5/5 real paths exactly matching gold** (`GET/POST /tiers`, `GET/PUT/DELETE /tiers/{id}`), real `TierService → DynamoTierDataStore` architecture-grade edge present, exact match to gold. Lab fixture (`java-lambda-apigw`) L0/L1/L2 all PASS against hand-authored gold. | L1 unit-kind + path recall for the **Java, explicit-CFN-resource (non-SAM-shorthand)** shape — proven on both lab and the real the reference AWS SaaS sample repo | **Yardstick authority:** matrix rows **FY-http-lambda**, **FY-infra-cfn** in Fidelity Yardstick Closeout Matrix. Design: Serverless HTTP and Dynamo Ownership Design. Real, honestly-named residuals: Node/Python handlers (no sample yet); SAM `Events:` shorthand (different shape, deferred); cross-stack/nested-stack CFN references (out of v1 scope); only 5/26 real CFN bindings found across the reference AWS SaaS sample's shared `resources/` dir bound to THIS scan's roots — the other 21 are real routes for other services not in this scan, correctly left unresolved, not guessed; multi-root scan across services not attempted. `threat-signals`' S2 check now DOES extend to CFN-bound routes (real positive side effect — 'structured-file' source shares the 'http-entry-point' category). Product: **B-lambda-http** (partial), **B-dynamo-handler-kind** (done). |
| **U-entity** | JPA `@Entity` → database unit | **proven** | the reference Java/JAX-RS banking platform charge/core, lab java | L1 database nodes | |
| **U-persist-import** | Driver/import persistence (SQLAlchemy, Prisma `ref_`, …) | **partial → proven for @prisma/client's ownership shape** (B-ontology, 2026-08-08); **Java Dynamo client import still coarse** (HT-ASB-002) | the reference Python app SQLAlchemy; real the reference Node/NestJS wealth-management app re-run: `AccessService` (imports Prisma types only) correctly ZERO database units; `PrismaService` (genuinely `extends PrismaClient`) correctly 1 real unit; **counterexample:** the reference AWS SaaS sample `TierService` imports `DynamoDbClient` → mis-kinded `database` (true store is separate class) | L1 with low confidence ok; L1 ownership-precision now real for the one evidenced Prisma library; **must not** claim Java Dynamo ownership is fixed | Q13/B-ontology fixed Prisma only. Open: **B-dynamo-handler-kind** (handler evidence / ownership priority for AWS SDK Dynamo imports). Node Dynamo lab store path still valid (T-E3). |
| **U-msg-consumer** | `@KafkaListener` → network/topic | **partial** | the reference Java/JAX-RS banking platform listener + lab kafka; gold maps to network | L1 network unit | Producer call-site still open |
| **U-msg-producer** | `KafkaTemplate.send` / SQS send | **partial, both mechanisms now dispatched** (T-E1 + B-msg-prod-sqs, 2026-08-08) | Real the reference Java/JAX-RS banking platform `KafkaExternalEventProducer.java` (`fineract-provider`) — `topic`/`network` node from its `KafkaTemplate`-typed field; real lab `ts-orders-dynamo` — `OrdersDynamoStore` (imports both `@aws-sdk/client-dynamodb` and `@aws-sdk/client-sqs`) now carries real merged messaging evidence, not silently dropped | L1 network unit for Kafka; L1 import-only evidence for SQS/SNS (low confidence, same tier as persistence's own driver-import) | Kafka: detects the field TYPE (architectural commitment), not a paired `.send()` call — see `scope-limitations.yml`'s `messaging-producer-field-type-only`. SQS/SNS: import-only strategy was already catalogued but `messaging-pass.ts` silently dropped its evidence whenever the SAME class was already claimed by an earlier pass (persistence) — real gap, now fixed: evidence merges onto the existing unit instead (kind still decided by whichever pass ran first, unchanged precedent) |
| **U-spring-data** | Spring Data repository interfaces (`extends JpaRepository`) | **partial** (T-E3, 2026-08-08) | Real the reference Java/JAX-RS banking platform `ChargeRepository.java` — `extends` reference detection, plus a real knock-on `connects` relationship to `Charge` via R0 | L1 database unit | |
| **U-jooq** | jOOQ (`org.jooq.*` import) | **partial / proven on the reference Java governance platform** (T-R1-3) | Real the reference Java governance platform `waltz-data` — 229 `database` units via resolved `org.jooq.*` imports (`java-import-resolver.ts`) | L1 database units | Root cause was Graphify bare-symbol targets; fixed by source-line package resolve — not a separate jOOQ-only strategy |
| **U-outbound-http** | Outbound HTTP client → service→service relationship (Feign/WebClient/RestTemplate/requests/axios/OkHttp/HttpURLConnection/…) | **partial → proven for the `import-only-client` catalogued-library shape (2026-08-09)**: `outbound-http-detector.ts`/`outboundHttpPass` built and live in `DEFAULT_PASSES`; catalogue now covers `RestTemplate`/`requests`/`axios`/`httpx`/`node-fetch`/`WebClient`/`OkHttpClient`/`HttpURLConnection` | Real evidence gathered across sessions: the reference Python app `frontend.py`, the reference Java/JAX-RS banking platform `SendMessageToSmsGatewayTasklet.java` (both honest `unresolved-http-target` ignored-items — runtime-constructed URLs, no static literal target, never a fabricated `connects`); **B-http-client fix (2026-08-09)** — the reference Java/JAX-RS banking platform's real `ExternalCreditBureauIntegrationWritePlatformServiceImpl.java` uses `OkHttpClient`(L46)/`HttpURLConnection`(L33); added both to the catalogue, re-scanned the real credit-bureau package root and confirmed 2 `unresolved-http-target` items emitted, `calm validate` 0 errors, new locked regression test, full suite 60/60 green | L1 import-only evidence (low confidence, same tier as persistence's own driver-import) for cataloged libraries; still never resolves a target — every detection lands as an ignored-item for HITL, by design | `@FeignClient`-style resolvable-target detection (`resolvable-target-decorator` strategy) remains honestly `not-implemented` — no real sample found yet, separate residual from this cell's now-closed catalogue-completeness gap. |
| **R0** | Dual-unit Graphify structural edges | **proven** (as structural, now graded) | Core entity–entity; the reference Python app service–db when one-hop | L2 **only** for shapes that match R0 | Not the reference Java/JAX-RS banking platform API→entity. `TypedRelationship.grade`/`x-aac-relationship-grade` (T-A2, 2026-08-07) labels every relationship `structural`\|`architecture`\|`trust` — the reference Java/JAX-RS banking platform-core's 64 entity–entity edges verified `structural`, the reference Python app's service→db edge verified `architecture` |
| **R1** | One-hop architecture service→db (import/calls between units) | **partial, regression-locked** | the reference Python app Python service→AccountDb | L2 the reference Python app-class | T-B1 (2026-08-07): hard regression asserts a real service->database `connects` edge by endpoint KIND + `grade: architecture`, not just a relationship count — R2 work (Session C/D) cannot silently regress this shape |
| **R2-mechanism** | Multi-hop / layered detection mechanism (Phase 1 short-circuit + R2b implementer-import hop) | **partial/proven** (T-C1 + R2b/T-R1-2 + T-R1-3) | `multi-hop-bridge-detector.ts` + `java-import-resolver.ts` (T-R1-3 import-target root-cause fix both mechanisms depend on); synthetic fixtures `r2-bridge-sample` (Phase 1), `r2b-implementer-hop-sample` (R2b + 2-implementer ambiguity refusal, standing exams **E-r2-ambiguity**/**E-r2b-positive**); real the reference Java/JAX-RS banking platform `fineract-security`; real the reference Java governance platform `waltz-data` (229 real jOOQ units via the same import-resolution fix) | L1/L2 for the specific synthetic + real shapes cited above, regression-locked | The mechanism itself (never fabricates, honestly refuses 0/2+ candidate cases) is real and tested. **This cell alone does not establish what any specific package will resolve to** — see the two claim-triple-specific cells below for the reference Java/JAX-RS banking platform-charge, which is where the "mechanism proven" vs "flagship closed" conflation actually happened (paid for twice — AGENT TASKS Layered Architecture Story §0.2). Still inapplicable for: Python-shaped bridges (no structural `implements` edge), command-bus/dynamic-dispatch write paths (permanent non-goal), **and — real finding, 2026-08-08 — a bridge candidate with no `implements` edge at all (a concrete class referenced directly, common real Spring idiom without an interface layer)**: real probe against fresh full-clone `finos/waltz` found 28 such candidates, 0 fabricated (discipline held), 0 resolved (coverage doesn't reach this shape) — see `coe-lab/docs/findings/waltz-multihop-genericity-probe.md`, tracked as **B-multihop-direct-delegate**. **Program-final (T-L4-1, 2026-08-08):** `multi-hop-bridge-detector.ts` edges now carry a `mechanism` field (`r2-phase1`/`r2b`, T-L2-1) distinguishing which branch produced them, without decoding confidence values; `E-r2-ambiguity`/`E-r2b-positive` both reconfirmed via a full 56/56-green suite run. `D-terminal-refine` (domain-store refine, L2b) decided **not built** — no live case resolves via R2b for the flagship shape, reasoned + revisitable (AREC Store Terminal Policy §3). |
| **R2-gold-charge-single** | the reference Java/JAX-RS banking platform-charge, single-root, scored against single-root hand gold (`coe-lab/gold/calm/fineract-charge/`) | **expected-fail, by design — not a detector bug; now verified via the exact frozen protocol (T-L1-2/T-L1-3, 2026-08-08)** | Real `run-slice` on `fineract-charge` alone → `coe-lab/generated/fineract-charge/` → `validate-calm-pair.mjs --package fineract-charge`: `L0 schema PASS`, `L1 unit recall PASS`, `L2 story FAIL` (`missing connects topology service->database`) — the exact standing-exam protocol, not just the earlier mechanism-level regression test | L0 PASS, L1 PASS, **L2 FAIL is the correct, expected outcome, now automated** via `coe-lab/scripts/run-expected-fail-exams.mjs` (T-L1-3, exits 0 for this exact result) — see standing exam **E-charge-single-L2** | `ChargesApiResource` never imports `Charge`; there is no static one-hop chain in the real source for this shape (root cause #2, `fineract-gold-vs-platform-finding.md`). This is a real fact about the reference Java/JAX-RS banking platform's own layering, not a gap to be closed by loosening the gold or silently rescoring against a different root set. **Do not** cite R2-multi-root-access-terminal's PASS as evidence this cell passes — different claim triple entirely. **Program-final (T-L4-1, 2026-08-08):** re-confirmed on a genuinely fresh re-scan (not a cached artefact) via the automated `run-expected-fail-exams.mjs` harness — PASS, exit 0. This is the closing state of `B-layered-story`'s L0-L4 program for this cell. |
| **R2-multi-root-access-terminal** | the reference Java/JAX-RS banking platform `fineract-charge`+`fineract-provider`, multi-root, service→access-layer-store terminal | **partial/proven — now gold-scored (T-L1-1/T-L1-2, 2026-08-08), not only mechanism-level** | Real combined multi-root `run-slice` scan → scored via `validate-calm-pair.mjs --package fineract-charge-provider --require-l2` against the new hand-authored gold `coe-lab/gold/calm/fineract-charge-provider/` (authored from real source, `ChargesApiResource.java`/`ChargeReadPlatformServiceImpl.java`, never from generator output): `L0 schema PASS` (both), `L1 unit recall PASS`, `L2 story PASS`, exit code 0; standing exam **E-charge-multi-story** | **L2 PASS against a real hand-authored multi-root gold** — root-set/scan-mode guard (T-L1-2) confirms the scored generated file genuinely has 2 package roots (`x-aac-package-roots`) before scoring, so this isn't a mismatched artefact | **Terminal is `ChargeReadPlatformServiceImpl`, an access-layer JDBC-backed store unit — NOT the same node as R2-gold-charge-single's expected `Charge` entity endpoint.** Claim triple: `{rootSet: fineract-charge+fineract-provider, terminalGrain: access-layer, evalArtefact: E-charge-multi-story}`. This is the S-layered-access story (AGENT TASKS Layered Architecture Story §0.4), not S-shallow — stating it as "flagship the reference Java/JAX-RS banking platform case closed" without naming the terminal grain and root set is exactly the forbidden phrasing below. **Program-final (T-L4-1, 2026-08-08):** re-confirmed on a genuinely fresh multi-root re-scan (not a cached artefact) — `L0/L1/L2` all PASS, exit 0, same real edge reproduced. This is the closing state of `B-layered-story`'s L0-L4 program for this cell. |
| **R-k8s** | shares-secret / env soft-graph | **partial** | the reference Python app k8s; deployment correlation post-MVP | L2 when manifests passed | T-E6 (2026-08-08, watch — no fix needed): re-ran the full real 6-service the reference Python app k8s+env-soft-graph scan under HARDER conditions than the original fix was verified against (T-E3 added new, similarly-named `Transaction`/`TransactionRepository` database units for all 3 Java services since then) — confirmed 0 unresolved k8s deployments, 0 substring leaks into any k8s/env-derived relationship endpoint. Service-kind-only guard holds |
| **C-dec** | Decorator controls (`@PreAuthorize`) | **proven** | DatatableWriteService | L1 controls presence | Now also carries `expression` when the decorator has one (C-rich, T-D2) |
| **C-call** | Call-site auth (`validateHasReadPermission`, `jwt.decode`, `hasRole`, `isAuthenticated`) | **partial** (T-D1 2026-08-07 + T-R2-2 2026-08-08) | Real `ChargesApiResource.java` (fineract-charge, lines 84/101/129), real `AppUser.java` (fineract-core), real lab `py-jwt-gateway` (line 14); **new**: real the reference Java governance platform `waltz-web` (4 real call sites, `WebUtilities.java`/`BulkUploadLegalEntityRelationshipEndpoint.java`/`LicenceEndpoint.java`/`SettingsEndpoint.java`), real the reference Java/JAX-RS banking platform `PlatformSecurityContext.isAuthenticated()` (`ClientSearchService.java:52`) | L1 controls presence for 4 named vocabularies now (Java `validateHas*Permission`/`hasRole`/`isAuthenticated`, Python `jwt.decode`) | 4 vocabularies, still not a general call-graph auth inference; see `scope-limitations.yml`'s `call-site-controls-partial` |
| **C-contract** | OpenAPI securitySchemes | **partial, expanded** (T-E4, 2026-08-08) | Real lab `ts-nestjs-users` (`bearerAuth: {type: http, scheme: bearer}`) — matched against the scheme's real structural `type`/`scheme` fields (never the author-chosen name), attaches a real `security-contract-http-bearer-001` control | L1 controls presence for http-bearer/http-basic/apiKey/oauth2 | `openIdConnect` and richer OAuth2-flow detail not modeled |
| **C-rich** | Control config richness (expression, authority) | **partial** (T-D2 2026-08-07 + T-R2-3 2026-08-08) | Real `expression` field on the reference Java/JAX-RS banking platform's `security-rbac-002` (`RESOURCE_NAME_FOR_PERMISSIONS`) and lab jwt-gateway's `security-auth-001` (shows the real `verify_signature: False` anti-pattern in source); **new**: `authorityRef` structured field, real on both the reference Java/JAX-RS banking platform (`RESOURCE_NAME_FOR_PERMISSIONS`) and the reference Java governance platform (`SystemRole.ADMIN`/`SystemRole.LICENCE_ADMIN`/`SystemRole.BULK_LEGAL_ENTITY_RELATIONSHIP_EDITOR`), correctly ABSENT on the one real negative case (`WebUtilities.java`'s own plain-variable call site) | L1 requirement not yet raised to require it | Raw source-line TEXT only, never a resolved constant value; `authorityRef` is a naming-convention token match (ALL_CAPS/qualified-enum shape), not semantic authority modeling — a differently-cased or non-conventional constant name would not match |
| **S-silence** | Completeness / empty neighborhood loudness | **partial** | the reference Python app: S1 does not fire (R1 present); multi-root the reference Java/JAX-RS banking platform charge+provider: high arch-cov after T-R1-3; single-root residual modules may still S1; real `the reference AWS SaaS sample-tier-service`/`tenant-service`: **new S5** fires correctly (real dedicated fixture + real repo negative-path check) | L3 for S0/S1/S2/S5; S3 T-D3; S4 scope-limitations | T-A1 completeness block; T-E5 hitl-review-trigger (S1/S2); T-R0-5 S0 Graphify; T-R0-2 arch-cov rate; **T-R4-1** low-architecture-coverage review trigger; **T-Y5-1 (2026-08-08)** — new S5 flag closes the HT-ASB-006 class: 0 service units with a real store present, or real CFN routes found but unbound — the degenerate case S1 structurally cannot catch (S1 needs ≥1 service unit to even fire) |

---

## Product claims — allowed vs forbidden phrases

| Forbidden (without caveats) | Allowed |
|---|---|
| “Java relationships work end-to-end for every monorepo shape” | “JAX-RS/JPA units proven; R1 one-hop proven; R2 mechanism proven — **R2 recovers S-layered-access on multi-root fineract-charge+fineract-provider (E-charge-multi-story); single-root fineract-charge gold L2 remains expected-fail (E-charge-single-L2)**; Python multi-hop and command-bus write paths still out/partial” |
| “R2 flagship the reference Java/JAX-RS banking platform case closed” / “the reference Java/JAX-RS banking platform charge story closed” (bare, no claim triple) | Always name the claim triple: `{rootSet, terminalGrain, evalArtefact}` — e.g. the allowed phrase directly above. See `R2-gold-charge-single` vs `R2-multi-root-access-terminal` rows: these are two different, non-substitutable claims about the same package. |
| “All security/controls captured for every Java API” | “Decorator `@PreAuthorize` proven; call-site vocabularies partial (`validateHas*Permission`, `hasRole`, `isAuthenticated`, Python `jwt.decode`) — not unbounded call-graph auth inference” |
| “Graphify recovers full architecture graph alone” | “Graphify supplies structural dual-unit edges (R0); architecture grade needs R1/R2 + catalogues” |
| “Every ORM import is a database owner” | “`@prisma/client` requires `extends PrismaClient` ownership (B-ontology); plain type imports no longer create database units. Other drivers still import-only unless catalogue gains `ownerBaseClass`” |
| “Messaging complete including cloud queues” | “Kafka field-type producer + SQS **import** evidence dispatched (B-msg-prod-sqs merge fix); SNS still unverified; no queue name / send-call resolution; dual-role class keeps persistence kind with merged messaging evidence” |
| “a large financial-services organization / cloud-native stack fully covered” / “HTTP works for AWS fintech” | “Framework HTTP (**U-http**) proven; Dynamo/SQS **data plane** partial/proven on lab; **serverless HTTP (**U-http-serverless**) specified-unbuilt** — Lambda/API Gateway not product-green until **B-lambda-http** + the reference AWS SaaS sample exam” |
| “Dynamo detection means cloud Java services are correct” | “Dynamo **import** can create `database` units (T-E3); handlers that import the client may still be mis-kinded until **B-dynamo-handler-kind**” |

---

## Process note — how Lambda was “in a large financial-services organization” but not on the build queue

a large financial-services organization is cited often (v0.14, Java solution design, lab research, T-E3 Dynamo/SQS). That did **not** put serverless **HTTP** on the same queue as Dynamo/SQS. Root causes (not “forgot a large financial-services organization existed”):

1. **Concern binning:** v0.14 lists Lambda under **Cloud/deployment** (with EC2/Fargate/CFN/k8s), while **new scope additions §2** only productized Dynamo, SQS/SNS, OpenAPI, OAuth2, multi-DB — not “Lambda is another HTTP entry mechanism.”
2. **Lab proxy substitution:** `fidelity-and-fintech-stack-research.md` maps “AWS Lambda, EKS, …” → **k8s-style deploy manifests**, explicitly “no full AWS account” — EKS stood in for cloud; **Lambda never got a lab package or claim cell**.
3. **Mechanism fit bias:** proven HTTP work is decorator/native-route shaped (Spring/JAX-RS/Flask/Nest). Serverless paths live in **infra**, so they fell into G-L1-07 “IaC later” (low priority) instead of **U-http**.
4. **Partial close looked complete:** **B-dynamo-sqs** / T-E3 closed the a large financial-services organization *data + queue* half of AWS and the row was marked done — easy to read as “cloud a large financial-services organization done” without an HTTP residual.
5. **Evidence portfolio:** until CoE hard-test #2 (the reference AWS SaaS sample), every wild/lab API was annotation-framework HTTP. Discovery cadence re-ranked ontology/SQS, not “missing compute entry shape.”

**Correction:** treat serverless HTTP as a **first-class a large financial-services organization concern** (P1 **B-lambda-http** / **B-dynamo-handler-kind**), not a deploy nicety.

---

## Open questions (Q1–Q12) — decide / park

Living cadence: re-open when claim cells or AREC DoD change. Full narrative: Open Questions Validation.

| Q | Topic | State | Decision / park |
|---|---|---|---|
| Q1 | Definition of done: schema vs unit vs story | **decided** | Layered L0–L5 (`validation-approach-vnext.md`); story = L2 |
| Q2 | Meaning of relationship: code vs architecture | **decided** | R0 structural ≠ architecture claim; R1+ for architecture |
| Q3 | What incompleteness must be loud | **decided; L3 shipped (T-A1)** | Service+db with zero service-touching edges must not L2-pass; `completeness.silenceFlags` (S1/S2) now computed per run, exposed in `coverage-report.json` + `intelligence-ir.md` |
| Q4 | Dual-unit product boundary | **decided** | Interim structural (see above) |
| Q5 | Disconfirming samples for relationship claims | **decided (process)** | the reference Python app shallow + the reference Java/JAX-RS banking platform layered required before “Java rels complete” |
| Q6 | Lab shape-representative? | **decided** | Lab under-represents layering; wild gold mandatory for enterprise claims |
| Q7 | Wild gold owner | **parked** | Lab maintainer / eval owner; refresh on fixture or the reference Java/JAX-RS banking platform module intent change |
| Q8 | Validation stack | **decided; L0-L2 tooling shipped (T-A3)** | L0 calm validate; L1 semantic/unit; L2 story; L3 silence; L4 scope; L5 HITL residual. `validate-calm-pair.mjs` now prints these as distinct lines (not one bundled "semantic" verdict); exit code = L0+L1 by default, `--require-l2` opts into gating on L2 |
| Q9 | HITL → catalogue promotion | **parked** | Follow existing unmapped≥5 promotion; AREC residual separate |
| Q10 | Forever OOS vs deferred | **partial** | Register + scope-limitations; refresh when strategies ship |
| Q11 | Full monorepo vs module scan product mode | **decided (T-A6, 2026-08-07)** | **Module-root scan is the primary/default claim mode** — one `run-slice` invocation against a single package root is what every R0/R1/U-* mechanism-class cell above is proven/partial against, unless a cell's evidence explicitly says otherwise. **Multi-root scans are a real, supported, but secondary mode** — passing 2+ roots to one `run-slice` invocation runs ONE combined Graphify pass over their common ancestor, which is what makes cross-package (`crossPackage: true`) relationships possible at all (single-root runs structurally cannot produce them — there is nothing to cross). A claim of "N cross-package relationships found" is **only valid for the specific multi-root run that produced it** — re-running the same roots individually will show 0 cross-package edges, correctly, not a regression. Real evidenced case: the reference Java/JAX-RS banking platform `fineract-charge`+`fineract-core` combined scan → 265 real cross-module edges (`docs/solution/language/java.md`); every coe-lab/AREC remeasure in this register (§0.6) uses single-root scans and must not be read as having exercised cross-package detection. **Written procedure (T-R0-3, 2026-08-08):** `coe-lab/docs/multi-root-l2-protocol.md` — root-set declaration template, generic root-set shapes, and the rule that any multi-root finding must name its exact root set. |
| Q12 | STATUS “built” vs mechanism subset | **decided** | This register is authoritative for completeness claims |

---

## AREC DoD binding (Wave 2-3)

Architecture completeness claims also require Architecture Relation Evidence Completeness §7.  
In short: R0 structural ≠ architecture L2; R2 + C-call (or OOS) before the reference Java/JAX-RS banking platform-class story green. **Correction, 2026-08-08 (Phase L0, AGENT TASKS Layered Architecture Story)**: the prior wording here ("R2's own multi-hop story for the flagship the reference Java/JAX-RS banking platform-charge case is now L2 PASS in a real multi-root run") was exactly the claim-drift this program exists to stop — it read as if the single-root gold-scored case had passed, when what actually passed was a **different claim triple** (multi-root, access-layer terminal, mechanism-level regression test, not a gold-scored L2 run). Corrected: **R2-gold-charge-single remains L2 expected-fail** (standing exam `E-charge-single-L2`); **R2-multi-root-access-terminal is mechanism-level PASS** for the S-layered-access story on the multi-root fineract-charge+fineract-provider root set (standing exam `E-charge-multi-story`) — see the three split R2 rows above, and `coe-lab/docs/standing-disconfirming-exams.md` for the frozen protocols. C-call (T-D1) remains `partial` (two vocabularies only) — a real relationship existing and a control being detected on the source/target service are still two independent claims; a relationship's existence does not imply its endpoints carry full control coverage.

---

## Changelog

| Date | Note |
|---|---|
| 2026-08-08 | Full a large financial-services organization yardstick close-out matrix published (Fidelity Yardstick Closeout Matrix) — every plane/construct/locus/mechanism/claim/sample/status for one fintech org; agent tasks Y0–Y6. |
| 2026-08-08 | **U-http-serverless** added as **specified-unbuilt** (CoE HT-ASB-001); **U-http** narrowed to framework routes; **U-persist-import** notes Java Dynamo handler mis-kind (HT-ASB-002); forbidden phrases for “a large financial-services organization cloud complete”; process RCA for why Lambda was named in a large financial-services organization docs but never queued. Product: **B-lambda-http**, **B-dynamo-handler-kind**. |
| 2026-08-08 | **Y4 shipped — U-http-serverless promoted `partial (units only)` → `partial → proven for the Java+explicit-CFN shape`.** `cfn-manifest-provider.ts` (new): real, empirically-confirmed finding before coding — the `yaml` package resolves CFN's `!Ref`/`!GetAtt`/`!Sub` intrinsics for free, no custom tag handling needed. Real cross-file `Resource`(path tree)→`Method`(verb+Lambda ref)→`Function`(`Handler` string) join, same-directory multi-file pool (required by real evidence — the reference AWS SaaS sample splits API def and Lambda def across files). New `structured-file` Evidence source (CONTRACT_VERSION 9.0.0), `SOURCE_PRECEDENCE` tier 2 (openapi > CFN > decorator, per D-openapi-fallback). **Verified on the real wild repo, not just the lab fixture**: `the reference AWS SaaS sample-tier-service` re-scanned with its own real CFN templates — 5/5 real paths exact match to gold, real architecture-grade `TierService → DynamoTierDataStore` edge. HT-ASB-001/002/005 closed; HT-ASB-003/004/006 explicitly still open, not swept in. 2 new locked regression tests. |
| 2026-08-08 | **Y2+Y3 shipped (a large financial-services organization yardstick program)**: `U-http-serverless` promoted `specified-unbuilt` → `partial` (unit-kind detection real, verified via locked regression test; paths still pending Y4). Real, empirically-verified finding before writing code: Java `implements` produces its own `referenceKind: 'implements'`, distinct from `extends` — folded into the same extraction pipeline. Real bug found and fixed before shipping: reusing `http-entry-point` category built a bogus path-interface from the raw type-reference signal text; fixed with a new, additive `serverless-entry-point` category (CONTRACT_VERSION 8.0.0) that wins the kind tie-break but stays descriptive-only for interfaces. `B-dynamo-handler-kind` (Y2) required zero new code — the existing `existingServiceFilePaths` mechanism (built for B-ontology) already worked correctly once Y3 supplied the missing evidence. Full suite 56/56 green, including a new locked regression test. |
| 2026-08-08 | **Post-close genericity probe**: real fresh full-clone `finos/waltz` scan (the `spikes/waltz/repo` sparse copy was missing `waltz-service`, couldn't answer this). R2-mechanism's "never fabricate" discipline generalized perfectly (28 real candidates, 0 fabricated). Its resolution coverage did not — the reference Java governance platform's idiomatic concrete-class-direct-delegation shape (no `implements` edge) is invisible to bridge discovery entirely. New, real, generic gap named: **B-multihop-direct-delegate**. `R2-mechanism` cell updated. `D-terminal-refine` unaffected — its own trigger condition (a real R2b-resolved case needing a domain-refine choice) still genuinely unmet. |
| 2026-08-08 | **`B-layered-story` L0-L4 program CLOSED (T-L4-1/T-L4-2).** Full standing exam matrix re-run on genuinely fresh state (fresh scans, not cached artefacts) — all 5 exams PASS, none regressed across the whole program. All three R2 cells carry a final program-close note. AGENT TASKS Layered Architecture Story's own Definition of Program Done (T-L4-3) checklist fully satisfied — see that file for the itemized confirmation. This is the direct answer to the program's own goal: prove a third RCA on this claim-drift class won't be needed — the exam pair (`E-charge-single-L2`/`E-charge-multi-story`) and the split R2 cells are now the standing, checked mechanism that makes that claim durable, not just a one-time fix. |
| 2026-08-08 | **Phase L1 (AGENT TASKS Layered Architecture Story) complete**: both split R2 cells upgraded from mechanism-level/predicted to real gold-scored evidence. `R2-gold-charge-single` — the exact `E-charge-single-L2` protocol run for real for the first time (L0/L1 PASS, L2 FAIL, now automated via `run-expected-fail-exams.mjs`). `R2-multi-root-access-terminal` — now scored against a real hand-authored multi-root gold (`fineract-charge-provider`, T-L1-1), not only a regression test; L0/L1/L2 all PASS. |
| 2026-08-08 | **T-L0-2 (Phase L0, AGENT TASKS Layered Architecture Story):** R2 row split into **R2-mechanism** / **R2-gold-charge-single** / **R2-multi-root-access-terminal** — the prior single "flagship the reference Java/JAX-RS banking platform case CLOSED" row conflated a mechanism-level multi-root regression PASS with the single-root gold-scored claim, which never actually passed. Corrected forbidden/allowed phrases (line ~61-64) and the AREC DoD binding paragraph to require the claim triple `{rootSet, terminalGrain, evalArtefact}` going forward. Linked to new `coe-lab/docs/standing-disconfirming-exams.md` (T-L0-1). Prior 2026-08-08 row below (line 104, then-current) is left as historical record, not deleted — it is the entry that stated the since-corrected wording. |
| 2026-08-08 | Post robustness R0–R4 review: U-jooq → partial/proven the reference Java governance platform; S-silence notes T-R4-1; forbidden/allowed phrases updated (R2 flagship closed; ontology + SQS still open) |
| 2026-08-07 | Wave 1-A0 initial register from the reference Java/JAX-RS banking platform RCA + dual-unit decision |
| 2026-08-07 | Wave 2-3 AREC DoD cross-link |
| 2026-08-07 | Wave 3 T-A1: S-silence → partial (S1/S2 computed real, per-run); Q3 L3 shipped |
| 2026-08-07 | Wave 3 T-A2: R0 grading shipped (`grade` field, design A) — structural/architecture/trust verified on real the reference Java/JAX-RS banking platform-core + the reference Python app |
| 2026-08-07 | Wave 3 T-A3: eval L0/L1/L2 labels shipped in `validate-calm-pair.mjs`; Q8 tooling note |
| 2026-08-07 | Wave 3 T-A4: `scope-limitations.yml` hygiene pass (v0.1.0→v0.2.0) — k8s/env/OpenAPI/messaging bullets de-staled to match this register; new dedicated R2/C-call bullets |
| 2026-08-07 | Wave 3 T-A6: Q11 decided — module-root primary claim mode, multi-root secondary/scan-specific. Session A complete. |
| 2026-08-07 | Wave 3 T-B1: R1 → partial, regression-locked (hard endpoint-kind + grade assertion on the reference Python app service->database) |
| 2026-08-07 | Wave 3 T-B2: R2 design note shipped (AREC R2 MultiHop Strategy), grounded in fresh real the reference Java/JAX-RS banking platform re-investigation — found the interface bridge is real and Graphify-visible, but its implementation is in a third, unscanned module |
| 2026-08-07 | Wave 3 T-C1: R2 → partial. `multi-hop-bridge-detector.ts` built and verified on a synthetic positive fixture + two real the reference Java/JAX-RS banking platform scan shapes (single-root and multi-root), both honest residuals with 0 fabricated relationships. 35/35 tests green |
| 2026-08-07 | Wave 3 T-C2: confirmed `validate-calm-pair.mjs` correctly still reports `fineract-charge` L2 FAIL post-R2 (real regenerate + re-run, not assumed); gold not weakened. Session C complete. |
| 2026-08-07 | Wave 3 T-D1/T-D2/T-D3: C-call → partial (real the reference Java/JAX-RS banking platform + lab evidence), C-rich → partial (real `expression` field), threat-signals rationale softened (S3). `CONTRACT_VERSION` 4.0.0→5.0.0 (`Evidence.source: 'call'`). 38/38 tests green. Session D complete. |
| 2026-08-08 | Wave 3 T-E1: U-msg-producer → partial (real the reference Java/JAX-RS banking platform KafkaExternalEventProducer.java, field-type detection). `CONTRACT_VERSION` 5.0.0→6.0.0 (`Evidence.source: 'field-type'`). 39/39 tests green. |
| 2026-08-08 | Wave 3 T-E2: Q13 decided — persistence import-vs-ownership ambiguity is a real, disclosed, deliberately-not-fixed-this-round limitation (regression risk to proven the reference Python app/the reference Java/JAX-RS banking platform detection outweighed the fix without a full re-verify). No code change; scope-limitations.yml + Open Questions Validation updated. |
| 2026-08-08 | Wave 3 T-E3: U-spring-data → partial (real the reference Java/JAX-RS banking platform ChargeRepository), U-jooq re-confirmed unbuilt (same Java Graphify import-symbol gap as org.postgresql, now generalized). Real bug found+fixed: persistence/messaging detector double-emit on files with both library types produced duplicate unique-ids — fixed via existingUnitFilePaths. DynamoDB verified via lab fixture. `CONTRACT_VERSION` 6.0.0→7.0.0 (`Evidence.source: 'extends'`). 40/40 tests green. |
| 2026-08-08 | Wave 3 T-E4: trap card T8 promoted — openapi-pass.ts now merges an openapi.yaml unit into an overlapping code-derived unit instead of spawning a competing node (real lab ts-nestjs-users fixture). C-contract → partial, expanded: securitySchemes now matched by real structural type/scheme, not author-chosen name — real http-bearer/http-basic/apiKey/oauth2 control rows. 41/41 tests green. |
| 2026-08-08 | Wave 3 T-E5: S-silence note extended — thin offline `hitl-review-trigger.js` CLI ships (review-flow-capability-map.md's own recommended decision), turning S1/S2 into a concrete per-unit review-queue.json. No LLM, no core-path import. 43/43 tests green. |
| 2026-08-08 | Wave 3 T-E6: k8s substring residual re-watched under harder conditions (T-E3's new Transaction/TransactionRepository database units) — 0 new false positives, no fix needed. Session E complete (T-E0 skipped, T-E1–T-E6 all done). |
| 2026-08-08 | Robustness Phase R0 (T-R0-1…T-R0-5) complete: Pilot Ready Scorecard, architecture-coverage rate metric, `multi-root-l2-protocol.md`, `OOS_Registry.md`, S0 Graphify-backbone flag all shipped. S-silence note extended (S0 + architectureOutboundCoverage). 46/46 tests green. |
| 2026-08-07 | Robustness T-R1-1: R2b design addendum (AREC R2b Implementer Store Hop) — real evidence re-checked against `fineract-provider`, honest prediction stated before code: mechanism will generalize but won't close the reference Java/JAX-RS banking platform-charge's own flagship residual. |
| 2026-08-07 | Robustness T-R1-2: R2b implemented (`multi-hop-bridge-detector.ts` implementer→store hop), new fixture `r2b-implementer-hop-sample` (positive + ambiguity). Real multi-root remeasure confirms the T-R1-1 prediction exactly: 11 new real architecture relationships resolved elsewhere in `fineract-provider`, the reference Java/JAX-RS banking platform-charge's own case stays an honest, named residual. R2 row → partial (T-C1 + R2b). the reference Java/JAX-RS banking platform-core T-A2 regression test updated (was asserting "never architecture" — now correctly distinguishes R0 direct-reconciler edges (still structural) from R2/R2b edges (architecture), since R2b now legitimately closes 3 real fineract-core cases too. 48/48 tests green. |
| 2026-08-08 | Robustness T-R1-3: Java Graphify import target normalization fixed (`java-import-resolver.ts` — source-line read-back, not a string-transform, since the qualified package is lost from Graphify's Java `imports` edges entirely). `org.postgresql`/`org.jooq` now real, verified catalogue rows (real the reference Java/JAX-RS banking platform + 229-unit real the reference Java governance platform evidence). **Follow-up evidence check closes the flagship the reference Java/JAX-RS banking platform R2 residual**: `org.springframework.jdbc.core` added, real multi-root re-run shows `ChargesApiResource → ChargeReadPlatformServiceImpl` as a real, `calm validate`-clean, confidence-10 relationship — R2 row updated to reflect the flagship case is L2 PASS, not just "mechanism partial." |
| 2026-08-08 | B-ontology (Q13 code-fix) + B-msg-prod-sqs closed, real evidence. U-persist-import: new `ownerBaseClass` catalogue field + `class-ownership-resolver.ts`; real finding falsified Q13's own proposed "require an inherits edge" fix before it was implemented (Graphify emits no inherits edge for an external base class); real the reference Node/NestJS wealth-management app re-run confirms `AccessService` correctly no longer database, `PrismaService` correctly still is. U-msg-producer: `messaging-pass.ts` now merges messaging evidence onto an already-typed unit instead of silently dropping it; real lab `ts-orders-dynamo` evidence confirms `OrdersDynamoStore` now carries both persistence and messaging evidence. 2 pre-existing tests fixed for a real, expected new CALM warning (a removed false-positive node orphaned a real one) — assertion corrected to the honest count, not silenced. |
