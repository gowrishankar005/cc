# Claim Register — what we may say is "built"

**Authority:** Prefer this file over marketing language in `Capabilities.md`/`CLAUDE.md` when they disagree on **relationship or control completeness**.

---

## How to use this register

| Status | Meaning | Allowed green |
|---|---|---|
| **proven** | Verified on real evidence + regression or documented run | L1 unit recall; L2 only if claim says so |
| **partial** | Some strategies work; not the full product claim | L0–L1; L2 only for proven sub-shapes |
| **specified-unbuilt** | Design/requirements named it; code incomplete or absent | Must not claim "works"; OOS or fail L2 |
| **unevidenced** | No solid sample yet | No product claim |
| **OOS** | Explicitly out of product scope for now | Silence OK only if scope-limitation id present |

**Rule:** Do not mark `Capabilities.md` "built" for a cell here without an evidence pointer + validation layer that is allowed to green.

---

## Dual-unit Graphify decision (product, not implementation accident)

| Decision | **Dual-unit structural edges are an interim / structural product (R0), not the full definition of "architecture relationship."** |
|---|---|
| Meaning | Graphify reconciler only emits edges when **both** endpoints are TypedUnits. That is correct for **code-level structural** edges. It is **insufficient** alone for enterprise **architecture stories** (e.g. layered HTTP resource API → platform service → entity). |
| Claim language | May say: "structural/code-graph relationships between discovered units." Must **not** say: "service→persistence architecture is recovered for Java enterprise monorepos" without R1/R2 strategies. |

---

## Mechanism-class matrix

| ID | Mechanism class | Status | Evidence | Validation allowed green | Notes |
|---|---|---|---|---|---|
| **U-http** | HTTP entry via **framework routes** (native route typing / composed JAX-RS / Nest / Spring-style annotations) | **proven** for that shape only | a reference Python microservices banking app (Flask), NestJS fixture, a reference Java/JAX-RS banking platform (charge/core modules), lab fixtures | L1 paths for framework-annotated / native-typed routes | **Does not** include AWS Lambda + API Gateway (paths often live only in CFN/SAM). Do not cite this cell as "HTTP complete for cloud-native architectures." See **U-http-serverless**. **NestJS GraphQL:** `@Resolver` is catalogue-corroborated bootstrap only (synthetic fixture `pipeline/test/fixtures/nestjs-graphql-sample`, `scope-limitations.yml` `nestjs-graphql-resolver-bootstrap-only`). `@Query`/`@Mutation` are deliberately not catalogued (same-language collision with TypeORM `@Query`). GraphQL operations are not a U-http-proven shape. |
| **U-http-serverless** | HTTP entry via **serverless / API Gateway** (Lambda `RequestHandler` / handler class + path from CFN/SAM/Serverless and/or OpenAPI join) | **partial → proven for the Java+explicit-CFN shape** | Real, full join now works end to end: `implements RequestHandler` → real `service` unit; handler owning `DynamoDbClient` directly stays `service`, not `database`; **CFN/SAM path join (`cfn-manifest-provider.ts`)** resolves the real cross-file `Resource`→`Method`→`Function.Handler` join and attaches real paths. Verified on a real AWS SaaS reference sample re-scanned with its own real CFN templates — `TierService` now a real `service` unit with **5/5 real paths exactly matching gold** (`GET/POST /tiers`, `GET/PUT/DELETE /tiers/{id}`), real `TierService → DynamoTierDataStore` architecture-grade edge present, exact match to gold. Lab fixture (`java-lambda-apigw`) L0/L1/L2 all PASS against hand-authored gold. | L1 unit-kind + path recall for the **Java, explicit-CFN-resource (non-SAM-shorthand)** shape — proven on both lab and the real reference sample | Real, honestly-named residuals: Node/Python handlers (no sample yet); SAM `Events:` shorthand (different shape, deferred); cross-stack/nested-stack CFN references (out of v1 scope); only 5/26 real CFN bindings found across the reference sample's shared `resources/` dir bound to THIS scan's roots — the other 21 are real routes for other services not in this scan, correctly left unresolved, not guessed; multi-root scan across services not attempted. The threat-signals module's S2 check now DOES extend to CFN-bound routes (real positive side effect — 'structured-file' source shares the 'http-entry-point' category). |
| **U-entity** | JPA `@Entity` → database unit | **proven** | a reference Java/JAX-RS banking platform (charge/core), lab java fixture | L1 database nodes | |
| **U-persist-import** | Driver/import persistence (SQLAlchemy, Prisma `ref_`, AWS SDK Dynamo clients, …) | **partial → proven for @prisma/client's extends-based ownership shape AND for the AWS SDK Dynamo clients' composition-based ownership shape (T-MR-4)** | a reference Python microservices banking app (SQLAlchemy); real re-run of a reference Node/NestJS wealth-management app: `AccessService` (imports Prisma types only) correctly ZERO database units; `PrismaService` (genuinely `extends PrismaClient`) correctly 1 real unit; **the TierService counterexample below is now closed**: `persistence-detection-catalogue.yml`'s new `ownerFieldType` (class-ownership-resolver.ts's `classDeclaresFieldOfType`) requires a real field of the client's own type, not just an import — re-verified against the real, checked-in `java-lambda-apigw` lab fixture (`TierStore`/`LegacyTierHandler`/`OrderStore`, all genuine field owners, still resolve `database`) plus two NEW synthetic fixtures isolating the negative shape (`pipeline/test/fixtures/dynamo-ownership-sample` for the Node v3 SDK, `dynamo-ownership-java-sample` for the Java v1 SDK — a second, different instance of the same mechanism, per Catalogue_Intake.md), disclosed as synthetic, not found-in-the-wild | L1 with low confidence ok; L1 ownership-precision now real for @prisma/client (extends) AND for `@aws-sdk/client-dynamodb`/`com.amazonaws.services.dynamodbv2`/`software.amazon.awssdk.services.dynamodb` (field-composition); every other driver-import library (SQLAlchemy, org.postgresql, org.jooq, Spring-JDBC, TypeORM, Mongoose, `aws-sdk` legacy v2, …) still has no ownership check and is unchanged | Prisma ownership fixed. Dynamo ownership fixed (T-MR-4, 2026-08-20) for the composition-ownership shape — see `scope-limitations.yml`'s `dynamo-field-ownership-heuristic` for the heuristic's own disclosed breadth (a locally-constructed client inside a method body still counts as ownership, not distinguished from a class field). Handler-vs-store kind tie-break (a handler that ALSO owns a Dynamo client directly still wins `service` over `database`) was a separate, already-closed mechanism (`existingServiceFilePaths`, real `java-lambda-apigw` test). |
| **U-msg-consumer** | `@KafkaListener` → network/topic | **partial** | a reference Java/JAX-RS banking platform listener + lab kafka fixture; gold maps to network | L1 network unit | Producer call-site still open |
| **U-msg-producer** | `KafkaTemplate.send` / SQS send | **partial, both mechanisms now dispatched** | Real evidence from a reference Java/JAX-RS banking platform's `KafkaExternalEventProducer.java` — `topic`/`network` node from its `KafkaTemplate`-typed field; real lab `ts-orders-dynamo` — `OrdersDynamoStore` (imports both `@aws-sdk/client-dynamodb` and `@aws-sdk/client-sqs`) now carries real merged messaging evidence, not silently dropped | L1 network unit for Kafka; L1 import-only evidence for SQS/SNS (low confidence, same tier as persistence's own driver-import) | Kafka: detects the field TYPE (architectural commitment), not a paired `.send()` call — see `scope-limitations.yml`'s `messaging-producer-field-type-only`. SQS/SNS: import-only strategy was already catalogued but `messaging-pass.ts` used to silently drop its evidence whenever the SAME class was already claimed by an earlier pass (persistence) — fixed: evidence merges onto the existing unit instead (kind still decided by whichever pass ran first, unchanged precedent). T-LR-3 follow-up: a bare framework-bootstrap stereotype + import-only messaging client now replace-not-duplicates (same mechanism as persistence), synthetic fixture `weak-service-messaging-sample`; KafkaTemplate is not this collision |
| **U-spring-data** | Spring Data repository interfaces (`extends JpaRepository`) | **partial** | Real evidence from a reference Java/JAX-RS banking platform's `ChargeRepository.java` — `extends` reference detection, plus a real knock-on `connects` relationship to `Charge` via R0 | L1 database unit | |
| **U-jooq** | jOOQ (`org.jooq.*` import) | **partial / proven on a reference Java governance platform** | Real evidence: a reference Java governance platform's `waltz-data` module — 229 `database` units via resolved `org.jooq.*` imports (`java-import-resolver.ts`) | L1 database units | Root cause was Graphify bare-symbol targets; fixed by source-line package resolve — not a separate jOOQ-only strategy |
| **U-outbound-http** | Outbound HTTP client → service→service relationship (Feign/WebClient/RestTemplate/requests/axios/OkHttp/HttpURLConnection/…) | **partial → proven for the `import-only-client` catalogued-library shape**: `outbound-http-detector.ts`/`outboundHttpPass` built and live in `DEFAULT_PASSES`; catalogue now covers `RestTemplate`/`requests`/`axios`/`httpx`/`node-fetch`/`WebClient`/`OkHttpClient`/`HttpURLConnection` | Real evidence: a reference Python microservices banking app's `frontend.py`, a reference Java/JAX-RS banking platform's `SendMessageToSmsGatewayTasklet.java` (both honest `unresolved-http-target` ignored-items — runtime-constructed URLs, no static literal target, never a fabricated `connects`); a reference Java/JAX-RS banking platform's real `ExternalCreditBureauIntegrationWritePlatformServiceImpl.java` uses `OkHttpClient`/`HttpURLConnection` — added both to the catalogue, re-scanned the real credit-bureau package root and confirmed 2 `unresolved-http-target` items emitted, `calm validate` 0 errors | L1 import-only evidence (low confidence, same tier as persistence's own driver-import) for cataloged libraries; still never resolves a target — every detection lands as an ignored-item for human review, by design | `@FeignClient`-style resolvable-target detection remains honestly `not-implemented` — no real sample found yet, separate residual from this cell's now-closed catalogue-completeness gap. |
| **R0** | Dual-unit Graphify structural edges | **proven** (as structural, now graded) | Core entity–entity; a reference Python microservices banking app service–db when one-hop | L2 **only** for shapes that match R0 | Not the reference Java/JAX-RS banking platform's own API→entity story. `TypedRelationship.grade`/`x-aac-relationship-grade` labels every relationship `structural`\|`architecture`\|`trust` — a reference Java/JAX-RS banking platform's core module's 64 entity–entity edges verified `structural`, a reference Python microservices banking app's service→db edge verified `architecture` |
| **R1** | One-hop architecture service→db (import/calls between units) | **partial, regression-locked** | a reference Python microservices banking app's Python service→AccountDb | L2 for that same app-class shape | Hard regression asserts a real service->database `connects` edge by endpoint KIND + `grade: architecture`, not just a relationship count — R2 work cannot silently regress this shape |
| **R2-mechanism** | Multi-hop / layered detection mechanism (Phase 1 short-circuit + implementer-import hop + T-LR-2's direct-delegate hop + T-LR-3's stereotype-disambiguation hop) | **partial/proven** | `multi-hop-bridge-detector.ts` + `java-import-resolver.ts` (the import-target root-cause fix both mechanisms depend on) + `spring-service-stereotype` (signal-catalogue.yml, T-LR-3's bare `@Service` row); synthetic fixtures `r2-bridge-sample` (Phase 1), `r2b-implementer-hop-sample` (second-hop + 2-implementer ambiguity refusal, standing exams **E-r2-ambiguity**/**E-r2b-positive**), `r2c-direct-delegate-sample` (T-LR-2, direct-delegate + ambiguity refusal), `stereotype-disambiguation-sample` (T-LR-3, stereotype-disambiguated resolution + both-stereotyped ambiguity refusal); real evidence from a reference Java/JAX-RS banking platform's security module (R2b/R2c) and Entry 13 of its own architect-pilot notes (T-LR-3's underlying gap, real `@Service`-only class); real evidence from a reference Java governance platform's `waltz-data` module (229 real jOOQ units via the same import-resolution fix), and its `waltz-web`+`waltz-service`+`waltz-data` 3-module scan (T-LR-2: 7 real direct-delegate chains, 0 fabricated) | L1/L2 for the specific synthetic + real shapes cited above, regression-locked | The mechanism itself (never fabricates, honestly refuses 0/2+ candidate cases) is real and tested. **This cell alone does not establish what any specific package will resolve to** — see the two claim-triple-specific cells below for the reference Java/JAX-RS banking platform's charge module, which is where the "mechanism proven" vs "flagship closed" conflation actually happened. Still inapplicable for: Python-shaped bridges (no structural `implements` edge), command-bus/dynamic-dispatch write paths (permanent non-goal). **T-LR-2 (2026-08-13) closed the previously-named gap** — a bridge candidate with no `implements` edge at all (a concrete class referenced directly, common real Spring idiom without an interface layer): a real probe against a full clone of a reference Java governance platform originally found 28 such candidates, 0 fabricated, 0 resolved; re-run after T-LR-2 shipped resolves 7 of them (real direct-delegate chains, e.g. `SettingsEndpoint -> SettingsDao`, no interface), the remainder correctly still refused (0 or 2+ candidate stores at the delegate itself — same "never guess" discipline, not a partial implementation). **T-LR-3 (2026-08-15) closes the "plain-interface bridge" gap for the specific 2+-implementer/stereotype-disambiguation shape**: a bridge with 2+ real `implements` candidates now resolves when exactly one carries a real `@Service` stereotype and is itself a terminal database/topic unit — mechanism-class fix (a catalogue row plus the existing decorator-extraction mechanism, matching the `Catalogue_Intake.md` bar), but its real-world evidence base is Fineract-only (Entry 13 + E1b, both already-documented findings, not independently re-verified against real source in this session) and its regression lock is a synthetic fixture only — **no live second-real-repo instance could be run**: neither `spikes/fineract/repo` nor `spikes/waltz/repo` was present in the environment this was built in, a real, disclosed gap in the second-instance-verification bar T-LR-1/T-LR-2 both cleared, not a silent skip (see `scope-limitations.yml`'s `stereotype-disambiguation-narrow`). `multi-hop-bridge-detector.ts` edges carry a `mechanism` field (`r2-phase1`/`r2b`/`r2c`/`r2-stereotype`) distinguishing which branch produced them, without decoding confidence values. **T-LR-4 (Bean-factory / stereotype-free wiring) is closed by T-LR-5's `bean-factory` branch** — see this file's `T-LR-5-codeql-di` row. Graphify/CodeGraph still cannot see a `@Bean` method body's `new X(...)` instantiation; that remains a genuine mechanism gap for those two engines, now filled by opt-in CodeQL DI rather than a T-LR-3-style catalogue-row fix. A domain-store refine step (second-hop) was decided **not built** — no live case resolves via that path for the flagship shape, reasoned + revisitable. **2026-08-16 real-data update, found fixing a T-LR-3 regression:** the Waltz 3-module scan's real r2c count moved from 7 to 0 — not a loss of signal, verified via direct investigation before this note was written. The bare-`@Service` catalogue row (T-LR-3) gives classes like `SettingsService` their own first-class `service` unit for the first time, which removes them from bridge-candidacy entirely (the `nodeToUnit.has(bridgeNodeId)` gate); the same real chain (e.g. `SettingsEndpoint -> SettingsService -> SettingsDao`) now resolves as two real, direct, architecture-grade R1 edges instead of one weak, uncorroborated r2c edge — strictly stronger evidence for the identical real connectivity, confirmed against real source. This also surfaced and fixed a real T-LR-3 regression (`overridableServiceFilePaths` in `pass-registry.ts`): a bare-`@Service` class that ALSO genuinely imports a persistence driver was being silently kept `service`-kind instead of `database`, dropping real persistence signal — see `BACKLOG.md`'s remaining residual rows for the gaps this surfaced (exception-shaped bridge noise, implementer-list collision — both unreproduced/disclosed, not fixed speculatively). Messaging-pass parity with persistence (including sub-floor stereotype merge) closed 2026-08-19. |
| **R2-gold-charge-single** | a reference Java/JAX-RS banking platform's charge module, single-root, scored against single-root hand gold (`coe-lab/gold/calm/fineract-charge/`) | **expected-fail, by design — not a detector bug; verified via the frozen protocol** | Real `run-slice` on the charge module alone → generated output → scored: `L0 schema PASS`, `L1 unit recall PASS`, `L2 story FAIL` (`missing connects topology service->database`) — the standing-exam protocol, not just a mechanism-level regression test | L0 PASS, L1 PASS, **L2 FAIL is the correct, expected outcome, now automated** — see standing exam **E-charge-single-L2** | The resource class never imports the entity class directly; there is no static one-hop chain in the real source for this shape. This is a real fact about that platform's own layering, not a gap to be closed by loosening the gold or silently rescoring against a different root set. **Do not** cite R2-multi-root-access-terminal's PASS as evidence this cell passes — different claim triple entirely. |
| **R2-multi-root-access-terminal** | a reference Java/JAX-RS banking platform's charge+provider modules, multi-root, service→access-layer-store terminal | **partial/proven — gold-scored, not only mechanism-level** | Real combined multi-root `run-slice` scan → scored against a hand-authored gold (authored from real source, never from generator output): `L0 schema PASS` (both), `L1 unit recall PASS`, `L2 story PASS`, exit code 0; standing exam **E-charge-multi-story** | **L2 PASS against a real hand-authored multi-root gold** — a root-set/scan-mode guard confirms the scored generated file genuinely has 2 package roots (`x-aac-package-roots`) before scoring, so this isn't a mismatched artefact | **Terminal is the access-layer implementation class, an access-layer JDBC-backed store unit — NOT the same node as R2-gold-charge-single's expected entity endpoint.** Claim triple: `{rootSet: charge+provider modules, terminalGrain: access-layer, evalArtefact: E-charge-multi-story}`. Stating this as "flagship case closed" without naming the terminal grain and root set is exactly the forbidden phrasing below. |
| **R-k8s** | shares-secret / env soft-graph | **partial** | a reference Python microservices banking app's k8s manifests; deployment correlation post-MVP | L2 when manifests passed | Re-ran the full real 6-service scenario under harder conditions than the original fix was verified against — confirmed 0 unresolved k8s deployments, 0 substring leaks into any k8s/env-derived relationship endpoint. Service-kind-only guard holds. **Deployment correlation's own "post-MVP" gap is now closed for the runtime-placement shape** — see `T-MR-3-k8s-deployed-in` below; this row stays scoped to trust/env-soft-graph, not restated |
| **U-floor-consistency** | Analysis-stage unit-set consistency — relationship producers (`unitsByRoot` → `buildNodeToUnitMap`) and grading/emission (`allUnits`) share one confidence-floor set | **proven** | Synthetic standing exam (`mapSignalsPass: unitsByRoot and allUnits share the confidence floor`); real second instance: a reference Java governance platform's `JWTAuthenticationFilter` (Waltz, T-LR-2 scan) no longer anchors a relationship | L1 (consistency; no new L2 architecture-story claim) | Mechanism class: same run, one definition of "a real unit." Sub-floor units stay `INSUFFICIENT_EVIDENCE` IgnoredItems. Not a new detection strategy. |
| **E2-admitted-unresolved** | Graded fact admission (T-P0-1, `docs/solution/E2-graded-fact-admission-experiment.md`) — a Graphify edge whose one endpoint doesn't resolve to a real TypedUnit is admitted with a synthesized `kind: 'unresolved'` placeholder on that side, always graded `structural`, never `architecture` | **partial, shipped 2026-08-14** | Real recall recovered on `fineract-core`/`fineract-charge` (371/9 additional low-confidence facts) and on narrow `ghostfolio` scans (real NestJS `@Module` wiring edges closing previously-orphaned nodes); 3 real architectural conflicts found and fixed against real `spikes/` repos, not synthetic cases — full regression suite (93/93) is the standing exam | L1 (structural-only, never counted toward any L2 architecture-story claim) | Deliberately below every other mechanism's confidence floor (2/3). Defers to any more specialized detector's own examined-edge/examined-file set (today: `multi-hop-bridge-detector.ts`'s `examinedPairs`/`examinedBridgeFiles`) rather than racing it — a future detector needing the same deferral opts in the same way. Never promotes an admitted fact to `architecture` grade even when the resolved endpoint is a service unit. |
| **C-dec** | Decorator controls (`@PreAuthorize`) | **proven** | DatatableWriteService | L1 controls presence | Also carries `expression` when the decorator has one |
| **C-call** | Call-site auth (`validateHasReadPermission`, `jwt.decode`, `hasRole`, `isAuthenticated`) | **partial** | Real evidence from a reference Java/JAX-RS banking platform's `ChargesApiResource.java` (lines 84/101/129) and `AppUser.java`, real lab `py-jwt-gateway` (line 14); real evidence from a reference Java governance platform's `waltz-web` module (4 real call sites across 4 endpoint files), real evidence from a reference Java/JAX-RS banking platform's `PlatformSecurityContext.isAuthenticated()` | L1 controls presence for 4 named vocabularies now (Java `validateHas*Permission`/`hasRole`/`isAuthenticated`, Python `jwt.decode`) | 4 vocabularies, still not a general call-graph auth inference; see `scope-limitations.yml`'s `call-site-controls-partial` |
| **C-contract** | OpenAPI securitySchemes | **partial, expanded** | Real lab `ts-nestjs-users` (`bearerAuth: {type: http, scheme: bearer}`) — matched against the scheme's real structural `type`/`scheme` fields (never the author-chosen name), attaches a real `security-contract-http-bearer-001` control | L1 controls presence for http-bearer/http-basic/apiKey/oauth2 | `openIdConnect` and richer OAuth2-flow detail not modeled |
| **C-rich** | Control config richness (expression, authority) | **partial** | Real `expression` field on a reference Java/JAX-RS banking platform's `security-rbac-002` control and lab jwt-gateway's `security-auth-001` (shows the real `verify_signature: False` anti-pattern in source); real `authorityRef` structured field on both a reference Java/JAX-RS banking platform and a reference Java governance platform, correctly ABSENT on the one real negative case (a plain-variable call site) | L1 requirement not yet raised to require it | Raw source-line TEXT only, never a resolved constant value; `authorityRef` is a naming-convention token match (ALL_CAPS/qualified-enum shape), not semantic authority modeling — a differently-cased or non-conventional constant name would not match |
| **S-silence** | Completeness / empty neighborhood loudness | **partial** | a reference Python microservices banking app: S1 does not fire (R1 present); multi-root a reference Java/JAX-RS banking platform's charge+provider modules: high arch-cov after the import-target fix; single-root residual modules may still S1; a real reference AWS SaaS sample's tier/tenant services: S5 fires correctly (real dedicated fixture + real repo negative-path check) | L3 for S0/S1/S2/S5; S3 by design; S4 scope-limitations | S5 flag closes the class of degenerate case S1 structurally cannot catch: 0 service units with a real store present, or real CFN routes found but unbound (S1 needs ≥1 service unit to even fire) |
| **T-FS-6-status-vocabulary** | Status vocabulary (BACKLOG.md-adjacent, `AGENT_TASKS_Ext_Fact_Semantics.md`'s own T-FS-6, BR-40) — `TypedUnit.status`/`TypedRelationship.status: FactStatus` (`typed-facts.ts`), fixed 5-value vocabulary (`observed`/`inferred`/`requires-review`/`reviewed`/`externally-verified`) matching this extension's own prior research doc (`research/codeintel/.../02-solution-architecture.md` §9), assigned by `status-assignment.ts`'s `assignStatuses()` — the new true-last Analysis pass — purely from signals every run already computes (unit kind, confidence band via the existing `bandFor()`, evidence source, relationship mechanism/source, contradiction ignored-items) — no new extraction mechanism. Surfaced as `x-aac-status` CALM node/relationship metadata (`metadata-builder.ts`/`relationship-builder.ts`) | **partial, proven on synthetic + 3 real fixtures** | Direct `assignStatuses()` unit test covering every status value in one place (synthetic units/relationships, `pipeline/test/regression.test.js`); real end-to-end wiring proven on 3 checked-in fixtures: `stereotype-disambiguation-sample` (medium-band confidence → `inferred`, high-band → `observed`, r2-stereotype mechanism → `inferred`, verified through to CALM `x-aac-status`), `spring-config-sample` + `contradiction-manifests/conflicting` (a real contradiction-flagged unit → `requires-review`, overriding its own high confidence band), lab `ts-nestjs-users` (real `openapi`-sourced evidence → `externally-verified`) | L1 (real, per-fact status), synthetic + real-fixture verified; never claims `reviewed` is reachable except via a real applied Override — a dedicated `override-applier.ts` test confirms a successfully applied override both promotes status to `reviewed` AND replaces (never duplicates) the prior analysis-time `x-aac-status` entry | **Hard rule, tested**: this pipeline has no distinct "external-system" unit kind — its closest analog is `kind: 'unresolved'` (a real call site whose counterpart couldn't be classified, carries zero `Evidence` by construction, per `graphify-reconciler.ts`'s own admitted-placeholder code) — asserted to always read `requires-review`, never `observed`/`externally-verified`, regardless of its (low, fixed) confidence value. `externally-verified` is reachable for units only via `openapi`-sourced evidence and for relationships only via `k8s`-sourced (`shares-secret`) edges today — a real k8s-manifest-corroborated UNIT (not just a shares-secret relationship) has no evidenced trigger yet, disclosed rather than fabricated. `reviewed` is written ONLY by `override-applier.ts`, never by any Analysis pass — this is the mechanism enforcing "Analysis concludes, Override corrects," not just a convention. IR (`analysis/ir/`) does not yet surface `status`, same as `grade`/`mechanism` before it — consistent with existing precedent, not a new gap this task introduced. |
| **T-FS-3-contradiction-detection** | Contradiction detection between evidence sources (BACKLOG.md "Contradiction detection between evidence sources") — `contradiction-detector.ts` compares a k8s Deployment manifest's container image (opt-in, `--k8s-manifests`) against a spring-config-sourced database unit's `spring.datasource.url`/`spring.datasource.hikari.jdbcUrl` JDBC scheme; when they name DIFFERENT datastore engines for what should be one real fact, it writes a distinguishable `contradiction:` ignored-item (never touches the unit's own confidence — no averaging, no silent pick) that `hitl-review-trigger.ts` turns into an always-on Tier A review-queue trigger (`contradicting-evidence-force-review`, `tools/review-session/triage.py`'s `contradicting-evidence` class) | **partial, real-instance-verified** | Real evidence: `apache/fineract`'s own checked-in `kubernetes/fineractmysql-deployment.yml` (image `mariadb:12.2`) genuinely conflicts with `fineract-provider/src/main/resources/application.properties`'s own default (`spring.datasource.hikari.jdbcUrl` default `jdbc:postgresql://...`) — a real, unforced instance, gated regression test in `pipeline/test/regression.test.js` ("T-FS-3 real-instance verification"), the unit's own confidence asserted byte-identical before/after. Finding this real instance surfaced and fixed two real gaps the synthetic-fixture-only pass had missed (the Hikari key form; a colon-default placeholder wrapping the literal URL) — see `docs/solution/language/spring-config-property-vocabulary.md`. A SECOND independent code review (2026-08-16) then found and fixed 6 further real bugs, verified with dedicated tests: (1) the "never guess" gate was computed globally across the whole k8s manifest set instead of per-unit — a co-present, unrelated deployment (e.g. Redis alongside Postgres) silently disabled contradiction checking for the ENTIRE run; fixed to scope per-unit. (2) `imageEngine()` broke on a private-registry-with-port image and a real official multi-segment image where the engine isn't the last path segment (`mcr.microsoft.com/mssql/server:...`) — both fixed and directly tested. (3)/(6) `port-interface-builder.ts`'s own JDBC-signal check had already drifted out of sync with the placeholder-unwrap fix in the SAME commit — the 3 independent copies were consolidated into one shared function (`pipeline/src/analysis/jdbc-url.ts`). (4) `extractDatasource()` used key-presence instead of key-value-truthiness when choosing between the two property keys. (5) the placeholder-unwrap regex corrupted a NESTED `${FOO:${BAR:jdbc:...}}` shape by appending a stray trailing brace — fixed to detect and reject nesting explicitly rather than risk a corrupted `Evidence.argument`. Synthetic fixtures (`spring-config-sample`, `contradiction-manifests/{conflicting,agreeing,ambiguous}`) retained as the fast/deterministic regression layer alongside the real-instance test | L1 (a real, actionable Tier A review-queue item), verified on both synthetic and real (`apache/fineract`) data; never L2, never a confidence-score claim — this mechanism explicitly never blends, averages, or silently resolves the two sources | Claim triple: `{rootSet: apache/fineract (fineract-provider + kubernetes/), terminalGrain: k8s-Deployment-image vs spring-config-JDBC-scheme pairing, evalArtefact: this fixture's own + the real-instance regression test's assertions, not a standing exam}`. Scope is Java/Spring-config-only — see `scope-limitations.yml`'s `contradiction-detection-java-spring-only`; does not extend to Python/Node config sources or non-datastore fact classes. E5 (`E5-confidence-replay-experiment.md`) already found averaging correlated evidence makes decisions LESS correct — this mechanism's "never average, always force review" design is the same lesson applied to genuine disagreement rather than corroboration. Mechanism class named per CLAUDE.md's "bug fixes are capability work": the 6 review-found bugs are all correlation/parsing-mechanism gaps (per-unit scoping, Docker image-reference parsing, placeholder-syntax resolution) fixed at the mechanism level, not instance patches — each carries its own dedicated regression test independent of the case that revealed it. |
| **T-LR-5-codeql-di** | CodeQL DI-resolution engine (`AGENT_TASKS_Ext_CodeQL_Engine.md`) — `scanner/codeql-di-provider.ts` builds a real CodeQL Java database via a caller-supplied build command and runs `pipeline/src/rules/codeql-queries/di-resolution/di_resolution.ql` (Session C's own E1b query, unmodified — generic, `stereotype`/`bean-factory` mechanisms, real ambiguity refusal; relocated here from `soln/`, found the hard way: `soln/` is entirely gitignored, so the query file this mechanism depends on would never have shipped in a fresh clone — degrading gracefully but uselessly for every real user, not caught until checking the repo's own `.gitignore`). `analysis/codeql-di-pass.ts` (opt-in, `--codeql-source-root`/`--codeql-build-command`) turns real DI bindings into relationships, introducing a placeholder unit (T-FS-4-class, `codeql-di` evidence source) when the resolved implementation has no unit of its own yet. Architecturally shaped like Graphify's own combined pass, not a per-file `StructuralEngine` — the task's own "second StructuralEngine" framing was a mechanism-shape mismatch found building this, not the actual design | **partial, proven on one real repo; second/third-instance verification done — mechanism generalizes safely, yield does not** | Real, live end-to-end run against `spikes/fineract/repo` (fineract-charge + fineract-provider), 2026-08-19: 2105 real DI bindings parsed, 731 new relationships, 56 new units introduced, `calm validate` 0 errors/0 warnings. Flagship chain (`ChargesApiResource -> ChargeReadPlatformService -> ChargeReadPlatformServiceImpl`) resolves via `bean-factory`; a second, genuinely-new-signal case verified directly (`LoanChargesApiResource -> LoanChargeReadPlatformServiceImpl`, real `@Bean`-factory wiring, no stereotype at all — this IS T-LR-4's own acceptance criterion, closed by this work). A real bug was found and fixed live-testing this (not assumed safe): CodeQL's `RefType.getName()` returns `""` for an anonymous `new SomeInterface() { ... }` implementation — `codeqlDiPass` now skips any binding with an empty `resolvedImpl`/`injectingClass` rather than emit a CALM-schema-invalid empty-name node (`calm validate`'s `architecture-has-no-empty-string-properties` rule caught it). 6 regression tests: binary-absent degradation, real-CSV-shape parsing (real rows copied from the live run), trust-tier (never contests an existing edge, never crosses an unscanned root), opt-in-only no-op, the empty-name fix, and T-FS-6 hard-rule interaction (an introduced unit + its relationship both read `requires-review`) | L1 (real relationships + introduced units), verified end-to-end through `calm validate`; never L2 — this mechanism only fills gaps neither CodeGraph nor Graphify reach, it does not attempt a full architecture story on its own | **Second-real-repo-instance verification (checklist item 6): Waltz attempt could NOT be completed** — disclosed, not silently skipped: Waltz (`spikes/waltz/repo`) uses Maven (installed for this attempt, `brew install maven`); its POM reactor fails to resolve for every module, including a `waltz-common`-only build via `-pl` (Maven still parses the whole reactor), with real, pre-existing `${jooq.group}`/`${jdbc.group}` property-resolution errors in `waltz-schema`'s POM — consistent with Waltz's own jOOQ Pro (commercial) dependency needing license-gated Maven profile/settings this environment doesn't have. Two independent attempts (full network-enabled reactor build, and the scoped `waltz-common` build) hit the identical failure; not a defect in this mechanism's own code. **Second/third-real-repo-instance verification actually completed 2026-08-21 against `spring-projects/spring-petclinic` and `finos/spring-bot`** (`E2-java-framework-generalization-experiment.md`) — real, different Spring codebases, unmodified `di_resolution.ql`, mechanism ran mechanically clean on both (no false positives, correct built-in ambiguity/empty-name refusal) but produced **zero usable relationships on either**, traced to real cause (not assumed): both repos have far less of the specific "ambiguous interface resolved via `@Bean`-factory/stereotype" architecture shape Fineract has in abundance, and `spring-bot`'s one otherwise-valid binding was correctly refused because its injecting class carries no unit at all in this pipeline's own extraction. **Conclusion: the mechanism's safety generalizes; its yield is gated by a codebase's own architecture shape, not by framework identity** — this refines rather than weakens the original Fineract-only proof, and means `fact-trust-matrix.ts`'s confidence tier should not yet be read as validated across "Java/Spring" broadly, only as unrefuted by these two additional data points. CI/Docker (checklist item 3): this pipeline's own CI never runs `--codeql-source-root` — the free CodeQL CLI license permits automated/CI use only against an Open Source Codebase or under GHAS, and this repo has no `LICENSE` file (checked). Trust-tier PLACEMENT is done (confidence 7/4, between R2b's 8 and R2c's 6, structurally never overrides an existing edge); the full evidence-earned-per-fact-type trust MATRIX (T-LR-6) shipped 2026-08-20 — see the `T-LR-6-fact-trust-matrix` row below. `R2-gold-charge-single`'s standing expected-fail (`E-charge-single-L2`) was not re-run with `--codeql-di` enabled — unknown whether this flips it, named explicitly as unverified rather than assumed unaffected. |
| **T-LR-6-fact-trust-matrix** | Per-(engine, fact-type) trust matrix (`AGENT_TASKS_Ext_CodeQL_Engine.md`, `BACKLOG.md` "Per-(engine, fact-type) trust tiers") — `pipeline/src/analysis/fact-trust-matrix.ts` centralizes the confidence tier for every (engine, mechanism, scope) triple this pipeline emits for a gap-filling/inferred fact: Graphify's multi-hop family (`r2-phase1`/`r2-stereotype`/`r2b`/`r2c`/`admitted-unresolved`, same-root and cross-root) and CodeQL's DI-resolution mechanisms (`codeql-di-stereotype`/`codeql-di-bean-factory` relationship-edges, plus `codeql-di` unit-introduction), each row carrying a real evidence citation rather than an assumed number. `multi-hop-bridge-detector.ts`, `graphify-reconciler.ts`, and `codeql-di-pass.ts` were refactored to read from this matrix instead of each independently defining `_CONFIDENCE` constants compared only through prose cross-references ("below R2b's tier") — a pure, behavior-preserving refactor (`npm test` produced the identical 96/96 pass, 0 fail before and after). "CodeQL is never automatically primary" (the task's own acceptance wording) is enforced two ways, not just documented: `codegraph`'s own primary-tier R0/R1 relationships never carry an explicit `confidence` at all (per `relationship-builder.ts`'s pre-existing "absence, not a fake 0" convention) and so structurally never enter this matrix in the first place; and `assertCodeqlNeverPrimary()` asserts no `engine: 'codeql'` row ever reaches or exceeds the strongest tier any OTHER engine holds in the matrix | **proven** | 2 new regression tests in `pipeline/test/regression.test.js`: one asserts every currently-consumed (engine, mechanism, scope) triple returns its known, previously-hardcoded value and that an unregistered triple throws rather than silently defaulting; the other calls the real `assertCodeqlNeverPrimary()` against both the real, shipped matrix (must not throw) and a deliberately-rigged matrix with an inflated `codeql` row (must throw) — the guard is proven to fail closed, not merely proven not to fire today. `npm test` 96/96 | L1 (a mechanism-shape/data-integrity guarantee about existing facts' confidence values, not a new fact-producing mechanism); no new detection, no new L2 claim | Deliberately scoped to the fact types currently in production (Graphify multi-hop family + CodeQL DI) — does **not** extend `scanner/engine-capability-matrix.yml`'s own (language, framework) routing config to read from this matrix; that routing axis stays load-and-log/config-declared, a separate, larger open item (see `BACKLOG.md`'s own row). A future third engine or fact type registers a new matrix row with a real evidence citation, per this file's own `lookup()` — it does not get a confidence value without one (throws otherwise). |
| **T-FS-4-secondary-source-introduction** | Secondary sources may introduce facts, not only corroborate (BACKLOG.md "Secondary sources may introduce facts, not only corroborate") — `cdxgen-corroboration-pass.ts`: when a real, unambiguous cdxgen SBOM dependency match exists but ZERO code-derived persistence/messaging unit exists in the root to corroborate, introduces a new unit at the corroboration weight (10) instead of staying mute (the prior behavior: a real, named `AMBIGUOUS_BOUNDARY` ignored-item and nothing else — a real signal the pipeline could see and chose to discard). Synthetic id/filePath (`cdxgen:<root>:<name>` / `dependency-manifest:<name>`) — no file:line exists for a manifest-only fact, same honest convention `spring-config-pass.ts`'s synthetic config-derived units already established. Still never guesses: 2+ real matches with zero candidate units stays a named ignored item | **partial, proven** | `pipeline/test/fixtures/cdxgen-sample` (real, checked-in `requirements.txt` naming `psycopg2`, no code import of it) — zero-candidate + exactly-one-match case now introduces a real unit (confidence 10, `dependency-manifest`-sourced) instead of an ignored-item, `pipeline/test/regression.test.js`'s updated T-CDX-3 test. A separate, dedicated test (mocked `discoverCdxgenComponents`, 2 real matches, 0 candidates) confirms the ambiguous case still refuses rather than guessing | L1 (a real, distinctly-tagged unit); never L2 — "at its own tier without being promoted" is enforced by `status-assignment.ts`'s own hard rule (T-FS-6): a unit whose entire evidence is `dependency-manifest`-sourced always reads `requires-review`, regardless of its own confidence value, tested directly | Only reachable for the `driver-import`/`import-only-cloud-client` catalogue strategies (the same libraries `cdxgenCorroborationPass` already corroborated against pre-T-FS-4) — a secondary source outside those two strategies (e.g. a future non-persistence/messaging catalogue) has no introduction path yet, not silently assumed to have one. |
| **T-FS-1-tier-b-residual** | Tier-B residual class (BACKLOG.md "Tier-B residual detection") — `multi-hop-bridge-detector.ts` distinguishes "exactly ONE real database/topic candidate among a bridge interface's several syntactic implementers" (`tier-b-single-candidate` ignored-item) from genuine multi-candidate ambiguity (2+ real store candidates, unchanged `unresolved-multi-hop` refusal). `hitl-review-trigger.ts` surfaces the former as a new, always-on `multi-hop-single-candidate-below-threshold` review-queue trigger; `tools/review-session/triage.py` maps it to Tier B (`single-candidate-below-threshold`) — the first real trigger to reach that tier; `cards.py` offers a real accept/reject choice card, never re-deriving or inventing the candidate id | **partial, synthetic-fixture-evidenced; real-instance search run, genuinely negative** | `pipeline/test/fixtures/r2-tier-b-candidate-sample` (synthetic, explicitly labeled as such) — WidgetApiResource's bridge has 2 syntactic implementers, exactly 1 a real store, correctly routed to the new Tier-B path; SprocketApiResource's bridge has 2 syntactic implementers, BOTH real stores, correctly stays on the original Tier-A refusal path (proves the mechanism discriminates rather than over-firing) — same fixture, both cases regression-asserted end-to-end through CALM output, `typed-facts.json`, `review-queue.json`, and `tools/review-session`'s `triage.py`/`cards.py` (`pipeline/test/regression.test.js`, `tools/review-session/test_triage.py`, `test_cards.py`). **Real-instance search (2026-08-16, `spikes/` confirmed present and scanned)**: `spikes/waltz/repo` (3-module web+service+data scan), `spikes/fineract/repo` (fineract-charge+provider), `spikes/ghostfolio/repo/apps/api`, and `spikes/boa/repo/src/ledger` (3 Java services) all scanned for real `implementers.length >= 2` bridges — 0 real Tier-B instances found in any of the four. One real, informative NEGATIVE result found instead: Waltz's `Endpoint` marker interface has 126 real syntactic implementers, 0 of which are database/topic units — correctly stays on the generic refusal path, not misclassified as Tier-B, confirming the mechanism doesn't over-fire on a large, real, unrelated multi-implementer interface. Stated as a genuine "checked for real, not found" result per the coordinator's own request, not a silently-skipped search | L1 (a real, actionable review-queue item + Tier B card), synthetic-verified; real-repo NEGATIVE-path verified (correctly does not fire on Waltz's real 126-implementer case) — never L2, no relationship is ever auto-emitted by this mechanism, same "never guess" discipline as every other R2 branch | Claim triple: `{rootSet: synthetic fixture (positive case); spikes/waltz+fineract+ghostfolio+boa (negative-path real verification), terminalGrain: bridge-implementer count (R2 Phase 1 top level only), evalArtefact: this fixture's own regression assertions, not a standing exam}`. **No real positive (`spikes/`) repo instance found** after checking all 4 available real repos — the shape (2+ syntactic implementers, exactly 1 a real store) genuinely doesn't occur in the sampled real code, not an unchecked gap. Scope is narrower than "multi-hop ambiguity resolved": does not extend to R2b's second-hop store-import ambiguity or R2c's direct-delegate ambiguity, both unchanged (`scope-limitations.yml`'s `tier-b-single-candidate-scope`). Never auto-promotes an external-system/unresolved node — this is purely a review-queue signal. |
| **T-RT-1-bulk-residual-authoring** | Bulk residual-decision authoring (`AGENT_TASKS_Ext_Review_Throughput.md`'s T-RT-1, BACKLOG.md "Bulk residual-decision authoring") — `tools/review-session/bulk_apply.py`: the architect answers ONE residual's choice card the normal, individual way (its Decision Record is the "anchor"), then this script replicates that same answer across every OTHER open residual in the anchor's own (tier, class) group (`cards.py`'s own `group_by_class` — the exact grouping already shown on every card as "Similar residuals this session"). §2.1's own "bulk-apply integrity" rule (found on review there) is enforced mechanically, not just by convention: one real Decision Record file per residual (never a blanket batch record), each with its OWN id/evidence/target_ref (a target residual's own real evidence, never the anchor's file:line — S5), every affected unit id printed before any file is written, and an explicit confirmation required (interactive prompt or `--i-confirm-bulk-apply`) before the batch commits. Only replicates an anchor whose answer doesn't itself name a specific other unit (`type_change` override, or a no-override "leave open"/"accepted" decision) — `relationship_add`/`node_add`/`node_remove`/`node_rename`/`boundary_change` anchors are refused outright, not guessed at, since blindly recopying a specific target/endpoint onto a different residual's own unit is exactly the "one wrong first answer silently propagates" failure the design names | **partial, synthetic-fixture-evidenced** | `tools/review-session/test_bulk_apply.py` (19 tests): anchor located by target_ref match (never by parsing rationale prose), sibling grouping matches `cards.py`'s own, ambiguous/ambiguous-unit-count targets skipped and reported (never silently dropped), decision/override id collisions refused (never overwritten), non-replicable override types refused for the whole batch up front, real subprocess end-to-end run against a synthetic 4-residual pack (2 siblings, 1 different class, 1 already carried_forward) writes exactly 2 Decision Records + 2 Overrides, each with its own target's real evidence — never the anchor's | L1 (drafts only — never a legal CALM write path on its own; `apply.py` remains the only chokepoint that ever calls `run-slice`/`override-applier.ts`, unchanged by this task) | Not yet run against a real multi-residual scan (`spikes/`) — the checked-in fixtures never produce 2+ same-class open residuals in one run today, so this is verified against synthetic Session Packs shaped like a real one (matching the same schema `pack.py` writes), not (yet) a real end-to-end scan with real siblings. Named, not hidden. |
| **T-RT-2-consequence-ranked-queue** | Consequence-ranked queue (`AGENT_TASKS_Ext_Review_Throughput.md`'s T-RT-2, BACKLOG.md "Consequence-ranked queue") — `tools/review-session/consequence.py` computes a real, deterministic `consequence` field (`signals` + `score`) per residual from facts this pipeline already produces (`TypedUnit.kind`, `TypedRelationship.kind`) — never a new detection mechanism. `pack.py` attaches it to every residual in `residuals.json` (additive field, `residuals-schema.json` updated); `queue_rank.py` sorts the OPEN backlog highest-consequence-first, reports backlog size, and (given `--history` of prior Session Packs) real residual age via the same `(trigger, unitIds)` signature `triage.py`'s own `apply_baseline()` already uses for carry-forward matching | **partial, proven on synthetic + 1 real fixture** | `tools/review-session/test_consequence.py` (11 tests) + `test_queue_rank.py` (12 tests): each signal exercised independently and in combination, deterministic (same input → same output), a `calls`/`imports` relationship never mistaken for `shares-secret`, ranking excludes `carried_forward`/`applied` (already-decided, not backlog), ties broken by id, missing `consequence` (an older pack) sorts as score 0 rather than crashing, age lookup finds the OLDEST matching history pack and reports "unknown" (never a fabricated zero) when no `--history` given or no signature match found. Real end-to-end run against `pipeline/test/fixtures/nestjs-sample`: `pack.py` writes a real `S2-http-without-security-control` residual with `{"signals": ["external-system-identity"], "score": 1}`, `queue_rank.py` ranks and reports it correctly | L1 (a real, computed ranking signal over already-produced facts); never claims a real PII/data-classification result — see `consequence.py`'s own module docstring | **Hard, disclosed limit**: this pipeline has no real PII/data-classification detector and no distinct "external-system" unit kind. `pii-proxy` is `TypedUnit.kind == 'database'` (the closest real proxy this pipeline can name, not a data-classification result); `external-system-identity` is `TypedUnit.kind == 'unresolved'` (this project's own documented closest analog, per `T-FS-6-status-vocabulary`'s row above) OR the `security-authority-policy` class; `trust-boundary-edge` is `TypedRelationship.kind == 'shares-secret'` (T-X5-0's own named trust edge). Signal names say "proxy," not the real thing, deliberately. Age tracking requires `--history` of real prior Session Packs, none of which exist yet in this repo — age is honestly "unknown" until a real multi-session series is run. |
| **T-RT-4-reviewer-assistance-advisory** | Reviewer-assistance advisory layer (`AGENT_TASKS_Ext_Review_Throughput.md`'s T-RT-4, BACKLOG.md "Reviewer-assistance layer (advisory, off the generation path)") — `tools/review-session/advisory.py`: for an open residual, calls an LLM to produce plain-language explanation + hypotheses and, optionally, ONE catalogue-rule candidate, in a fixed JSON shape (`explanation`/`hypotheses`/`catalogue_rule_candidate`). **Never writes a fact** — a stricter boundary than T-FS-1's `draft_tier_b.py`: `parse_and_validate_response` rejects ANY response containing a `decision` or `override` key outright, and this file never touches `drafts/decisions` or `drafts/overrides` at all. Attaches the result as an additive `advisory` field on the residual in `residuals.json` (`residuals-schema.json` updated, same pattern T-RT-2's `consequence` field already established) AND regenerates `advisory/report.md` (`render_advisory_report`) — a human-readable rendering of every advised residual, explicitly labeled "Advisory only... never a decision", the real review-queue surface this task's own acceptance wording means (found missing on self-review: the first pass only attached the raw JSON field with no rendered surface at all, the same role `queue_rank.py`'s own `render_report` already plays for T-RT-2's `consequence` field — added same pass, not a separate follow-up). Never acted on automatically; a human accepting a hypothesis still goes through the SAME existing card → answer → Decision Record path, unchanged by this task, and counts as one human judgement, not a second corroborating signal. A proposed catalogue-rule candidate is appended to `advisory/catalogue-rule-candidates.json` in the same `ProposedRule` shape `suggest-rules.ts` already writes to `rules/proposed-updates.json` — reusing the existing catalogue lane (this directory's own README rule 11) rather than a second promotion mechanism; still never auto-merged into `signal-catalogue.yml`. Every residual processed, regardless of outcome, appends one episode (model name, input context — the evidence refs actually given, timestamp, outcome) to `advisory/episodes.json` | **partial, synthetic-fixture-evidenced only** | `tools/review-session/test_advisory.py` (32 tests): the never-a-fact guardrail (a `decision`/`override` key present is rejected outright, regardless of how well-formed), an invented file:line evidence ref anywhere in the free-text `explanation`/`hypotheses` fields OR `catalogue_rule_candidate.rationale` is rejected (the latter found missing and fixed on self-review — the evidence-ref scan initially covered only two of the three free-text fields) — the same practical, prose-adapted form of `draft_tier_b.py`'s own "no invented ref" hard rule, `catalogue_rule_candidate`'s shape validated against `rule-schema.ts`'s real `CatalogueRule` fields/enums, a duplicate candidate id within one batch refused rather than silently overwriting the first, the input residual dict never mutated in place, `render_advisory_report`'s own output never reads as authoritative, and a real subprocess end-to-end no-key CLI run (writes nothing, creates no `advisory/` dir) plus `--residual` scoping | L1 (advisory text + a catalogue-rule proposal only); structurally cannot reach L1/L2 fact status on its own — no code path in this file or any caller ever constructs a Decision Record or Override from its output | **Same honest, disclosed gap `draft_tier_b.py` already carries**: no `ANTHROPIC_API_KEY` is set in the environment this was built in, so the live-model path (`_call_llm`) has never been exercised against a real API — the guardrail (`parse_and_validate_response`) is proven against synthetic response fixtures only, by the same deliberate design (the network boundary is one small, isolated, swappable function specifically so everything around it is testable without a live call). No trigger in `triage.py` currently produces a residual whose evidence would exercise this tool's real catalogue-rule-candidate path end-to-end against real source — same disclosed gap T-FS-1's Tier B path already carries for its own drafting mechanism. `advisory/report.md` is regenerated fresh from `residuals.json`'s own current state each run, not appended — unlike `episodes.json`/`catalogue-rule-candidates.json`, it is a derived view, not a log, so this is intentional, not an inconsistency. |

| **T-CL-2-incremental-merge** | Incremental merge + review history (BACKLOG.md "Fact identity, incremental merge, and review history", `AGENT_TASKS_Ext_Contract_Lifecycle.md` T-CL-2/T-CL-3) — `analysis/incremental-merge.ts`'s `mergeIncrementalFacts`, called from `run-slice.ts` right after this run's `TypedFacts` are assembled but before `finishRun`/`writeArtefacts` overwrite `<outDir>/typed-facts.json`: reads that same directory's PRIOR `typed-facts.json` (a contractVersion mismatch or a first run is treated as "no prior state", not an error), keyed by the stable `TypedUnit.id`/`TypedRelationship.id` T-CL-1 already guarantees. A fact whose PRIOR status was `'reviewed'` and whose signature is unchanged (unit: evidence array; relationship: kind/source/mechanism/confidence/grade, since `TypedRelationship` carries no `Evidence[]` of its own) carries `'reviewed'` forward untouched; one whose signature CHANGED is forced to `'requires-review'`, never silently kept `'reviewed'` and never silently promoted to whatever a fresh recompute would have produced. Every other fact keeps its freshly-computed status (already correct, deterministic from this run's own evidence). A fact absent from this run is never resurrected — recorded in history/report only, matching T-CL-1's own "disappeared fact plus a new one, not a bug" doc comment. Writes `<outDir>/merge-report.json` (this run's new/disappeared/unaffected/flagged-for-re-review counts) and appends (never overwrites) to `<outDir>/fact-history.json` via `analysis/fact-history.ts`'s `appendFactHistory` (T-CL-3 — "who/when/on-what-evidence a status changed is retrievable"; human-driven `'reviewed'` transitions already carry their own full audit trail on the Decision Record itself — `reviewer`/`reviewed_at`/`rationale`/`evidence_snapshot`, `types/overrides.ts` — so this file deliberately only records ANALYSIS-driven transitions, not a duplicate of that). T-CL-6 (determinism) is the end-to-end proof this merge logic is correct, not just unit-tested: two identical `run-slice` runs into the SAME `--out` directory must classify every fact `unaffected`, 0 new/disappeared/flagged | **proven** | Direct unit tests (`pipeline/test/regression.test.js`) covering all three real transitions: a genuinely new fact, an unaffected fact (including a `'reviewed'` one whose evidence is byte-identical — status carried forward, not overwritten), and a `'reviewed'` fact whose evidence genuinely changed (forced to `'requires-review'`, history records the `reviewed → requires-review` transition). A dedicated `fact-history.ts` test confirms `appendFactHistory` grows the log across calls rather than replacing it. Real end-to-end CLI verification (not just unit tests) against `test/fixtures/nestjs-sample`: run 1 into a fresh `--out` dir reports 1 new/0 unaffected; run 2 into the SAME dir (unchanged source) reports 0 new/1 unaffected, `fact-history.json` unchanged (no spurious transition recorded); manually stamping the on-disk `typed-facts.json`'s unit `status: 'reviewed'` and rerunning with unchanged source carries `'reviewed'` forward; then editing the fixture (adding a real `@Delete` route, a genuine evidence change) and rerunning flips that same unit to `'requires-review'` with a real history entry recording `reviewed → requires-review` — the exact "no reviewed fact silently overwritten, contradicting evidence flags for re-review" rule, verified against real CLI output, not assumed from the code. T-CL-6's own determinism test (`pipeline/test/regression.test.js`) runs `run-slice` twice into one `--out` directory against `test/fixtures/nestjs-sample` and asserts the sorted, generatedAt-excluded unit/relationship/status snapshot is byte-identical, and that the second run's `merge-report.json` shows 0 new/0 disappeared/0 flagged | L1 (fact-level status-transition correctness across reruns), synthetic-unit-test + real-fixture-CLI verified; not L2 — no detection logic changed, only how a fact's status is carried or challenged across runs | Claim triple: `{rootSet: test/fixtures/nestjs-sample (real CLI verification) + synthetic unit-test facts, terminalGrain: per-fact status-transition correctness across two runs of the same --out directory, evalArtefact: pipeline/test/regression.test.js's own assertions, not a standing exam}`. `IgnoredItem` stays out of scope (no `status` field), same as T-CL-1 already noted. Merge only wires into the fresh-scan `runSlice` path, not `--from-facts` reconstruct-only mode (that mode's `typed-facts.json` already IS frozen prior state — nothing to merge it against). |
| **T-CL-4-contract-bump** | Contract bump (`AGENT_TASKS_Ext_Contract_Lifecycle.md` T-CL-4, `Contract_Evolution_Policy.md` §2(c)) — `CONTRACT_VERSION` `13.0.0` → `14.0.0`: `TypedUnit.status`, `TypedRelationship.status`, `TypedRelationship.id` promoted optional → REQUIRED. A real new-required-field change (not a closed-union extension) because `assignStatusPass`/`factIdentityPass` are both unconditional, always-last `DEFAULT_PASSES` entries — the type now states a guarantee the pipeline already honored in practice. Every producer that builds a `TypedUnit`/`TypedRelationship` literal before those passes run was updated to satisfy the type with a shared construction-time placeholder (`PENDING_STATUS`/`PENDING_RELATIONSHIP_ID`, `types/typed-facts.ts`), unconditionally overwritten by the two always-last passes | **proven** | `tsc` build enforces the required fields at every one of the ~12 producer call sites across `analysis/` (a real compile-time check, not just a runtime assertion). Direct regression test (`pipeline/test/regression.test.js`, "T-CL-4") asserts `CONTRACT_VERSION === '14.0.0'`, runs a real `run-slice` scan against `test/fixtures/stereotype-disambiguation-sample`, and confirms every unit in the written `typed-facts.json` carries a real `status` and every relationship carries a real, non-placeholder `id`/`status` — i.e. the two always-last passes really do overwrite every placeholder in a real run, not just in unit-test isolation. Also asserts `--from-facts` refuses a `contractVersion: "13.0.0"` input against this build's `14.0.0` (`process.exit(1)`, existing major-version-gate logic, unchanged by this task). `calm validate` reverified 0 errors against two real fixtures (`nestjs-sample`, `stereotype-disambiguation-sample`) post-bump. All three modules' `supportedMajorVersion` reviewed and bumped to `"14"` in the same commit, each with a stated reason (`Contract_Evolution_Policy.md`'s `14.0.0` changelog row) | L1 (contract-shape correctness, compile-time + real-run verified); not L2 — no detection logic changed | Claim triple: `{rootSet: test/fixtures/stereotype-disambiguation-sample + nestjs-sample, terminalGrain: every produced unit/relationship carries a real (non-placeholder) status/id after a real run, evalArtefact: pipeline/test/regression.test.js's own assertions + tsc's compile-time enforcement, not a standing exam}`. The placeholder values (`PENDING_STATUS`/`PENDING_RELATIONSHIP_ID`) are never themselves a claim about a fact's real status/id — they exist only to satisfy the type between a producer's construction and the two always-last passes overwriting them within the same run; no code path reads a unit/relationship between those two points. |
| **T-CL-1-fact-identity** | Fact identity scheme (BACKLOG.md "Fact identity, incremental merge, and review history", `AGENT_TASKS_Ext_Contract_Lifecycle.md`) — every `TypedUnit.id` already derived from semantic coordinates per-producer (filePath / `cdxgen:<root>:<name>` / `<fileKey>::<suffix>` / `<relativeFilePath>::<className>` / `codeql-di:<root>:<relativeFilePath>` / `unresolved:<graphifyNodeId>`), audited and confirmed, never file:line or a run-scoped counter — only its doc comment was stale, fixed. `TypedRelationship.id` (new, additive optional field, tier (b) per `Contract_Evolution_Policy.md` §2, no `CONTRACT_VERSION` bump) is a real mechanism-class bug fix found building this task: `relationship-builder.ts`'s CALM `unique-id` was a positional `rel-${i}` counter, the exact "never a run-scoped counter" anti-pattern this task exists to forbid — it changed on rerun whenever producer ordering or count varied even though the relationship set itself hadn't. `analysis/fact-identity.ts`'s `computeRelationshipId`/`assignFactIds` (new `factIdentityPass`, `analysis/passes.ts`, after `gradeRelationshipsPass`) computes `kind\|from\|to\|(mechanism-or-source)` once per run; `relationship-builder.ts` now reuses it directly as `unique-id` instead of computing its own | **proven** | Direct unit test (`computeRelationshipId`/`assignFactIds`, `pipeline/test/regression.test.js`) asserting a plain edge and a specialized-mechanism edge between the identical two units get distinct, stable ids, and that computing twice from unchanged inputs reproduces the identical id (the property a positional counter cannot hold). The pre-existing `B-duplicate-relationship-objects` test (2 different raw `kind` values between the same real pair) was updated from asserting `rel-0` to asserting the real content-derived id, verified as a second, different instance of the same "stable relationship identity" mechanism class. Real end-to-end verification: two independent `run-slice` runs against `test/fixtures/stereotype-disambiguation-sample` (fresh `.graphify-cache` each time) produce byte-identical relationship `unique-id` sets in the generated CALM, `calm validate` 0 errors both times. `TypedUnit.id`'s `unresolved:<graphifyNodeId>` case was verified empirically, not assumed: a real `graphify extract` two-run diff (2026-08-20, `test/fixtures/nestjs-sample`, `graphifyy` 0.9.34) confirmed graphifyy's own node ids are content-derived from the symbol's own file+name (`_make_id`), not a counter — the one existing id shape whose stability genuinely wasn't obvious from reading the TypeScript side alone | L1 (fact-level identity correctness), synthetic + real-fixture-determinism verified; not L2 — this task changes no detection logic, only how an already-produced fact is named | Claim triple: `{rootSet: test/fixtures/stereotype-disambiguation-sample + synthetic unit-test relationships, terminalGrain: relationship unique-id stability across reruns, evalArtefact: pipeline/test/regression.test.js's own assertions, not a standing exam}`. A file rename (which changes an endpoint `TypedUnit.id`) or a producer/mechanism change on an existing edge legitimately changes the derived relationship id too — by design, read by T-CL-2's incremental merge as a disappeared fact plus a new one, not patched around here (`scope-limitations.yml`'s `fact-identity-endpoint-coordinates-only`). `IgnoredItem` was deliberately left untouched — it carries no `status` field, so it isn't in scope for T-CL-2/T-CL-3's merge-and-history requirement. **Review pass (2026-08-20) found and fixed two real second instances of the same mechanism class rather than leave them as disclosed risk**, per `CLAUDE.md`'s "second, different instance" bug-fix discipline: `k8s-trust-detector.ts` could push 2+ `shares-secret` facts for the same pair when 2+ real Secrets are shared (neither this id nor the pre-existing CALM-level dedup can tell them apart, since neither carries a secret name) — fixed with the same per-pair `seenPairs` dedup `env-soft-graph-detector.ts` already used for an analogous shape, new regression test asserting exactly 1 relationship from 2 shared secrets. `port-interface-builder.ts`'s CalmInterface id (`iface-port-${i}`, a positional index into a sorted port list — deterministic per-run but not content-derived) was the same anti-pattern one layer down from `TypedRelationship`; fixed to `iface-port-<port>`, verified directly. Both fixes rebuilt/retested: `npm test` 96/0/28 (was 95/0/28 before this pass, +1 new test), coe-lab core gates + `calm validate` + the two-run determinism check all re-verified green after the fixes, not just after the original T-CL-1 change. |
| **T-MR-3-k8s-deployed-in** | Kubernetes-derived `deployed-in` runtime-placement relationships (`AGENT_TASKS_Ext_MultiRepo_Deployment.md` T-MR-3, BACKLOG.md) — the placement half of the k8s manifest provider; `R-k8s`'s trust half (`shares-secret`) was already built. `analysis/cross_package/k8s-deployment-detector.ts`'s `detectK8sDeployedInRelationships` resolves each real `Deployment` to its TypedUnit via the SAME `findUnitForDeployment` correlation `k8s-trust-detector.ts`/`env-soft-graph-detector.ts` already share (service-kind-only, exact-then-normalized-substring), emitting one `deployed-in` `TypedRelationship` per resolved Deployment (`to: k8s-namespace:<namespace>`), deduped when 2+ manifests name the same unit+namespace; an unresolved Deployment is a named `CROSS_DOMAIN_UNRESOLVED` ignored-item, never guessed. `modules/calm-generator/k8s-namespace-node-builder.ts` builds the namespace as a real `node-type: system` CALM node (never a `TypedUnit` — a namespace has no source file), built BEFORE `relationship-builder.ts` runs so `deployed-in`'s `container` endpoint already resolves. `relationship-type-mapping.yml` gained the row activating `relationship-builder.ts`'s pre-existing (previously unreachable) `deployed-in` branch. Flat/pre-rendered manifests only, same `OOS-helm-kustomize` boundary as the rest of this provider. `TypedRelationship.kind` gained `'deployed-in'` — `CONTRACT_VERSION` `14.0.0` → `15.0.0`, closed-union extension, tier (c) | **proven** | Real end-to-end verification (2026-08-20, manual CLI run against `test/fixtures/nestjs-sample` + a hand-written Deployment manifest, `spikes/boa/repo` not present in this environment so the gated real-repo test below is honestly `skip`ped here, not silently omitted): `[k8s-trust] 1 deployment(s) read, ... 1 deployed-in relationship(s), 0 unresolved`, generated `architecture.calm.json` carries the real `k8s-namespace:prod` `system` node and a `deployed-in: {container: "k8s-namespace:prod", nodes: ["src/users.controller.ts"]}` relationship, `x-aac-relationship-grade: structural`, `x-aac-status: externally-verified` (generic `rel.source === 'k8s'` rule, unchanged), `calm validate` 0 errors/0 warnings. **Self-review found and fixed a real correctness bug before shipping, not after**: `relationship-grading.ts`'s generic service-endpoint fallback initially graded every `deployed-in` edge `'architecture'` — the SAME grade `coverage-report.ts`'s `architectureOutboundCoverage` and `hitl-review-trigger.ts`'s `low-architecture-coverage` trigger both filter on, both documented as meaning specifically "R1 one-hop or R2 bridge-resolved, never structural/trust" (`typed-facts.ts`'s own `grade` field doc comment). Left unfixed, a service unit whose ONLY relationship is a k8s placement fact would have silently counted as having real architecture-outbound coverage, suppressing S1/low-architecture-coverage review flags for a genuinely under-connected service — a real false-negative against the `S-silence` claim below, not a cosmetic label. Fixed with an explicit `rel.kind === 'deployed-in' -> 'structural'` branch in `relationship-grading.ts` (same explicit-branch pattern `shares-secret -> 'trust'` already used), reusing the existing `'structural'` enum value rather than adding a new one (confirmed no code branches on `grade === 'structural'` specifically — only the `'architecture'` filter needed to exclude it), so no further `CONTRACT_VERSION` bump. Regression suite: `detectK8sDeployedInRelationships` unit test (resolve/unresolve/dedup), `buildK8sNamespaceNodes` unit test (dedup across relationships, ignores non-`deployed-in` kinds), a synthetic `buildCalm` mapping test asserting the real CALM `deployed-in` shape (not `connects`), a second real instance added to the existing T-FS-6 status-assignment test (`deployed-in` also reads `externally-verified`, generalizing the rule beyond `shares-secret`), a direct `gradeRelationships` unit test asserting `deployed-in -> 'structural'` while a real service->database edge in the same call still grades `'architecture'`, a direct `computeCompleteness` regression test locking in the actual bug scenario (a service whose only relationship is `deployed-in` must count 0/1 covered, not 1/1), and a real-manifest end-to-end test gated on `spikes/boa/repo` (self-derives its own expected namespace from `discoverDeployments`, not hardcoded — same discipline as the pre-existing `shares-secret` real-case test) | L1 (a real, per-Deployment placement fact); never L2 — this doesn't attempt a full architecture story, only "which namespace does this service run in," and is now structurally excluded from ever being mistaken for real service connectivity | Claim triple: `{rootSet: test/fixtures/nestjs-sample (manual real verification) + spikes/boa/repo (gated, not run in this environment), terminalGrain: one deployed-in relationship per resolved Deployment + its namespace node, evalArtefact: pipeline/test/regression.test.js's own assertions, not a standing exam}`. Same disclosed limits as `R-k8s`'s own provider: `DeploymentManifest.image` is the FIRST container only, no multi-container modeling; a Deployment naming no resolvable unit is a real, disclosed gap, not silently dropped. |
| **T-MR-5-boundary-override** | Decision Record boundary-change overrides (`AGENT_TASKS_Ext_MultiRepo_Deployment.md` T-MR-5, BACKLOG.md) — `boundary_change` was a named `OverrideType` value since the schema's original design but unimplemented (`override-applier.ts` reported it `skipped`, "recognized but not yet implemented," per `Capabilities.md`'s prior row). Now moves a target node's `composed-of` container-boundary membership: removes it from any `composed-of` relationship it currently belongs to (dropping that relationship entirely if it would otherwise carry zero members, same "no degenerate empty construct" instinct as `node_remove`'s own relationship cleanup) and, unless `new_value.container` is explicitly `null`, adds it to the named container's `composed-of` (merging into an existing one if present, else creating a new one) — the first override type to touch the `composed-of` relationship shape at all (`relationship_add` stays `connects`-only by design, per `isValidConnectsRelationship`'s own doc comment). Same DR-enforcement, orphan/reject discipline as every other override type: an unresolved `decision_record_ref` or inactive Decision Record rejects; a `target_ref` or non-null `container` that doesn't resolve to a real node is an orphan (not just a rejection — same `T-X6-2` classification); a malformed `new_value` shape (missing/wrong-typed `container`) rejects, not orphans | **proven** | Real end-to-end verification (2026-08-20, manual CLI run against `test/fixtures/nestjs-sample`): a `node_add` + `boundary_change` override pair applied via `--overrides`, `[write-artefacts] overrides: 2 applied, 0 rejected, 0 skipped`, generated `architecture.calm.json` carries the real `domain-boundary--composed-of--override` relationship (`composed-of: {container: "domain-boundary", nodes: ["src/users.controller.ts"]}`), `x-aac-override-provenance`/`x-aac-status: reviewed` on the touched relationship, `calm validate` 0 errors/0 warnings. Regression suite: 6-case unit test mirroring the existing `relationship_add`/`relationship_remove` DR-enforcement test's own structure — (1) move between two existing boundaries (old composed-of dropped since it had exactly one member, new one created, `x-aac-status: reviewed`), (2) `container: null` removes from the only boundary with no replacement, (3) unresolved DR rejects, (4) unresolved `target_ref` orphans, (5) unresolved non-null `container` orphans (no dangling composed-of container), (6) malformed `new_value` rejects (not orphans — a different failure class) | L1 (a real, auditable node-to-boundary reassignment mechanism); no detection logic — same "Analysis concludes, Override corrects" separation every prior override type already holds, verified again here for the new type | Claim triple: `{rootSet: test/fixtures/nestjs-sample (manual real CLI verification) + synthetic unit-test CalmDocuments, terminalGrain: composed-of container membership after applying one boundary_change override, evalArtefact: pipeline/test/regression.test.js's own assertions, not a standing exam}`. A node can belong to at most one `composed-of` boundary under this override (moving it always vacates any prior one first) — a deliberate simplicity choice, not evidenced as a real multi-boundary-membership need; revisit only if a real case requires a node in 2+ boundaries simultaneously. |
| **T-CL-5-emission-coverage** | Emission-coverage rule (`AGENT_TASKS_Ext_Contract_Lifecycle.md` T-CL-5, BACKLOG.md "Emission-coverage reporting as a governed rule") — "what the representation could not carry, and why" as a required emission output, not an implicit assumption. `modules/calm-generator/emission-coverage.ts`'s `EmissionCoverageGap`/`buildEmissionCoverageReport`: gaps are recorded INLINE, by the three builders that already silently dropped facts at their own pre-existing `continue`/filter points — `node-builder.ts` (a `TypedUnit` whose `kind` has no `node-type-mapping.yml` row), `relationship-builder.ts` (a `TypedRelationship` whose endpoint was never emitted as a node, whatever the cause), `control-builder.ts` (a `security-control`-category evidence entry with no `control-requirement-catalogue.yml` row) — never re-derived independently after the fact, which would risk drifting out of sync with what the builders actually decide. `write-artefacts.ts` writes `modules/calm-generator/emission-coverage-report.json` (module-private, same convention as `overrides-applied-report.json` — only calm-generator's own builders decide CALM representability, unlike the pre-CALM, cross-module `coverage-report.json`/`unmapped-signals-report.json`) every real run, computed BEFORE overrides are applied (overrides patch already-emitted output; they can't create or resolve a builder-time gap). Not a `TypedFacts`/`CONTRACT_VERSION` change — a new module output, not a new fact-schema field or closed-union value (`Contract_Evolution_Policy.md` §2 doesn't apply) | **proven** | Two synthetic `buildCalm`/`buildEmissionCoverageReport` unit tests (`pipeline/test/regression.test.js`, "T-CL-5"): the first constructs one real instance of each of the three independent drop mechanisms (an unmapped unit kind, a relationship whose destination cascades to a dropped node, an unmapped security-control signal) and asserts a real gap with the real reason string for each, plus the exact expected `coverageRatio` (0.25: 1 of 4 considered facts represented); the second — a real, different second instance per `CLAUDE.md`'s bug-fix discipline — asserts a MAPPED security-control signal (`PreAuthorize`, the same real catalogue row `security-rbac-001` uses) produces zero gaps and `coverageRatio: 1`. A third, real end-to-end test runs `run-slice` against the checked-in `test/fixtures/nestjs-sample` fixture and asserts the written `emission-coverage-report.json` has the required shape (`represented + dropped === total` for all three sections, `gaps.length` matches the summed dropped counts) — verified live: 100% coverage, 0 gaps, `calm validate` still 0 errors/0 warnings on that fixture post-change. **Review fix (2026-08-20, same pass):** `attachControls`'s original `if (!node) continue` skipped a dropped unit's entire evidence loop before ever recording a control-stage gap, while `buildEmissionCoverageReport`'s `controlEvidenceTotal` counts all units' security-control evidence unconditionally — a unit dropped at the node stage that also carried security-control evidence would have had that evidence silently counted as "represented," inflating `coverageRatio` and contradicting the row's own "what could not be carried, and why" purpose. Found by both a manual self-review and an independent `/code-review` pass converging on the identical file:line; fixed to record a `control`-stage gap for that evidence too (reason: "unit was never emitted as a node"), with a dedicated regression test for the exact shape | L1 (a real, mechanical count of what a real run's own builders dropped and why); not L2 — no detection or matching logic changed, only whether an existing silent drop point is now recorded | Claim triple: `{rootSet: test/fixtures/nestjs-sample (real CLI verification) + synthetic unit-test facts covering all three drop mechanisms, terminalGrain: one gap entry per real dropped node/relationship/control-evidence fact plus an accurate coverageRatio, evalArtefact: pipeline/test/regression.test.js's own assertions, not a standing exam}`. Scoped to the three mechanisms that already had a real, pre-existing silent-drop point in calm-generator's builders — `interface-builder.ts`'s "no interface-worthy evidence" and `port-interface-builder.ts`'s malformed-port-value cases are cascades or edge shapes, not independent representability losses, and are deliberately not double-counted here. |
| **T-MR-1-repo-manifest** | Per-repo manifest (`AGENT_TASKS_Ext_MultiRepo_Deployment.md` T-MR-1, BACKLOG.md "Cross-repo relationship resolution (beyond co-scanned roots)") — a small, human-authored, checked-in file (`weaver-manifest.yml`) a target repo's own maintainers declare at their repo's root, naming what that repo publishes: `nodeId`/`name` per published contract, plus at least one of `apiSpec.title` (shared API-spec identity), `artifact.coordinates` (published artifact coordinate), or `serviceCatalogue.name`/`.dns` (service-catalogue/DNS) — the exact three tiers T-MR-2 resolves in order. `scanner/repo-manifest-provider.ts`'s `loadRepoManifest`/`discoverRepoManifests`: fails loudly on a malformed file or a publish entry declaring no identity a join could ever resolve against (same "fail loudly on a human-authored file" discipline `loadSignalCatalogue` already uses); empty array (not an error) when the manifests directory doesn't exist. **The pipeline never writes this file** — only ever reads copies a human has placed in a local directory for another repo's own run to join against (`--repo-manifests <dir>`, T-MR-2) | **proven** | `pipeline/test/fixtures/repo-manifests/other-service.weaver-manifest.yml` (real, checked-in, 3 publish entries, one per T-MR-2 tier) + `pipeline/test/regression.test.js`'s dedicated schema test: parses the real fixture correctly, a missing-directory call returns `[]` not an error, a publish entry with no identity throws, a manifest missing `repo`/`publishes` throws | L1 (a real, validated declaration schema); no detection logic of its own — this task produces no relationship or unit by itself, only the input T-MR-2 consumes | Claim triple: `{rootSet: pipeline/test/fixtures/repo-manifests (real checked-in fixture) + synthetic malformed-input unit tests, terminalGrain: one parsed RepoManifest per file / one thrown error per malformed input, evalArtefact: pipeline/test/regression.test.js's own assertions, not a standing exam}`. `nodeId` is never verified against anything (the publishing repo isn't scanned by the consuming run) — documentation for humans/future joins, not a checked invariant, stated explicitly in the provider's own doc comment. |
| **T-MR-2-cross-repo-join** | Ranked cross-repo joins (`AGENT_TASKS_Ext_MultiRepo_Deployment.md` T-MR-2, BACKLOG.md) — `analysis/cross_package/cross-repo-join-detector.ts`'s `detectCrossRepoJoins`, gated behind `--repo-manifests <dir>` (opt-in, `crossRepoJoinPass`, no-op when unset). Per package root, per manifest publish entry, tries exactly the acceptance text's own order — shared API-spec identity (this root's own `scanner/openapi-provider.ts`-parsed document `title`, exact match) → published artifact coordinates (this root's own `cdxgen`-derived SBOM `purl`/`name@version`, exact match) → service-catalogue/DNS (this root's own spring-config property VALUES, exact match, never substring) — stopping at the FIRST tier that resolves per (root, contract) pair, never collecting every tier that happens to match (verified directly: a synthetic entry carrying both a matching `apiSpec` and a matching `artifact` resolves via tier 1 only). A non-matching entry produces neither a relationship nor an ignored-item (a manifest entry is not itself a root-scoped correlation candidate the way a k8s Deployment name is — Tier 4, "human review," is the existing `relationship_add` override path, not an automatic step). Both endpoints are synthetic `node-type: system` CALM nodes (`modules/calm-generator/external-repo-node-builder.ts`, `repo-root:<root>` / `external-contract:<repo>\|<nodeId>`, same "no source file, build the node directly" pattern as `k8s-namespace-node-builder.ts`) — root granularity, never a specific local `TypedUnit`, since none of the three join inputs are attributable to one class/file more precisely than "this root produced this evidence." `TypedRelationship.source` gained `'repo-manifest'` — `CONTRACT_VERSION` `15.0.0` → `16.0.0`, closed-union extension, tier (c). Every relationship this mechanism produces is capped at `status: 'requires-review'` (`status-assignment.ts`, checked before any tier-specific signal — even Tier 1 never reads `'externally-verified'`) and `grade: 'structural'`, never `'architecture'` (`relationship-grading.ts`, explicit branch, same reasoning as `shares-secret`/`deployed-in`'s own explicit branches) — the task's own acceptance text ("review status at best") enforced as an absolute cap on the whole mechanism, not just its weakest tier | **proven** | Real end-to-end verification against THREE different checked-in fixtures, one per tier, not just the case that motivated the mechanism: (1) tier 1, `test/fixtures/openapi-sample`'s real `openapi.yaml` (`title: Widgets API`) resolves via `cross-repo-api-spec`; (2) tier 2, `test/fixtures/cdxgen-sample`'s real `requirements.txt` (`psycopg2==2.9.9`, real `cdxgen` shell-out, no mocking) resolves via `cross-repo-artifact` against `pkg:pypi/psycopg2@2.9.9`; (3) tier 3, `test/fixtures/spring-config-properties-sample`'s real `application.properties` (`spring.data.redis.host=prop-redis-host`) resolves via `cross-repo-service-catalogue`. All three: `calm validate` 0 errors/0 warnings, `x-aac-status: requires-review`, `x-aac-relationship-grade: structural`, the referenced `external-contract:...` node present as a real `node-type: system` node, not a dangling endpoint. Plus: a synthetic ranking test (dual-identity entry resolves tier 1 only, tier 2 never even checked), a dedup test (2 local OpenAPI docs matching one contract collapse to 1 relationship), a `buildCrossRepoNodes` unit test, a status-assignment unit test (Tier-1 mechanism still reads `requires-review`), a relationship-grading unit test (`repo-manifest` source grades `structural` while a real same-run service→database edge in the same call still grades `architecture`), and a no-op test (`--repo-manifests` absent produces zero cross-repo relationships regardless of local evidence). All three modules reviewed and bumped to `supportedMajorVersion: "16"` (`Contract_Evolution_Policy.md`'s changelog) | L1 (a real, per-pair ranked join, always reviewable); never L2 — this doesn't attempt a full architecture story, and is now structurally excluded from ever being mistaken for same-run-confirmed service connectivity (`grade` cap) or a settled fact (`status` cap) | Claim triple: `{rootSet: test/fixtures/openapi-sample + cdxgen-sample + spring-config-properties-sample (real checked-in fixtures, one per tier) + test/fixtures/repo-manifests (real checked-in manifest), terminalGrain: one connects relationship per resolved (root, published-contract) pair, evalArtefact: pipeline/test/regression.test.js's own assertions, not a standing exam}`. No real cross-organization two-repo pair was available in this environment (`spikes/` scratch clones absent) — a real, disclosed gap in evidence breadth (`scope-limitations.yml`'s `cross-repo-join-root-granularity-only`), not a claim of production-scale verification. Anchored at repo-root granularity only, same disclosed limit. |

---

## Product claims — allowed vs forbidden phrases

| Forbidden (without caveats) | Allowed |
|---|---|
| "Java relationships work end-to-end for every monorepo shape" | "JAX-RS/JPA units proven; R1 one-hop proven; R2 mechanism proven — **R2 recovers the layered-access story on the multi-root charge+provider case (E-charge-multi-story); the single-root charge module's gold L2 remains expected-fail (E-charge-single-L2)**; Python multi-hop and command-bus write paths still out/partial" |
| "R2 flagship case closed" (bare, no claim triple) | Always name the claim triple: `{rootSet, terminalGrain, evalArtefact}` — e.g. the allowed phrase directly above. See `R2-gold-charge-single` vs `R2-multi-root-access-terminal` rows: these are two different, non-substitutable claims about the same package. |
| "All security/controls captured for every Java API" | "Decorator `@PreAuthorize` proven; call-site vocabularies partial (`validateHas*Permission`, `hasRole`, `isAuthenticated`, Python `jwt.decode`) — not unbounded call-graph auth inference" |
| "Graphify recovers full architecture graph alone" | "Graphify supplies structural dual-unit edges (R0); architecture grade needs R1/R2 + catalogues" |
| "Every ORM import is a database owner" | "`@prisma/client` requires `extends PrismaClient` ownership; the AWS SDK's Dynamo clients (`@aws-sdk/client-dynamodb`, `com.amazonaws.services.dynamodbv2`, `software.amazon.awssdk.services.dynamodb`) require a real owned field (`ownerFieldType`, T-MR-4); plain type/parameter imports no longer create database units for either. Every other driver is still import-only unless catalogue gains `ownerBaseClass` or `ownerFieldType`" |
| "Messaging complete including cloud queues" | "Kafka field-type producer + SQS **import** evidence dispatched; SNS still unverified; no queue name / send-call resolution; dual-role class keeps persistence kind with merged messaging evidence" |
| "Cloud-native / serverless stack fully covered" / "HTTP works for all AWS deployments" | "Framework HTTP (**U-http**) proven; Dynamo/SQS **data plane** partial/proven on lab; **serverless HTTP (U-http-serverless) is Java+explicit-CFN proven, not universally covered** — SAM shorthand, Node/Python handlers, and cross-stack references are named residuals" |
| "Dynamo detection means cloud Java services are correct" | "Dynamo units now require a real owned field (`ownerFieldType`, T-MR-4), not just an import; a handler that ALSO owns a Dynamo client directly still wins `service` over `database` (kind tie-break, already-closed separate mechanism). Still only proven for the Java v1/v2 SDKs and the Node v3 SDK, on the fixtures cited in `U-persist-import` — not every cloud-Java shape" |

---

## Open questions — decide / park

| Topic | State | Decision / park |
|---|---|---|
| Definition of done: schema vs unit vs story | **decided** | Layered L0–L5 (`validation-approach-vnext.md`); story = L2 |
| Meaning of relationship: code vs architecture | **decided** | R0 structural ≠ architecture claim; R1+ for architecture |
| What incompleteness must be loud | **decided; shipped** | Service+db with zero service-touching edges must not L2-pass; `completeness.silenceFlags` (S1/S2) now computed per run, exposed in `coverage-report.json` + `intelligence-ir.md` |
| Dual-unit product boundary | **decided** | Interim structural (see above) |
| Disconfirming samples for relationship claims | **decided (process)** | A shallow Python-shaped sample and a layered Java-shaped sample are both required before claiming "relationships complete" for a language |
| Lab shape-representative? | **decided** | Lab under-represents layering; wild gold mandatory for enterprise claims |
| Wild gold owner | **parked** | Lab maintainer / eval owner; refresh on fixture or module-intent change |
| Validation stack | **decided; tooling shipped** | L0 calm validate; L1 semantic/unit; L2 story; L3 silence; L4 scope; L5 human-review residual. `validate-calm-pair.mjs` prints these as distinct lines (not one bundled "semantic" verdict); exit code = L0+L1 by default, `--require-l2` opts into gating on L2 |
| HITL → catalogue promotion | **parked** | Follow existing unmapped≥5 promotion; multi-hop residual separate |
| Forever OOS vs deferred | **partial** | Register + scope-limitations; refresh when strategies ship |
| Full monorepo vs module scan product mode | **decided** | **Module-root scan is the primary/default claim mode** — one `run-slice` invocation against a single package root is what every R0/R1/U-* mechanism-class cell above is proven/partial against, unless a cell's evidence explicitly says otherwise. **Multi-root scans are a real, supported, but secondary mode** — passing 2+ roots to one `run-slice` invocation runs ONE combined Graphify pass over their common ancestor, which is what makes cross-package (`crossPackage: true`) relationships possible at all (single-root runs structurally cannot produce them — there is nothing to cross). A claim of "N cross-package relationships found" is **only valid for the specific multi-root run that produced it** — re-running the same roots individually will show 0 cross-package edges, correctly, not a regression. Real evidenced case: a reference Java/JAX-RS banking platform's charge+core modules combined scan → 265 real cross-module edges (`docs/solution/language/java.md`). Written procedure: `coe-lab/docs/multi-root-l2-protocol.md` — root-set declaration template, generic root-set shapes, and the rule that any multi-root finding must name its exact root set. |
| Capabilities doc "built" vs mechanism subset | **decided** | This register is authoritative for completeness claims |

---

## Architecture-relation evidence-completeness binding

Architecture completeness claims also require the layered-story evidence discipline this program established. In short: R0 structural ≠ architecture L2; R2 + C-call (or OOS) before a layered-Java story goes green. **R2-gold-charge-single remains L2 expected-fail** (standing exam `E-charge-single-L2`); **R2-multi-root-access-terminal is mechanism-level PASS** for the layered-access story on the multi-root charge+provider root set (standing exam `E-charge-multi-story`) — see the three split R2 rows above, and `coe-lab/docs/standing-disconfirming-exams.md` for the frozen protocols. C-call remains `partial` (two vocabularies only) — a real relationship existing and a control being detected on the source/target service are still two independent claims; a relationship's existence does not imply its endpoints carry full control coverage.

---

## Pattern-conformance stance (T-LM-8, `AGENT_TASKS_Ext_Remaining_Lenses.md`)

**Decided 2026-08-20: external (a).** BR-50 names "architectural-pattern
conformance" as one of eight minimum lenses. Weaver does **not** implement a
pattern-matching engine. Consumers evaluate `architecture.calm.json` against
FINOS CALM patterns via the external `calm validate -p` tool. This matches
`OOS-pattern-conformance-engine` in `OOS_Registry.md` and the non-goal
already stated in `README.md` ("a CALM pattern-governance product").

**Claim language:** May say "Weaver generates CALM output that external
pattern-governance tooling (`calm validate -p`) can validate." Must **not**
say "Weaver checks/enforces architectural-pattern conformance" — no such
mechanism exists or is planned under this decision.

**Reversal condition:** see `OOS-pattern-conformance-engine`'s revisit
trigger — a real consumer needing conformance evaluated inside the
generation pipeline itself, not merely "would be convenient." If that fires,
scope is `T-LM-8b`, a new task, not a reopening of this row.

---

## Module fitness (T-LM-0, `AGENT_TASKS_Ext_Lens_Modules.md`)

Module outputs (`outDir/modules/<name>/`) are a **separate claim surface** from
the architecture-level rows above — a module can be well-tested and still be
`unevidenced` here if it has never been scored against real ground truth.
Per `BR-110`: an unmeasured lens may *inform* but must never *gate* a
governance decision. **T-LM-5 (2026-08-18)** made this table's own numbers
machine-readable, not only prose here: `modules/fitness.ts`'s
`loadModuleFitness()` reads a checked-in `fitness.json` per module and
attaches it to that module's own report JSON as `{findings, fitness}` —
deliberately never computed live against gold at run time (`CON-40`
isolation; gold is never present in a real customer run). Both rows below
were re-verified the same day this mechanism shipped: threat-signals 7/7,
resilience-lens 8/8, both exact-match. Re-run each module's own scorer and
hand-update its `fitness.json` before trusting these numbers against a
codebase state materially newer than 2026-08-18 — nothing catches this
table (or the checked-in JSON) drifting stale on its own.

| Module | Status | Evidence | Validation allowed green | Notes |
|---|---|---|---|---|
| **threat-signals** | **partial, now scored** | `coe-lab/gold/modules/threat-signals/*.gold.json` (7 packages, authored from fixture source per `CON-40`, verified against real module output only after independent authoring) + `coe-lab/scripts/score-module-threat-signals.mjs` | L1-equivalent (exact finding-set match) for the 7 scored coe-lab core/trap packages | Findings themselves are still bounded by the same C-dec/C-call detection coverage every other control claim is (see the mechanism-class matrix above) — a clean score here means "correctly reflects what this pipeline's own catalogue can see," never "confirmed absence of authorization in source." No real (`spikes/`) repo scored yet — coe-lab-only so far. |
| **resilience-lens** (T-LM-2) | **partial, now scored — narrow by design** | `coe-lab/gold/modules/resilience-lens/*.gold.json` (8 packages: 1 real positive on a **dedicated** fixture package, `java-resilience-handlers` — never `java-spring-payments`, which carries its own pre-existing architecture-level `gold/calm/`+`gold/packages/` gold this module-level gold must not couple to, per review fix 2026-08-16 — `CurrencyRateResource.list()` carries a real Resilience4j `@Retry(name=..., fallbackMethod=...)` plus a real resilience4j `timelimiter.instances.currencyRateLookup.timeout-duration` config value — and 7 must-not-detect traps across the other coe-lab core packages, all authored from fixture source per `CON-40`) + `coe-lab/scripts/score-module-resilience-lens.mjs` (verified to actually catch a mismatch: deliberately flipped the gold's `hasTimeout` to `false`, confirmed FAIL + exit 1, restored). **Second-instance verification is now real, not self-authored-only** (review fix 2026-08-16, `Catalogue_Intake.md`'s requirement): Resilience4j's `@Retry(name=..., fallbackMethod=...)` is independently confirmed in a reference Java/JAX-RS banking platform's core module under `spikes/` — 49 real files use this exact shape; a real `run-slice` scan of that module detects 7 of them (`CurrencyUpdateCommandHandler.java` and 6 others), confirming the mechanism against real, independently-authored source, not a fixture pair authored in the same diff. `pipeline/test/fixtures/resilience4j-retry-sample` remains as a fast, isolated regression fixture for the same shape, not the primary evidence anymore. **Spring Retry's `@Retryable` and the `resilience4j.timelimiter.instances.*.timeout-duration` config key have NO real `spikes/` evidence** — checked across all 4 available reference repos (zero hits for either), disclosed honestly rather than left to stand on the self-authored fixture pair alone; see `scope-limitations.yml`'s `resilience-lens-retry-timeout-only` for the full disclosure, including the real, different config vocabulary (`resilience4j.retry.instances.*.wait-duration`) the reference platform actually uses. CONTRACT_VERSION 12.0.0 (`Evidence.category` gained `'resilience'`). | L1-equivalent (exact hasRetry/hasTimeout match) for the 8 scored coe-lab packages + 3 dedicated `pipeline/test` regression fixtures + 1 real `spikes/` module (`fineract-core`, resilience4j `@Retry` only, not gold-scored — a real detection-count check, not a hand-authored gold comparison) | **Deliberately narrow, per the lane file's own gate ("not 'resilience' scoped whole")**: exactly 2 retry annotations (Spring Retry, Resilience4j) and 1 timeout-config shape (resilience4j `timelimiter`). Circuit-breaker/bulkhead/rate-limiter annotations, non-Spring/non-Java retry or timeout mechanisms (Python `tenacity`, Node `p-retry`, generic `HttpClient` connect/read-timeout config), and code-only (non-annotation, non-config) resilience handling are real, named residuals. **Also disclosed (review fix 2026-08-16):** the two catalogue rows match bare decorator names only, disambiguated by language/matchSource, never framework/package — the same collision class already named for `nestjs-graphql-resolver-bootstrap-only`'s `@Query`/`@Mutation` rows (Micronaut ships its own `@Retryable`; hand-rolled AOP retry aspects commonly use `@Retry`) — not reproduced against a real conflicting case, so not a confirmed bug, but a structurally real risk. See `scope-limitations.yml`'s `resilience-lens-retry-timeout-only` for all of the above, not silently claimed covered. |
| **Every other lens** (vulnerability, data-flow/lineage, green-engineering) | `specified-unbuilt` — **Phase 2 (2026-08-19)** | Session A checked each gate (no CVE feed; no classification facts; no cost feed). Not a current-phase build. See `BACKLOG.md` Phase 2. | None | Reopen only with a real feed/signal + gold/scorer (`T-LM-0` pattern) before any governance gate. |
