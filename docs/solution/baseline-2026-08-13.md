# Baseline — 2026-08-13

**Task:** T-P0-0 (`AGENT_TASKS_Ext_P0_Experiments.md`). Captured before any edit
on `feature/semantic-model-extension`, per `AGENT_TASKS_Semantic_Model_Extension.md`
§3. Every later claim in this extension is a delta against this file.

**Commit:** `f74bb6a` · **Branch:** `feature/semantic-model-extension` · **Node:** v22.16.0

## 1. `npm test` (pipeline/)

```
# tests 89
# suites 0
# pass 64
# fail 0
# cancelled 0
# skipped 25
# todo 0
```

Matches the expected 64/0/25 stated in `AGENT_TASKS_Semantic_Model_Extension.md` §3 and
`CLAUDE.md`'s "Build and run" section exactly.

## 2. coe-lab — generate + calm validate (`--all-core`, 7 packages)

`node scripts/generate-calm.mjs --all-core` — all 7 packages: `calm validate ok`.

Note: every package's Graphify cross-package pass reconciled **0 relationships**
(`graphify: 0 relationship(s) reconciled`) across all 7 runs. Consistent with
the hard-gate table in the master task file: `graphifyy` is **not present** on
this machine's `PATH`. Per `CLAUDE.md`, this degrades cross-package detection
silently rather than failing the build — confirmed here as the observed
behaviour, not just the documented one. All packages in `--all-core` are
single-root scans in this run, so this does not affect L0/L1/L2 pass/fail
below, but it means **no cross-package relationship signal was exercised in
this baseline** — relevant context for any future claim that depends on it.

## 3. coe-lab — validate-calm-pair (`--all-core`)

| Package | L0 (gold) | L0 (gen) | L1 unit recall | L2 story |
|---|---|---|---|---|
| py-accounts-api | PASS | PASS | PASS | PASS |
| py-ledger-worker | PASS | PASS | PASS | PASS |
| ts-nestjs-users | PASS | PASS | PASS | N/A — gold has no connects-shaped relationships |
| java-jaxrs-charges | PASS | PASS | PASS | N/A — gold has no connects-shaped relationships |
| java-spring-payments | PASS | PASS | PASS | N/A — gold has no connects-shaped relationships |
| java-rbac-datatable | PASS | PASS | PASS | N/A — gold has no connects-shaped relationships |
| lib-fintech-common | PASS | PASS | PASS | N/A — trap/empty gold, no architecture asserted |

`ALL PASS (7 package(s))`.

## 4. coe-lab — scoreboard

Ran `bash scripts/run-eval.sh <pkg>` for each of the 7 core/trap packages to
populate `eval-results/` (gitignored), then `node scripts/scoreboard.mjs`:

| package | tier | pass (L0/L1) | node P/R | iface R | rel R (L2 proxy) | ctrl# | viol |
|---|---|---|---|---|---|---|---|
| java-jaxrs-charges | core | true | 1/1 | 1 | 1 | 0 | 0 |
| java-rbac-datatable | core | true | 1/1 | 1 | 1 | 2 | 0 |
| java-spring-payments | core | true | 1/1 | 1 | 1 | 0 | 0 |
| lib-fintech-common | trap | true | 1/1 | 1 | 1 | 0 | 0 |
| py-accounts-api | core | true | 1/1 | 1 | 1 | 0 | 0 |
| py-ledger-worker | core | true | 1/1 | 1 | 1 | 0 | 0 |
| ts-nestjs-users | core | true | 1/1 | 1 | 1 | 1 | 0 |

All 7 packages pass the L0/L1 gate; `lib-fintech-common` (the must-not-detect
trap referenced by E2's acceptance criteria) currently passes clean — 0
violations, 0 controls, node P/R 1/1. This is the reference point E2's
must-not-detect check (§4 of the P0 lane file) must not regress.

## Isolation note

This baseline run used `coe-lab/scripts/run-eval.sh` and `validate-calm-pair.mjs`,
which read `coe-lab/gold/**` internally to score. No gold file was opened
directly by this session — per `CLAUDE.md`'s CoE Lab isolation table, running
the provided scoring scripts (not hand-reading gold JSON) is the sanctioned
path even for an implementation session establishing a baseline.

## Environment gates (re-verified, matches §1 of the master task file)

| Prerequisite | State |
|---|---|
| `spikes/` sample repos | Absent |
| CodeQL CLI | Not installed |
| `graphifyy` on PATH | Not present (confirmed via 0 cross-package relationships above) |
| coe-lab fixtures + scripts | Present, exercised above |
