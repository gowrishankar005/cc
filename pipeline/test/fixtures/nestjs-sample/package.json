{
  "name": "nestjs-sample-fixture",
  "version": "0.0.1",
  "private": true,
  "description": "Minimal real NestJS fixture, used to verify the pipeline's genericity beyond the Flask proxy repo used for the initial build.",
  "dependencies": {
    "@nestjs/common": "^10.0.0",
    "@nestjs/core": "^10.0.0"
  }
}
