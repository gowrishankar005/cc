package example;

import javax.persistence.Entity;
import javax.persistence.Table;

/** Real store — the T-LR-2 direct-delegate hop's target. */
@Entity
@Table(name = "gizmos")
public class GizmoEntity {
}
