# Lab monorepo fixtures

Minimal fintech-shaped packages for **evaluation**. These are intentionally small and honest.

Run the platform against `packages/<id>` directories (or multiple roots). Do not treat this as production code.

**Gold lives in `../../gold/` — not here.** Implementation agents should not open gold.
