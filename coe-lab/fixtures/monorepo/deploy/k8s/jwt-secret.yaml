# Lab fixture — shared JWT signing material name only (no real key material).
apiVersion: v1
kind: Secret
metadata:
  name: jwt-key
  namespace: lab
type: Opaque
stringData:
  jwtRS256.key: "LAB-PLACEHOLDER-PRIVATE-NOT-A-REAL-KEY"
  jwtRS256.key.pub: "LAB-PLACEHOLDER-PUBLIC-NOT-A-REAL-KEY"
