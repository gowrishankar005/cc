# Lab fixture — real datasource config, for spring-config-pass.ts's
# structured-config-derived database-unit detection (server.port +
# spring.datasource.* -> a real `database (postgresql)` CALM node).
server.port=8080
spring.datasource.url=jdbc:postgresql://localhost:5432/orders
