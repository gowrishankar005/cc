# Trap / negative-gold backlog (Wave 1-B3)

Cards for shapes that must **fail loudly** or **OOS-disclose** if completeness is claimed.  
Not all need fixtures yet — cards guide Wave 3 eval (W3-eval).

| ID | Family | Intent | Sample / gold | Layer that must fail if completeness claimed |
|---|---|---|---|---|
| T1 | R2 multi-hop | API does not import entity; store still real | fineract-charge gold L2 | **PROMOTED (T-C1/T-R1-2/T-R1-3, re-confirmed T-R3-3 2026-08-08)** — multiple automated tests: synthetic positive fixture (`r2-bridge-sample`, `r2b-implementer-hop-sample`), real Fineract `fineract-charge`-alone honest-residual test (0 fabricated relationships), and the real multi-root flagship-closure test (`ChargesApiResource -> ChargeReadPlatformServiceImpl`, confidence 10, `calm validate` 0 errors) |
| T2 | C-call HTTP auth | Permission checks are calls not decorators | fineract ChargesApi; lab jwt-gateway | **PROMOTED (T-D1/T-D2/T-R2-2, re-confirmed T-R3-3)** — "AREC T-D1/T-D2 — call-site control detection" tests (Fineract `security-rbac-002`, lab jwt-gateway `security-auth-001`), extended by T-R2-2's Waltz `security-rbac-003` and Fineract `security-auth-002` tests |
| T3 | Utility not service | Pure helpers must not be services | lab lib-fintech-common | **PROMOTED (T-R3-3, 2026-08-08)** — new automated expected-pass test ("Robustness T-R3-3 (trap-gold T3 promoted)"): `lib-fintech-common` (pure `StringUtils`/`Money` classes, zero catalogue evidence of any kind) produces ZERO CALM nodes, `calm validate` 0 errors — this card had a real fixture built for it but, until this round, was never actually wired into the regression suite |
| T4 | Controller + ORM types | HTTP file imports client types for DTO only | ghostfolio AccessController | **PROMOTED (Wave M, re-confirmed T-R3-3)** — "Node/TS real evidence (ghostfolio/ghostfolio, NestJS+Prisma)" test asserts the Controller/database precedence fix directly, against real source |
| T5 | K8s name ≠ entity | Deployment must not bind to JPA entity | BoA Transaction.java FP fixed | **PROMOTED (T-E6, re-confirmed T-R3-3)** — "Deployment correlation — Java Controller-class naming resolves via normalize+substring, never matches a database/topic unit" test, real full-BoA evidence |
| T6 | Messaging producer only | send() without listener | Fineract/lab producer path | **PROMOTED (T-E1, re-confirmed T-R3-3)** — "AREC T-E1 — messaging PRODUCER" test: real Fineract `KafkaExternalEventProducer.java` detected as a `network`-kind node from its own scan (no listener present in that module), grep-verified provenance line |
| T7 | Dynamo/SQS cloud import | Cloud native units | lab ts-orders-dynamo | **FULLY PROMOTED (T-E3, re-confirmed T-R3-3, SQS side closed B-msg-prod-sqs 2026-08-08)** — same "AREC T-E3" test, extended: now also asserts the real merged SQS messaging evidence (`ref_aws_sdk_client_sqs`, category `messaging`) survives onto `OrdersDynamoStore` alongside its DynamoDB persistence evidence, not silently dropped by the earlier file-level exclusion. Both halves of this fixture's real dual-role shape (persistence AND messaging on one class) are now proven, not just the persistence half. |
| T8 | OpenAPI dual unit | Spec + controller same routes | lab ts-nestjs-users | **PROMOTED (T-E4, 2026-08-08)** — automated expected-pass test in `pipeline/test/regression.test.js` ("AREC T-E4 — OpenAPI dual-unit merge"). Merge policy: overlapping normalized routes -> one unit; 0 or 2+ overlaps fall back to standalone (never guessed) |

**8/8 traps promoted (7/8 as of T-R3-3, 2026-08-08; T7 fully closed same-day by B-msg-prod-sqs).** T-R3-3's own acceptance bar was "≥2 traps promoted" — that round found 6 of 8 already substantively covered by real, pre-existing tests never marked as such here (a doc-sync gap, not new coverage), and built the one genuinely missing case (T3). T7 was left honestly partial (DynamoDB proven, SQS producer side undetected) until B-msg-prod-sqs closed the SQS half same-day — now full.

Promotion: when Wave 3 implements a cell, convert card to automated expected-fail or expected-pass test.
